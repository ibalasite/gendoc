{
  "id": "local_deploy-block1",
  "project": "erp",
  "html_file": "local_deploy.html",
  "block_idx": 1,
  "mode": "replay",
  "source_kind": "md",
  "source_file": "docs/LOCAL_DEPLOY.md",
  "input_md": "# LOCAL_DEPLOY — Local Development Deployment Guide (Rancher Desktop + Kubernetes)\n\nERP API Token Manager — local first-day onboarding guide. Following this document, a new engineer can bring up the complete K8s-based local environment in under 5 minutes without asking anyone for help.\n\n---\n\n## Document Control\n\n| Field | Value |\n|-------|-------|\n| **DOC-ID** | LOCAL_DEPLOY-ERP-TOKEN-20260508 |\n| **Project** | ERP API Token Manager |\n| **Project Slug** | erp-token |\n| **GitHub Org** | erp-org |\n| **GitHub Repo** | erp-api-token-manager |\n| **Version** | v1.0 |\n| **Status** | DRAFT |\n| **Author** | Engineering Team |\n| **Date** | 2026-05-08 |\n| **Upstream EDD** | [EDD.md](EDD.md) §3.3 / §16 / §17 |\n| **Upstream RUNBOOK** | [RUNBOOK.md](RUNBOOK.md) §1.1 / §2.3 |\n| **Last Verified** | 2026-05-08 |\n| **Verified By** | Engineering Team |\n\n---\n\n## 1. Prerequisites\n\nInstall and verify every tool before proceeding. Setup will not work without exact version minimums. The stack follows EDD §3.3：C# 12 / .NET 8 LTS / ASP.NET Core 8 (Razor Pages) / EF Core 8 / PostgreSQL 16 / Kubernetes 1.29 (Rancher Desktop k3s)。\n\n| Tool | Min Version | Install | Verify |\n|------|------------|---------|--------|\n| macOS | 13.0+ | — | `sw_vers -productVersion` |\n| **Rancher Desktop** | 1.13 | [rancherdesktop.io](https://rancherdesktop.io) | `rdctl version` |\n| kubectl | 1.29 | Bundled with Rancher Desktop | `kubectl version --client` |\n| helm | 3.14 | Bundled with Rancher Desktop | `helm version` |\n| nerdctl | 1.7 | Bundled with Rancher Desktop | `nerdctl version` |\n| **.NET SDK** | 8.0.100 | `brew install --cask dotnet-sdk` 或 [dotnet.microsoft.com/download/dotnet/8](https://dotnet.microsoft.com/download/dotnet/8) | `dotnet --version` |\n| **dotnet-ef** (global) | 8.0.0 | `dotnet tool install -g dotnet-ef` | `dotnet ef --version` |\n| Node.js (Tailwind 編譯) | 20 LTS | `brew install node@20` | `node --version` |\n| k9s（選配）| 0.32 | `brew install derailed/k9s/k9s` | `k9s version` |\n| skaffold（選配）| 2.11 | `brew install skaffold` | `skaffold version` |\n| psql client | 15 | `brew install libpq && brew link libpq` | `psql --version` |\n| mkcert | 1.4 | `brew install mkcert` | `mkcert --version` |\n| Git | 2.40 | `brew install git` | `git --version` |\n| Make | 4.3 | `brew install make` | `make --version` |\n| curl | 8.0 | Pre-installed | `curl --version` |\n| jq | 1.7 | `brew install jq` | `jq --version` |\n\n> **Rancher Desktop 設定：** 啟動後進入 **Preferences > Virtual Machine > Resources**，至少配置 **8 GB RAM / 4 CPU**（依 EDD §16.2.1 Local 資源限制：CPU 500m + Mem 512Mi × 2 replicas + DB 1 GB ≈ 4 GB；預留 50% 緩衝）。Container engine 選 **containerd**（nerdctl）。\n>\n> **kubectl context：** Rancher Desktop 啟動後會自動注入 `rancher-desktop` context。執行 `kubectl config use-context rancher-desktop` 確認。\n>\n> **Tailwind CDN vs CLI：** 開發環境預設透過 CDN 載入 Tailwind 與 Alpine.js（FRONTEND.md §2.3，無 build step）。若要產生 production CSS bundle，使用 `npm install` + `npx tailwindcss -i ./src/ErpApplication/wwwroot/css/input.css -o ./src/ErpApplication/wwwroot/css/site.css`，因此保留 Node.js 20 LTS 為選配工具。\n\n---\n\n## 2. Architecture Overview\n\n本地環境完全在 Kubernetes 內執行，與 staging / production 採用相同 K8s 資源模型（Deployment、Service、ConfigMap、Secret、Ingress）。開發者透過 Ingress（traefik，k3s 內建）或 `kubectl port-forward` 存取各服務。\n\n**所有應用服務均在 namespace `erp-token-local` 內。**\n\n```\n┌───────────────────────────────────────────────────────────────────────────┐\n│  Rancher Desktop (k3s)  —  namespace: erp-token-local                     │\n│                                                                           │\n│  Ingress (traefik)  ← http://erp-token.local（需設定 /etc/hosts）         │\n│         │                                                                 │\n│         ▼                                                                 │\n│  ┌─────────────────────────────────────┐                                  │\n│  │  erp-api-token-manager (api-server) │                                  │\n│  │  Deployment, replicas: 2            │                                  │\n│  │  Razor Pages + REST API + Auth MW   │                                  │\n│  │  :8080  (http)                      │                                  │\n│  └────────────────┬────────────────────┘                                  │\n│                   │ TCP 5432 (psycopg)                                    │\n│                   ▼                                                       │\n│  ┌──────────────────────┐    ┌──────────────────────┐                     │\n│  │  postgres            │    │  pgadmin             │                     │\n│  │  StatefulSet (1)     │    │  Deployment          │                     │\n│  │  postgres:16-alpine  │    │  dpage/pgadmin4      │                     │\n│  │  :5432               │    │  :80 (web UI)        │                     │\n│  └──────────────────────┘    └──────────────────────┘                     │\n│                                                                           │\n│  本地觀測（選配）：Jaeger all-in-one (UI :16686, OTLP :4317)              │\n└───────────────────────────────────────────────────────────────────────────┘\n```\n\n```mermaid\ngraph TB\n    Dev[\"Developer<br/>(localhost)\"]\n    Ingress[\"Ingress (traefik)<br/>erp-token.local\"]\n\n    subgraph K8s[\"K8s Namespace: erp-token-local\"]\n        API[\"erp-api-token-manager<br/>Deployment (replicas=2)<br/>:8080\"]\n        DB[(\"postgres<br/>StatefulSet<br/>:5432\")]\n        PGA[\"pgadmin<br/>Deployment<br/>:80\"]\n        Jaeger[\"jaeger-aio (optional)<br/>Deployment<br/>UI :16686 / OTLP :4317\"]\n    end\n\n    Dev -->|HTTP via Ingress / port-forward| Ingress\n    Ingress -->|/| API\n    Ingress -->|/api/| API\n    Ingress -->|/health/*| API\n    Ingress -->|/pgadmin| PGA\n    Ingress -->|/jaeger| Jaeger\n    Dev -->|kubectl port-forward 5432| DB\n    Dev -->|kubectl port-forward 5050| PGA\n    API -->|ClusterIP TCP 5432| DB\n    API -.->|OTLP :4317 (optional)| Jaeger\n```\n\n**K8s 資源對照（完整清單，與 §3/§4/§5 完全對齊）：**\n\n| 服務 | Kind | Image | ConfigMap | Secret | Resource Limits |\n|------|------|-------|-----------|--------|-----------------|\n| erp-api-token-manager | Deployment (replicas: 2) | `erp-token/api:local` | `erp-token-api-config` | `erp-token-api-secret` | CPU 500m / Mem 512Mi（per pod，依 EDD §16.2.1 Local 欄）|\n| postgres | StatefulSet (replicas: 1) | `postgres:16-alpine` | `erp-token-db-config` | `erp-token-db-secret` | CPU 500m / Mem 1Gi |\n| pgadmin | Deployment (replicas: 1) | `dpage/pgadmin4:8.x` | — | `erp-token-pgadmin-secret` | CPU 200m / Mem 256Mi |\n| jaeger-aio（選配）| Deployment (replicas: 1) | `jaegertracing/all-in-one:1.57` | — | — | CPU 200m / Mem 512Mi |\n\n> **K8s manifest 位置：** `k8s/overlays/local/`（Kustomize）。Base 在 `k8s/base/`，與 staging / production overlays 共用，僅資源大小、replicas、ConfigMap 不同。\n>\n> **imagePullPolicy：** 所有 Deployment / StatefulSet 設定 `imagePullPolicy: Never`，確保使用本機 `nerdctl build` 產出的 image，不從 registry 拉取（containerd image store 與 k3s 共用）。\n>\n> **本地不部署：** Redis（EDD §3.3：Cache=None），Worker / CronJob（EDD §3.3：MQ=None），MinIO / S3（無物件儲存需求），mailpit（無 outbound email），Prometheus / Grafana（觀測平台統一在 staging 起，本地以 stdout JSON log + 選配 Jaeger 即可）。\n\n### 2.1 Bounded Context（單一 Modular Monolith）\n\n依 ARCH §4.1 Context Map 與 RUNBOOK §1.3，本模組為單一 Modular Monolith deployment（同 process 內三個 sub-context）：\n\n| Sub-Context | Module Path（C# namespace） | Owning DB Tables | Public Surface |\n|------------|-----------------------------|------------------|----------------|\n| api-token-mgmt | `ErpApi.Domain.ApiToken` + `ErpApi.Application.ApiToken` | `api_tokens`, `token_audit_logs` | `/api/tokens/*`, `/ApiTokens/*`（Razor）|\n| api-token-auth | `ErpApi.Infrastructure.Auth` | `api_tokens`（read-only hot path） | 全域 middleware（套用 `/api/poc/*`） |\n| poc-demo-api | `ErpApi.Presentation.PocController` | None（mock data） | `/api/poc/sales`, `/api/poc/inventory`, `/api/poc/orders/{id}/status`, `/api/poc/health` |\n\n> 本地環境不做跨 BC stub（無微服務拆分需求）；BC 隔離測試由 ArchUnit / Test 覆蓋（見 test-plan.md §3）。\n\n---\n\n## 3. Quick Start（5 分鐘上手）\n\n適合熟悉 K8s 的工程師。初次設定請使用 §4 Step-by-Step。\n\n```bash\n# 1. Clone 並進入專案\ngit clone git@github.com:erp-org/erp-api-token-manager.git\ncd erp-api-token-manager\n\n# 2. 確認 kubectl context 為 Rancher Desktop\nkubectl config use-context rancher-desktop\n\n# 3. 設定 /etc/hosts（一次性）— 讓 Ingress domain 可解析\necho \"127.0.0.1   erp-token.local api.erp-token.local\" | sudo tee -a /etc/hosts\n\n# 4. 建立 namespace 與 Secret（執行 bootstrap script，見 §3.5）\nmake k8s-init\n\n# 5. Build 並載入 image（containerd image store）\nmake image-build-all\n\n# 6. 部署所有 K8s 資源\nmake k8s-apply\n\n# 7. 等待所有 Pod 就緒\nkubectl wait --for=condition=Ready pods --all -n erp-token-local --timeout=180s\n\n# 8. 初始化資料庫（EF Core migration + seed）\nmake db-migrate\nmake db-seed\n\n# 9. 驗證所有服務健康\nmake health-check\n```\n\n預期輸出：\n\n```\n[OK] api-server   http://erp-token.local/health/live   (200 Healthy)\n[OK] api-server   http://erp-token.local/health/ready  (200 Healthy)\n[OK] postgres     pod/postgres-0 — Running\n[OK] pgadmin      pod/pgadmin-<hash> — Running\n[OK] api-server   pod/erp-api-token-manager-<h1> — Running (1/2)\n[OK] api-server   pod/erp-api-token-manager-<h2> — Running (2/2)\n```\n\n如有任何 `[FAIL]`，請進入 §10 Common Issues。\n\n---\n\n## 3.5 Secret Bootstrap（密碼安全管理）\n\n> **原則**：所有 K8s Secret **禁止進 git**。禁止使用靜態明文密碼（如 `password: secret`）。本節定義三層密碼策略，確保本地開發環境的安全性符合企業標準。\n\n### 禁止規則\n\n以下檔案 **必須** 加入 `.gitignore`，違規者視為安全事故：\n\n```gitignore\n# Secret files — never commit\n*.env\n.env\n.env.*\nsecrets.env\nk8s/overlays/local/secrets.env\nscripts/bootstrap-secrets.sh.local\n```\n\n驗證：\n```bash\ngrep -E '\\.env|secrets\\.env' .gitignore && echo \"OK: .gitignore covers secrets\" || echo \"ERROR: .gitignore missing secret patterns\"\ngit check-ignore -v k8s/overlays/local/secrets.env\n# Expected: 顯示 .gitignore 規則命中\n```\n\n### 三層 Secret 策略\n\n| 層 | 類型 | 適用密碼 | 管理方式 |\n|----|------|---------|---------|\n| **層 1** | Ephemeral（每次重生成）| `DB_PASSWORD`、`HMAC_SECRET`（v2 預留）、`PGADMIN_DEFAULT_PASSWORD`、`DATA_PROTECTION_KEY` | 每次 `make k8s-init` 或 `make secrets-rotate` 重生成（openssl rand）|\n| **層 2** | OS Keychain（固定憑證）| `GHCR_TOKEN`（image push）、`NUGET_API_KEY` | macOS Keychain / Windows Credential Manager |\n| **層 3** | Enterprise Password Manager（可選）| `JAEGER_AUTH_TOKEN`（若啟用 Jaeger UI auth）| 1Password `op inject` / Bitwarden Secrets Manager |\n\n---\n\n### 層 1：Ephemeral 密碼 Bootstrap（必做）\n\n每次重啟 cluster 或執行 `make k8s-clean` 後，執行以下 bootstrap script 重新生成所有密碼。\n\n**macOS / Linux — `scripts/bootstrap-secrets.sh`：**\n\n```bash\n#!/usr/bin/env bash\n# scripts/bootstrap-secrets.sh\n# Auto-generate ephemeral K8s secrets — never commit this output\nset -euo pipefail\n\nNAMESPACE=\"${NAMESPACE:-erp-token-local}\"\nPROJECT_SLUG=\"erp-token\"\n\necho \"[bootstrap-secrets] Generating ephemeral secrets for namespace: ${NAMESPACE}\"\n\n# Ensure namespace exists\nkubectl create namespace \"${NAMESPACE}\" --dry-run=client -o yaml | kubectl apply -f -\n\n# Delete existing secrets to force regeneration\nkubectl delete secret \"${PROJECT_SLUG}-api-secret\" -n \"${NAMESPACE}\" --ignore-not-found\nkubectl delete secret \"${PROJECT_SLUG}-db-secret\"  -n \"${NAMESPACE}\" --ignore-not-found\nkubectl delete secret \"${PROJECT_SLUG}-pgadmin-secret\" -n \"${NAMESPACE}\" --ignore-not-found\n\n# Generate ephemeral values\nDB_PASSWORD=\"$(openssl rand -hex 32)\"\nHMAC_SECRET=\"$(openssl rand -hex 64)\"\nDATA_PROTECTION_KEY=\"$(openssl rand -hex 32)\"\nPGADMIN_PASSWORD=\"$(openssl rand -base64 16 | tr -dc 'a-zA-Z0-9' | head -c 16)\"\n\n# api-server connection string (Npgsql format)\nCONNECTION_STRING=\"Host=postgres;Port=5432;Database=erpapikey;Username=erpapp;Password=${DB_PASSWORD}\"\n\n# api-server Secret\nkubectl create secret generic \"${PROJECT_SLUG}-api-secret\" \\\n  -n \"${NAMESPACE}\" \\\n  --from-literal=ConnectionStrings__ApiTokenDb=\"${CONNECTION_STRING}\" \\\n  --from-literal=HmacSecret=\"${HMAC_SECRET}\" \\\n  --from-literal=DataProtection__Key=\"${DATA_PROTECTION_KEY}\"\n\n# postgres bootstrap Secret\nkubectl create secret generic \"${PROJECT_SLUG}-db-secret\" \\\n  -n \"${NAMESPACE}\" \\\n  --from-literal=POSTGRES_DB=\"erpapikey\" \\\n  --from-literal=POSTGRES_USER=\"erpapp\" \\\n  --from-literal=POSTGRES_PASSWORD=\"${DB_PASSWORD}\"\n\n# pgadmin Secret\nkubectl create secret generic \"${PROJECT_SLUG}-pgadmin-secret\" \\\n  -n \"${NAMESPACE}\" \\\n  --from-literal=PGADMIN_DEFAULT_EMAIL=\"admin@erp-token.local\" \\\n  --from-literal=PGADMIN_DEFAULT_PASSWORD=\"${PGADMIN_PASSWORD}\"\n\necho \"[bootstrap-secrets] OK: Ephemeral secrets created in ${NAMESPACE}\"\necho \"[bootstrap-secrets] Secrets are ephemeral — regenerated on every cluster restart\"\necho \"[bootstrap-secrets] pgadmin login: admin@erp-token.local / ${PGADMIN_PASSWORD}\"\n```\n\n```bash\n# 每次 cluster 啟動時執行\nchmod +x scripts/bootstrap-secrets.sh\n./scripts/bootstrap-secrets.sh\n\n# 或透過 make target（§6 Development Commands 已提供）\nmake secrets-rotate\n```\n\n**Windows — `scripts/bootstrap-secrets.ps1`：**\n\n```powershell\n# scripts/bootstrap-secrets.ps1\n# Auto-generate ephemeral K8s secrets — never commit this output\nparam(\n    [string]$Namespace = \"erp-token-local\"\n)\n\n$ProjectSlug = \"erp-token\"\n\nWrite-Host \"[bootstrap-secrets] Generating ephemeral secrets for namespace: $Namespace\"\n\n# Ensure namespace exists\nkubectl create namespace $Namespace --dry-run=client -o yaml | kubectl apply -f -\n\n# Delete existing secrets\nforeach ($s in @(\"$ProjectSlug-api-secret\",\"$ProjectSlug-db-secret\",\"$ProjectSlug-pgadmin-secret\")) {\n    kubectl delete secret $s -n $Namespace --ignore-not-found\n}\n\n# Helper: generate random hex string with CSPRNG\nfunction New-RandomHex([int]$Bytes) {\n    $rng = [System.Security.Cryptography.RandomNumberGenerator]::Create()\n    $arr = New-Object byte[] $Bytes\n    $rng.GetBytes($arr)\n    return ([System.BitConverter]::ToString($arr) -replace '-','').ToLower()\n}\n\n$DbPassword         = New-RandomHex 32\n$HmacSecret         = New-RandomHex 64\n$DataProtectionKey  = New-RandomHex 32\n$PgadminPassword    = (New-RandomHex 12).Substring(0,16)\n\n$ConnectionString = \"Host=postgres;Port=5432;Database=erpapikey;Username=erpapp;Password=$DbPassword\"\n\nkubectl create secret generic \"$ProjectSlug-api-secret\" -n $Namespace `\n    --from-literal=\"ConnectionStrings__ApiTokenDb=$ConnectionString\" `\n    --from-literal=\"HmacSecret=$HmacSecret\" `\n    --from-literal=\"DataProtection__Key=$DataProtectionKey\"\n\nkubectl create secret generic \"$ProjectSlug-db-secret\" -n $Namespace `\n    --from-literal=\"POSTGRES_DB=erpapikey\" `\n    --from-literal=\"POSTGRES_USER=erpapp\" `\n    --from-literal=\"POSTGRES_PASSWORD=$DbPassword\"\n\nkubectl create secret generic \"$ProjectSlug-pgadmin-secret\" -n $Namespace `\n    --from-literal=\"PGADMIN_DEFAULT_EMAIL=admin@erp-token.local\" `\n    --from-literal=\"PGADMIN_DEFAULT_PASSWORD=$PgadminPassword\"\n\nWrite-Host \"[bootstrap-secrets] OK: Ephemeral secrets created in $Namespace\"\nWrite-Host \"[bootstrap-secrets] pgadmin login: admin@erp-token.local / $PgadminPassword\"\n```\n\n```powershell\n# 執行\n.\\scripts\\bootstrap-secrets.ps1\n# 或指定 namespace\n.\\scripts\\bootstrap-secrets.ps1 -Namespace \"erp-token-local\"\n```\n\n**重生成時機：**\n\n```bash\n# 每次執行以下任何一個動作後，必須重跑 bootstrap script\nmake k8s-clean        # 刪除 namespace（所有 secret 一起被刪）\nmake cluster-reset    # 完全重建 cluster\nmake secrets-rotate   # 主動輪換（安全需求或 secret 洩漏後）\n```\n\n---\n\n### 層 2：OS Keychain 固定憑證（視需求）\n\n適用於**不能每次重生成**的憑證。對 ERP API Token Manager 開發者主要為 GHCR Token（image push）。\n\n**macOS Keychain（`security` CLI）：**\n\n```bash\n# 儲存憑證到 macOS Keychain（首次設定）\nsecurity add-generic-password \\\n  -s \"erp-token-ghcr\" \\\n  -a \"dev\" \\\n  -w \"<YOUR_GHCR_PAT_TOKEN>\"\n\n# 讀取憑證並登入（CI dry-run 或本機 push）\nGHCR_TOKEN=$(security find-generic-password -w -s \"erp-token-ghcr\" -a \"dev\")\necho \"${GHCR_TOKEN}\" | nerdctl login ghcr.io --username erp-org-bot --password-stdin\n```\n\n**Windows Credential Manager（PowerShell）：**\n\n```powershell\nInstall-Module -Name CredentialManager -Force\nNew-StoredCredential -Target \"erp-token-ghcr\" -UserName \"erp-org-bot\" -Password \"<YOUR_GHCR_PAT_TOKEN>\"\n\n$cred = Get-StoredCredential -Target \"erp-token-ghcr\"\n$token = $cred.GetNetworkCredential().Password\necho $token | nerdctl login ghcr.io --username erp-org-bot --password-stdin\n```\n\n---\n\n### 層 3：mittwald/kubernetes-secret-generator（進階選項）\n\n如果偏好 **in-cluster 全自動生成**（無需手動執行 bootstrap script）：\n\n```bash\nhelm repo add mittwald https://helm.mittwald.de\nhelm upgrade --install secret-generator mittwald/kubernetes-secret-generator \\\n  -n kube-system\n```\n\n```yaml\n# k8s/overlays/local/secrets.yaml — 範例（可選方案）\napiVersion: v1\nkind: Secret\nmetadata:\n  name: erp-token-api-secret\n  namespace: erp-token-local\n  annotations:\n    secret-generator.v1.mittwald.de/autogenerate: \"HmacSecret,DataProtection__Key\"\n    secret-generator.v1.mittwald.de/length: \"64\"\n    secret-generator.v1.mittwald.de/type: \"string\"\ntype: Opaque\n```\n\n> **何時用 mittwald**：當不需要在 bootstrap script 中讀取明文密碼（app 直接從 K8s Secret 讀取）時較便利。但 `ConnectionStrings__ApiTokenDb` 與 `POSTGRES_PASSWORD` 必須兩處同值，仍建議走層 1 openssl rand 流程。\n\n---\n\n### 驗證 Secret 已正確建立\n\n```bash\n# 確認 secret 存在（不顯示值）\nkubectl get secret -n erp-token-local\n\n# 確認所有 key 都存在\nkubectl get secret erp-token-api-secret -n erp-token-local \\\n  -o jsonpath='{.data}' | python3 -c \"\nimport sys, json\nkeys = list(json.load(sys.stdin).keys())\nrequired = ['ConnectionStrings__ApiTokenDb', 'HmacSecret', 'DataProtection__Key']\nmissing = [k for k in required if k not in keys]\nprint('Missing:', missing) if missing else print('OK: All required api secrets present')\n\"\n\n# 確認 .gitignore 涵蓋 secret 檔案\ngrep -E '\\.env|secrets\\.env' .gitignore && echo \"OK: .gitignore\" || echo \"ERROR: .gitignore missing secret patterns\"\n```\n\n---\n\n## 4. Step-by-Step Setup\n\n### 4.1 Clone & Configure\n\n```bash\ngit clone git@github.com:erp-org/erp-api-token-manager.git\ncd erp-api-token-manager\n\n# 確認 branch\ngit status\n# Expected: On branch main / develop\n```\n\n### 4.2 Rancher Desktop 設定\n\n```bash\n# 1. 確認 k3s context 已注入\nkubectl config get-contexts\n# 應看到 rancher-desktop 並標示 *（current）\n\n# 若未自動設定：\nkubectl config use-context rancher-desktop\n\n# 2. 確認 K8s API server 可連線\nkubectl cluster-info\n# Expected: Kubernetes control plane is running at https://127.0.0.1:6443\n\n# 3. 建立本地 namespace\nkubectl create namespace erp-token-local --dry-run=client -o yaml | kubectl apply -f -\n\n# 4. 設定 /etc/hosts（一次性）— 讓 Ingress domain 可解析\necho \"127.0.0.1   erp-token.local api.erp-token.local\" | sudo tee -a /etc/hosts\n\n# 驗證 /etc/hosts 設定生效\ngetent hosts erp-token.local || dscacheutil -q host -a name erp-token.local\n# Expected (macOS): 127.0.0.1   erp-token.local\n```\n\n### 4.3 建立 K8s Secret\n\nSecret 不走 ConfigMap，不提交到 git。Local 環境全為開發隨機值，由 `scripts/bootstrap-secrets.sh` 自動生成，**無需人工填寫**：\n\n```bash\n# 自動建立 namespace + 三個 Secret（呼叫 §3.5 bootstrap script）\nmake k8s-init\n# 等同：\n# bash scripts/bootstrap-secrets.sh\n\n# 若改採 secrets.env + --from-env-file 流程（不推薦於 local，但 staging 沿用此模式）：\n[[ ! -f k8s/overlays/local/secrets.env ]] && \\\n  cp k8s/overlays/local/secrets.example.env k8s/overlays/local/secrets.env\nkubectl create secret generic erp-token-api-secret \\\n  --from-env-file=k8s/overlays/local/secrets.env \\\n  -n erp-token-local \\\n  --dry-run=client -o yaml | kubectl apply -f -\n```\n\n> `k8s/overlays/local/secrets.env` 已加入 `.gitignore`。`secrets.example.env` 是變數名稱的唯一來源，新增變數時需同步更新。`--from-env-file` 比 `--from-literal` 安全：避免密碼出現在 shell history 與 process list。\n\n### 4.4 Build & 載入 Image\n\nRancher Desktop 使用 containerd runtime，需用 `nerdctl` 建置並直接載入到 k3s 內部 image store（不需額外 push step）。\n\n```bash\n# 一次 build 全部 image（僅 api，無獨立前端 image — Razor Pages server-rendered）\nmake image-build-all\n\n# 等同（單一 image：包含 ASP.NET Core api + 編譯後的 Razor 視圖 + Tailwind 編譯產出）：\nnerdctl --namespace=k8s.io build \\\n  -t erp-token/api:local \\\n  -f docker/api/Dockerfile .\n\n# 確認 image 存在\nnerdctl --namespace=k8s.io images | grep erp-token\n# Expected: erp-token/api  local  <hash>  ...\n```\n\n> **imagePullPolicy: Never** — 本地環境所有 Deployment / StatefulSet 在 manifest 設定 `imagePullPolicy: Never`，確保使用本機 build 的 image，不嘗試從 ghcr.io 拉取。\n>\n> **`--namespace=k8s.io`** — Rancher Desktop 內 containerd image store 與 k3s 共用 namespace `k8s.io`；省略此 flag 會把 image 載入 `default` namespace，導致 Pod 找不到 image。\n>\n> **客戶端引擎特定 Build 說明**（依 EDD §3.3 `client_engine = ASP.NET Core 8 Razor Pages + Alpine.js + Tailwind`）：\n>\n> | 引擎類型 | Dockerfile 策略 | 額外前置步驟 |\n> |---------|----------------|-------------|\n> | **ASP.NET Core 8 Razor Pages**（本專案）| multi-stage：`mcr.microsoft.com/dotnet/sdk:8.0` build → `mcr.microsoft.com/dotnet/aspnet:8.0` runtime；同 image 內含 Razor views + 編譯後 Tailwind CSS（`wwwroot/css/site.css`） | （選配）若使用 Tailwind CLI mode：build stage 內 `npm ci && npx tailwindcss -i wwwroot/css/input.css -o wwwroot/css/site.css --minify`。預設 dev mode 從 CDN 載入，不需 npm step |\n>\n> > 不適用於本專案：Cocos Creator / Unity WebGL / Phaser 等遊戲引擎（FRONTEND.md §2.3.1 標記 N/A）。\n\n**Dockerfile 範例（`docker/api/Dockerfile`，與 EDD §3.5 Base Image 一致）：**\n\n```dockerfile\n# Stage 1: build\nFROM mcr.microsoft.com/dotnet/sdk:8.0 AS build\nWORKDIR /src\nCOPY *.sln ./\nCOPY src/ErpApplication/*.csproj         src/ErpApplication/\nCOPY src/Application/*.csproj            src/Application/\nCOPY src/Domain/*.csproj                 src/Domain/\nCOPY src/Infrastructure/*.csproj         src/Infrastructure/\nRUN dotnet restore\nCOPY src/ src/\nRUN dotnet publish src/ErpApplication/ErpApplication.csproj \\\n    -c Release -o /app/publish --no-restore\n\n# Stage 2: optional Tailwind compile (production CSS bundle)\nFROM node:20-alpine AS css\nWORKDIR /css\nCOPY src/ErpApplication/wwwroot/css/input.css ./\nCOPY src/ErpApplication/tailwind.config.js    ./\nRUN npm install -g tailwindcss@3.4.x \\\n && tailwindcss -i input.css -o site.css --minify\n\n# Stage 3: runtime\nFROM mcr.microsoft.com/dotnet/aspnet:8.0 AS runtime\nWORKDIR /app\nCOPY --from=build /app/publish .\nCOPY --from=css   /css/site.css ./wwwroot/css/site.css\nENV ASPNETCORE_URLS=http://+:8080\nEXPOSE 8080\nENTRYPOINT [\"dotnet\", \"ErpApplication.dll\"]\n```\n\n### 4.5 部署所有 K8s 資源\n\n```bash\n# 使用 Kustomize 部署 local overlay\nmake k8s-apply\n# 等同：\n# kubectl apply -k k8s/overlays/local/\n\n# 觀察 Pod 啟動狀態（Ctrl+C 結束）\nkubectl get pods -n erp-token-local -w\n\n# 等待全部 Ready\nkubectl wait --for=condition=Ready pods --all -n erp-token-local --timeout=180s\n```\n\n> **Iron Constraint — Replicas ≥ 2**：覆寫 EDD §16.2.1 中 `min_replicas=1` 的設定（已記錄於 RUNBOOK §1.2）。本地 K8s Deployment 必須 `replicas: 2` 以驗證 HA 程式邏輯（DataProtection key sharing、distributed cookie auth、ASP.NET Core distributed cache）。HPA `minReplicas` 同樣不得低於 2。\n\n合規 Deployment YAML（`k8s/overlays/local/deployment-patch.yaml`）：\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: erp-api-token-manager\n  namespace: erp-token-local\nspec:\n  replicas: 2  # 本地 HA 最小副本數，禁止填 1（覆寫 EDD §16.2.1）\n  template:\n    spec:\n      containers:\n        - name: app\n          image: erp-token/api:local\n          imagePullPolicy: Never\n          ports:\n            - containerPort: 8080\n```\n\n預期 Pod 狀態（全部 `Running`）：\n\n```\nNAME                                       READY   STATUS    RESTARTS   AGE\nerp-api-token-manager-<hash1>              1/1     Running   0          60s\nerp-api-token-manager-<hash2>              1/1     Running   0          60s\npostgres-0                                 1/1     Running   0          60s\npgadmin-<hash>                             1/1     Running   0          60s\n```\n\n副本數驗證：\n```bash\nkubectl get pods -n erp-token-local -l app=erp-api-token-manager\n# Expected: 至少 2 個 erp-api-token-manager Pod 狀態為 Running\n\n# 計數驗證\nkubectl get pods -n erp-token-local -l app=erp-api-token-manager \\\n  -o jsonpath='{.items[?(@.status.phase==\"Running\")].metadata.name}' | wc -w\n# Expected: 2\n```\n\n如有 Pod 停在 `Pending` 或 `CrashLoopBackOff`，請見 §10。\n\n### 4.6 初始化資料庫（EF Core Migration + Seed）\n\nEDD §3.3 ORM = Entity Framework Core 8 + Npgsql。Migration 命令：`dotnet ef database update`。\n\n```bash\n# 在 api-server pod 內執行 EF Core migration（推薦：與 production 部署同模式）\nmake db-migrate\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef database update --no-build --connection \"$ConnectionStrings__ApiTokenDb\"\n\n# 載入 seed / fixture 資料\nmake db-seed\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet run --project src/Infrastructure -- seed --env Local\n\n# 查看 migration 狀態\nmake db-status\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef migrations list --no-build\n```\n\n預期 migration 輸出：\n\n```\nBuild started...\nBuild succeeded.\nApplying migration '20260101_InitialCreate'.\nApplying migration '20260315_AddTokenExpiresAt'.\nDone.\n```\n\n驗證 schema 已建立：\n```bash\nkubectl exec -n erp-token-local statefulset/postgres -- \\\n  psql -U erpapp -d erpapikey -c \"\\dt\"\n# Expected:\n#  Schema |        Name             | Type  | Owner\n# --------+-------------------------+-------+--------\n#  public | __EFMigrationsHistory   | table | erpapp\n#  public | api_tokens              | table | erpapp\n#  public | token_audit_logs        | table | erpapp\n```\n\n### 4.7 驗證所有服務健康\n\n```bash\nmake health-check\n```\n\n個別驗證：\n\n```bash\n# Liveness（透過 Ingress）\ncurl -sf http://erp-token.local/health/live && echo \" ALIVE\"\n# Expected: Healthy ALIVE\n\n# Readiness（透過 Ingress；確認 DB 可連）\ncurl -sf http://erp-token.local/health/ready && echo \" READY\"\n# Expected: Healthy READY (HTTP 200)\n\n# API health（POC endpoint，需 Token 才能呼叫業務 API；health 為公開）\ncurl -s http://erp-token.local/api/poc/health | jq .\n# Expected: {\"status\":\"ok\",\"version\":\"1.0.0\",\"feature_flags\":{\"enable_poc_demo_apis\":true}}\n\n# PostgreSQL（port-forward 後）\nkubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &\nPG_PASSWORD=$(kubectl get secret erp-token-db-secret -n erp-token-local \\\n  -o jsonpath='{.data.POSTGRES_PASSWORD}' | base64 -d)\n# WARNING: 此密碼僅為本機開發隨機值，staging / production 環境禁止使用此 port-forward 流程。\nPGPASSWORD=\"${PG_PASSWORD}\" psql \"postgres://erpapp@localhost:5432/erpapikey\" -c \"SELECT 1;\"\n# Expected: 1 row\n```\n\n---\n\n## 5. Service Reference\n\n### Ingress 存取（需設定 /etc/hosts）\n\n| 服務 | URL | 說明 |\n|------|-----|------|\n| api-server (Razor Pages UI) | `http://erp-token.local/` | 主 UI（Login → /ApiTokens 列表）|\n| api-server (REST API) | `http://erp-token.local/api/tokens` | Token 管理 REST API |\n| api-server (POC API) | `http://erp-token.local/api/poc/{sales,inventory,orders/{id}/status,health}` | POC demo endpoints（Bearer Token）|\n| api-server (Health) | `http://erp-token.local/health/live`, `/health/ready` | Liveness / Readiness（非 Ingress 限制；亦可直連 Pod port-forward）|\n| pgadmin | `http://erp-token.local/pgadmin` | 資料庫瀏覽器（subpath 配置；登入見 §8）|\n| jaeger UI（選配） | `http://erp-token.local/jaeger` | 分散式追蹤 UI（若部署）|\n\n> **API 路由前綴：** Razor Pages 占用 `/`、`/Account/*`、`/ApiTokens/*`；REST API 占用 `/api/tokens/*`、`/api/poc/*`；Health Probe 占用 `/health/*`。Ingress 規則順序由長到短，最長前綴優先匹配（traefik 預設行為）。\n>\n> **Admin SPA：** has_admin_backend=false，本專案不適用獨立 Admin Portal SPA（管理介面為同 process Razor Pages `/ApiTokens`，由 `[Authorize(Roles=\"ITAdmin\")]` 控制）。\n\n### kubectl port-forward 存取（直連 Pod，繞過 Ingress）\n\n| 服務 | 指令 | 本地 URL |\n|------|------|---------|\n| api-server | `kubectl port-forward -n erp-token-local deploy/erp-api-token-manager 8080:8080` | `http://localhost:8080` |\n| postgres | `kubectl port-forward -n erp-token-local statefulset/postgres 5432:5432` | `localhost:5432` |\n| pgadmin | `kubectl port-forward -n erp-token-local deploy/pgadmin 5050:80` | `http://localhost:5050` |\n| jaeger UI（選配）| `kubectl port-forward -n erp-token-local deploy/jaeger-aio 16686:16686` | `http://localhost:16686` |\n\n> **Make shortcuts：** `make pf-api`、`make pf-db`、`make pf-pgadmin`、`make pf-jaeger` 會在背景啟動 port-forward 並記錄 PID。`make pf-stop` 停止全部。\n>\n> **Admin SPA Ingress：** has_admin_backend=false，無需 `/admin` 路徑路由設定。所有後台功能透過 Razor Pages `/ApiTokens/*`（Cookie 驗證 + RBAC `Roles=\"ITAdmin\"`）實作。如未來引入獨立 Admin SPA，需依 LOCAL_DEPLOY 模板 §5「Admin SPA Ingress 設定」補充 nginx SPA fallback 與 `base: '/admin/'` 打包設定。\n\n---\n\n## 6. Development Commands\n\n所有 `make` 指令從 repo root 執行。Makefile target 命名與 CI（`make ci-*`）對齊（見 CICD.md §8）。\n\n> **註：** 本表新增 target 時須同步更新 [DEVELOPER_GUIDE §3.1 Quick Reference](./DEVELOPER_GUIDE.md) 與 [CICD §4.1 標準 Make Target 清單](./CICD.md)（三處 SoT 共識）。\n\n### Image & Deployment\n\n| 指令 | 等同底層命令 | 使用時機 |\n|------|------------|---------|\n| `make image-build-api` | `nerdctl --namespace=k8s.io build -t erp-token/api:local -f docker/api/Dockerfile .` | 修改後端程式碼 / Razor 視圖 / Tailwind input.css 後 |\n| `make image-build-all` | `make image-build-api`（本專案僅一個 image，無獨立前端 image） | 初次設定或全面更新 |\n| `make k8s-apply` | `kubectl apply -k k8s/overlays/local/` | 變更 K8s manifest 後 |\n| `make k8s-restart-api` | `kubectl rollout restart deploy/erp-api-token-manager -n erp-token-local` | Build 新 image 後套用 |\n| `make k8s-restart-all` | `kubectl rollout restart deployment --all -n erp-token-local` | 更新 ConfigMap / Secret 後 |\n| `make k8s-delete` | `kubectl delete -k k8s/overlays/local/` | 重新部署時（保留 PVC）|\n| `make k8s-clean` | `kubectl delete namespace erp-token-local` | 完全重置（銷毀所有資料）|\n| `make k8s-init` | `bash scripts/bootstrap-secrets.sh` | 首次 setup 或 secrets-rotate |\n| `make secrets-rotate` | 同 `make k8s-init` 但會強制刪除舊 Secret | 安全事件後輪換 |\n\n### 觀察 & 偵錯\n\n| 指令 | 等同底層命令 | 說明 |\n|------|------------|------|\n| `make pods` | `kubectl get pods -n erp-token-local` | Pod 列表 |\n| `make logs-api` | `kubectl logs -f deploy/erp-api-token-manager -n erp-token-local` | Tail api-server logs |\n| `make logs-db` | `kubectl logs -f statefulset/postgres -n erp-token-local` | Tail postgres logs |\n| `make shell-api` | `kubectl exec -it deploy/erp-api-token-manager -n erp-token-local -- bash` | Shell 進入 api-server pod |\n| `make shell-db` | `kubectl exec -it -n erp-token-local statefulset/postgres -- psql -U erpapp -d erpapikey` | 進入 psql shell |\n| `make health-check` | 依序執行 §4.7 個別驗證 curl 命令 | 全服務健康檢查 |\n| `make k9s` | `k9s -n erp-token-local`（需已安裝 k9s）| 互動式 K8s UI |\n| `make describe-api` | `kubectl describe deploy/erp-api-token-manager -n erp-token-local` | 描述 deployment（事件 / probe 設定）|\n\n### 測試\n\n| 指令 | 等同底層命令 | 說明 |\n|------|------------|------|\n| `make test-unit` | `dotnet test src/Application.Tests/Application.Tests.csproj --logger \"console;verbosity=normal\"` | 在本機執行 unit test（不需 K8s）|\n| `make test-integration` | `dotnet test src/Infrastructure.IntegrationTests/Infrastructure.IntegrationTests.csproj --logger \"console;verbosity=normal\"` | Testcontainers 起 PostgreSQL container（需 nerdctl 可達）|\n| `make test-e2e` | `dotnet test src/E2ETests/E2ETests.csproj` 並使 base URL 指向 `http://erp-token.local`（環境變數 `E2E_BASE_URL`）| Playwright/Selenium E2E（需 K8s 環境已部署）|\n| `make test-bdd-server` | `dotnet test src/Bdd.Server.Tests/` 對應 `features/*.feature`（SpecFlow / Reqnroll）| Server BDD scenarios |\n| `make test-bdd-client` | `dotnet test src/Bdd.Client.Tests/` 對應 `features/client/*.feature`（Razor + Alpine 互動驗證）| Client BDD scenarios |\n| `make lint` | `dotnet format --verify-no-changes && dotnet build /p:TreatWarningsAsErrors=true` | C# 格式化 + Warning-as-Error |\n| `make secscan` | `dotnet list package --vulnerable --include-transitive`（NuGet CVE）| 漏洞掃描 |\n\n### Database\n\n| 指令 | 等同底層命令 |\n|------|------------|\n| `make db-migrate` | `kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet ef database update --no-build` |\n| `make db-status` | `kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet ef migrations list --no-build` |\n| `make db-rollback` | `kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet ef database update <PreviousMigrationName> --no-build` |\n| `make db-seed` | `kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet run --project src/Infrastructure -- seed --env Local` |\n| `make db-reset` | `make k8s-delete && kubectl delete pvc -l app=postgres -n erp-token-local && make k8s-apply && make db-migrate && make db-seed` |\n| `make db-backup` | `kubectl exec -n erp-token-local statefulset/postgres -- pg_dump -U erpapp erpapikey > backups/local-$(date +%Y%m%d-%H%M%S).sql` |\n\n### Port Forward\n\n| 指令 | 等同底層命令 |\n|------|------------|\n| `make pf-api` | `kubectl port-forward -n erp-token-local deploy/erp-api-token-manager 8080:8080 &` |\n| `make pf-db` | `kubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &` |\n| `make pf-pgadmin` | `kubectl port-forward -n erp-token-local deploy/pgadmin 5050:80 &` |\n| `make pf-jaeger` | `kubectl port-forward -n erp-token-local deploy/jaeger-aio 16686:16686 &` |\n| `make pf-all` | 依序啟動上述全部 port-forward |\n| `make pf-stop` | `pkill -f 'kubectl port-forward.*erp-token-local'` |\n\n---\n\n## 7. Database Operations\n\n### 進入 psql Shell\n\n> **WARNING: 下方連線字串中的密碼 `secret` 僅為本機開發預設值（實際密碼由 `bootstrap-secrets.sh` 隨機生成），禁止複製至 staging / production 環境。** 正式環境請從 GCP Secret Manager 取得憑證。\n\n```bash\n# 方法 1：透過 kubectl exec（不需 port-forward）\nkubectl exec -it -n erp-token-local statefulset/postgres -- \\\n  psql -U erpapp -d erpapikey\n\n# 方法 2：port-forward 後用本地 psql client\nkubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &\nPG_PASSWORD=$(kubectl get secret erp-token-db-secret -n erp-token-local \\\n  -o jsonpath='{.data.POSTGRES_PASSWORD}' | base64 -d)\n# WARNING: 密碼來自隨機生成的本機 Secret；禁止在 staging / production 使用此模式。\nPGPASSWORD=\"${PG_PASSWORD}\" psql \"postgres://erpapp@localhost:5432/erpapikey\"\n\n# Make shortcut\nmake shell-db\n```\n\n### 執行 EF Core Migration\n\n```bash\n# 執行所有 pending migration\nmake db-migrate\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef database update --no-build\n\n# 查看 migration 狀態\nmake db-status\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef migrations list --no-build\n\n# Rollback 到前一個 migration\nmake db-rollback\n# 等同（指定上一個 migration name）：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef database update 20260315_AddTokenExpiresAt --no-build\n```\n\n### 新增一個 Migration（開發者本機）\n\n```bash\n# 在本機（非 Pod 內）執行 dotnet ef migrations add\ndotnet ef migrations add AddTokenSomeColumn \\\n  --project src/Infrastructure \\\n  --startup-project src/ErpApplication\n\n# Review 生成的 Up/Down 後 apply\nmake image-build-all && make k8s-restart-api && make db-migrate\n```\n\n### Seed 測試資料\n\n```bash\n# 載入所有 seed（idempotent，可重複執行）\nmake db-seed\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet run --project src/Infrastructure -- seed --env Local\n```\n\n> Seed 內容對齊 §8 Test Data：3 個角色（ITAdmin / BusinessUser / Auditor 唯讀），各角色 1–2 筆 api_tokens，覆蓋 active / revoked / expired 三種狀態及 token_audit_logs。\n\n### 重置為乾淨狀態\n\n```bash\n# Drop + recreate + migrate + seed（銷毀所有本地資料）\nmake db-reset\n# Warning: PVC 內資料全部清除\n```\n\n### Backup / Restore\n\n> **WARNING: 下方連線字串中的密碼僅為本機開發隨機值，禁止複製至 staging / production 環境。**\n\n```bash\n# Backup（在 pod 內執行；不需 port-forward）\nmake db-backup\n# 等同：\n# kubectl exec -n erp-token-local statefulset/postgres -- \\\n#   pg_dump -U erpapp erpapikey > backups/local-$(date +%Y%m%d-%H%M%S).sql\n\n# Restore（port-forward 後執行）\nkubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &\nPG_PASSWORD=$(kubectl get secret erp-token-db-secret -n erp-token-local \\\n  -o jsonpath='{.data.POSTGRES_PASSWORD}' | base64 -d)\n# WARNING: 密碼僅為本機隨機值，禁止 staging / production 使用此 port-forward 流程。\nPGPASSWORD=\"${PG_PASSWORD}\" psql \"postgres://erpapp@localhost:5432/erpapikey\" \\\n  < backups/local-20260508-093000.sql\n```\n\n---\n\n## 8. Test Data & Fixtures\n\n`make db-seed` 後以下帳號與資料固定可用。角色清單對齊 PRD §3 / RBAC Matrix（EDD §6.10），密碼策略遵守 PRD §6.2。\n\n### Default User Accounts\n\n| Role | Email | Password | 說明 |\n|------|-------|----------|------|\n| **IT Admin**（全權限）| `admin@erp-token.local` | `Password1!` | `erp_role = ITAdmin`；可看全量 Token + 撤銷他人 Token + 看 Audit Log（§7 §11.1）|\n| **Business User**（標準）| `user@erp-token.local` | `Password1!` | `erp_role = BusinessUser`；可建立 / 看自己的 Token；無 Audit Log 權限 |\n| **Auditor**（唯讀）| `auditor@erp-token.local` | `Password1!` | `erp_role = Auditor`；只讀 Audit Log，不可建立 / 撤銷 Token |\n| **N8N Service Account**（自動化）| `n8n-svc@erp-token.local` | `Password1!` | `erp_role = BusinessUser`；用於模擬 N8N 整合測試（US-N8N-005）|\n\n> **密碼複雜度**：`Password1!` 滿足 8+ 字元 + 大寫 + 小寫 + 數字 + 特殊字元（PRD §6.2 強度要求）；僅本機開發用，staging 起需 SSO。\n\n### Seed IT Admin（`is_superuser` 升級流程，PRD §5.9 / OQ-PRD-4 RESOLVED）\n\n> 本系統無獨立 Admin 後台（PRD §4.3 Out of Scope），IT Admin 權限以 `users.is_superuser=true` 旗標升級。\n> MVP 階段升級流程為 **DBA 手動執行 SQL**（OQ-PRD-4 已決議），完整 SOP：\n\n```bash\n# 1. 進入 postgres（kubectl exec 或 pgadmin）\nkubectl exec -it -n erp-token-local deploy/postgres -- \\\n  psql -U erpapp -d erpapikey\n```\n\n```sql\n-- 2. 將指定 ERP 使用者升級為 IT Admin\nUPDATE users\n   SET is_superuser = TRUE,\n       updated_at   = NOW()\n WHERE email = 'admin@erp-token.local'\nRETURNING id, email, is_superuser;\n-- 預期回傳一筆，is_superuser=t\n\n-- 3. 驗證升級結果（在資料庫層）\nSELECT id, email, is_superuser FROM users WHERE is_superuser = TRUE;\n-- MVP 期望結果：恰好 1 列（admin@erp-token.local）\n\n-- 4. 寫入升級事件至 audit log（合規必要，MVP 由 DBA 手動寫入）\nINSERT INTO token_audit_logs (event_id, action, actor_user_id, target_user_id, ip_address, created_at)\nSELECT gen_random_uuid(), 'admin_grant', NULL, u.id, '127.0.0.1', NOW()\n  FROM users u\n WHERE u.email = 'admin@erp-token.local';\n```\n\n`make db-seed` 預設會自動執行上述 SQL（已包含於 `scripts/seed-data.sql` 之 `seed-superuser.sql` 區塊）。\n若需要為其他既存 ERP 使用者升級，請以 SOP 步驟 2 替換 email 後重複執行。\n\n> **Pilot/Prod 階段**：請依 PRD §12 OQ-PRD-4 RESOLVED 註解執行 `kubectl exec ... psql` 並開立變更工單；嚴禁繞過 audit log。\n> **降級流程**：將 `is_superuser` 設回 FALSE 並寫入 `admin_revoke` audit event；操作同樣由 DBA 執行。\n\n### Sample Data（從 SCHEMA.md §3 業務資料表提取）\n\n| Entity | Count | 說明 |\n|--------|-------|------|\n| Users | 4 | 涵蓋四種角色，密碼同上 |\n| api_tokens | 8 | 涵蓋所有狀態：4 active（admin / user / auditor / n8n-svc 各 1）、2 revoked（手動撤銷與自動撤銷）、2 expired（已過期未清理）|\n| token_audit_logs | 16 | 對應 8 個 token 的 create + 部分 revoke / expire 事件，event_type 涵蓋 `create` / `revoke` / `expire` / `last_used` |\n| api_tokens (edge case) | 1 | description 含最大欄位長度（255 字元）+ Unicode CJK + 特殊字元（如 `「」、—、…`），用於 §10 邊界情境驗證 |\n\n### pgadmin 登入\n\n- URL：`http://erp-token.local/pgadmin`（透過 Ingress）或 port-forward `http://localhost:5050`\n- Email：`admin@erp-token.local`\n- Password：`scripts/bootstrap-secrets.sh` 隨機生成；查詢方式：\n  ```bash\n  kubectl get secret erp-token-pgadmin-secret -n erp-token-local \\\n    -o jsonpath='{.data.PGADMIN_DEFAULT_PASSWORD}' | base64 -d; echo\n  ```\n  > **WARNING：本機開發用，禁止用於其他環境**\n- 新增 Server：Host = `postgres`（K8s service 名稱），Port = `5432`，DB = `erpapikey`，User = `erpapp`，Password = 從 `kubectl get secret erp-token-db-secret -n erp-token-local -o jsonpath='{.data.POSTGRES_PASSWORD}' | base64 -d` 取得\n\n### API 測試樣本（curl，依 API.md §5）\n\n```bash\n# 1. 登入取得 Cookie + AntiForgery Token（瀏覽器登入後 export cookie）\ncurl -c /tmp/cookies.txt http://erp-token.local/Account/Login \\\n  -d \"email=admin@erp-token.local&password=Password1!&__RequestVerificationToken=<token>\"\n\n# 2. 列出當前使用者的 Token\ncurl -s -b /tmp/cookies.txt http://erp-token.local/api/tokens | jq .\n# Expected: HTTP 200, {\"success\":true,\"data\":[...]}\n\n# 3. 建立 Token（attach AntiForgery header）\ncurl -s -X POST -b /tmp/cookies.txt \\\n  -H \"X-XSRF-TOKEN: <antiforgery>\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"description\":\"Local dev verification token\"}' \\\n  http://erp-token.local/api/tokens | jq .\n# Expected: HTTP 201, response 含 rawToken（一次性明文）\n\n# 4. 用 Bearer Token 呼叫 POC\ncurl -s -H \"Authorization: Bearer <raw_token>\" \\\n  http://erp-token.local/api/poc/sales | jq .\n# Expected: HTTP 200, simulated sales data\n```\n\n---\n\n## 9. ConfigMap & Secret Reference\n\n### ConfigMap — `erp-token-api-config`\n\n| Key | Default | 說明 |\n|-----|---------|------|\n| `ASPNETCORE_ENVIRONMENT` | `Development` | Runtime 環境（影響 appsettings.{env}.json 讀取）|\n| `ASPNETCORE_URLS` | `http://+:8080` | API 監聽 URL |\n| `Logging__LogLevel__Default` | `Debug` | 日誌等級（debug 顯示 EF Core SQL）|\n| `Logging__LogLevel__Microsoft.EntityFrameworkCore` | `Warning` | 抑制 EF SQL command 噪音 |\n| `FeatureFlags__enable_api_token_management` | `true` | 主功能開關（PRD §5.1 / EDD §16.2.2）|\n| `FeatureFlags__enable_token_audit_log` | `true` | Audit Log 寫入開關 |\n| `FeatureFlags__enable_poc_demo_apis` | `true` | `/api/poc/*` 端點開關（local / dev / staging：true；prod：false，PRD §5.6）|\n| `OpenTelemetry__Endpoint` | `http://jaeger-aio:4317`（選配；若未部署 jaeger 則留空）| OTLP gRPC endpoint |\n| `OpenTelemetry__ServiceName` | `erp-api-token-manager` | Trace service name |\n| `Cors__AllowedOrigins` | `http://erp-token.local` | 允許的 CORS origins |\n| `RateLimiting__TokenCreate__WindowSeconds` | `60` | Token 建立速率限制窗口（CONSTANTS.md §6）|\n| `RateLimiting__TokenCreate__MaxRequests` | `60` | Token 建立速率限制上限 |\n\n> **K8s internal DNS 連線字串：** `ConnectionStrings__ApiTokenDb` 不放在 ConfigMap，改放在 Secret `erp-token-api-secret`。值的格式：`Host=postgres;Port=5432;Database=erpapikey;Username=erpapp;Password=<random>`。**禁止**使用 `localhost` 或 `127.0.0.1`，必須使用 K8s service 名稱 `postgres`，否則 pod 內無法連線。\n\n### ConfigMap — `erp-token-db-config`（postgres init）\n\n| Key | Default | 說明 |\n|-----|---------|------|\n| `POSTGRES_INITDB_ARGS` | `--encoding=UTF8 --locale=C` | initdb 參數，符合 SCHEMA.md §2 預設 |\n| `init.sql` | （見 `docker/postgres/init.sql`）| 建立 `public` schema GRANT 與 `erp_token` schema（per SCHEMA.md §1.1）|\n\n### Secret — `erp-token-api-secret`（從 bootstrap-secrets.sh 建立）\n\n| Key | 說明 | 設定方式 |\n|-----|------|---------|\n| `ConnectionStrings__ApiTokenDb` | PostgreSQL 連線字串（Host=postgres K8s DNS）| `bootstrap-secrets.sh`（隨機 DB password）|\n| `HmacSecret` | v2 HMAC-SHA256 Token Hash 用（MVP 預留，EDD §17.2）| `openssl rand -hex 64` |\n| `DataProtection__Key` | ASP.NET Core DataProtection Key Ring（Cookie / AntiForgery 簽章）| `openssl rand -hex 32`；多副本必須共享 key |\n\n### Secret — `erp-token-db-secret`（postgres bootstrap）\n\n| Key | 說明 |\n|-----|------|\n| `POSTGRES_DB` | DB 名稱（`erpapikey`）|\n| `POSTGRES_USER` | DB user（`erpapp`）|\n| `POSTGRES_PASSWORD` | DB password（隨機）|\n\n### Secret — `erp-token-pgadmin-secret`\n\n| Key | 說明 |\n|-----|------|\n| `PGADMIN_DEFAULT_EMAIL` | pgadmin 登入 email（`admin@erp-token.local`）|\n| `PGADMIN_DEFAULT_PASSWORD` | pgadmin 登入密碼（隨機；本機使用，勿用於其他環境）|\n\n### 查看目前 ConfigMap / Secret\n\n```bash\n# 查看 ConfigMap 內容\nkubectl get configmap erp-token-api-config -n erp-token-local -o yaml\n\n# 查看 Secret key 清單（值會 base64 遮罩）\nkubectl get secret erp-token-api-secret -n erp-token-local -o yaml\n\n# 解碼特定 secret 值\nkubectl get secret erp-token-api-secret -n erp-token-local \\\n  -o jsonpath='{.data.HmacSecret}' | base64 -d; echo\n```\n\n### 更新 ConfigMap / Secret 並套用\n\n```bash\n# 修改 ConfigMap\nkubectl edit configmap erp-token-api-config -n erp-token-local\n# 或直接重新 apply：\nkubectl apply -k k8s/overlays/local/\n\n# 更新後需 rolling restart 才生效\nmake k8s-restart-all\n```\n\n---\n\n## 10. Common Issues & Fixes\n\n| Issue | Symptom | Root Cause | Fix |\n|-------|---------|------------|-----|\n| Pod stuck in `Pending` | `kubectl get pods` 顯示 Pending | 資源不足或 namespace 未建立 | `kubectl describe pod <name> -n erp-token-local` 查看 Events；增加 Rancher Desktop RAM 至 8 GB（Preferences → Virtual Machine）|\n| Pod in `CrashLoopBackOff` | Pod 反覆重啟 | 應用程式啟動失敗（DB 未就緒 / appsettings 錯誤 / migration 未跑）| `kubectl logs <pod> -n erp-token-local --previous` 查看上次崩潰日誌；80% 是 DB 連線失敗，先 `kubectl get pod postgres-0` 確認就緒 |\n| `ImagePullBackOff` | Pod 無法找到 image | image 未 build 或載入錯 namespace | `nerdctl --namespace=k8s.io images \\| grep erp-token`；重新 `make image-build-all`；確認使用 `--namespace=k8s.io` flag |\n| Ingress 無法解析 | `curl: Could not resolve host` | /etc/hosts 未設定 | 確認 `/etc/hosts` 含 `127.0.0.1 erp-token.local`；macOS 執行 `sudo dscacheutil -flushcache` |\n| DB 連線拒絕 | api-server log: `Npgsql.NpgsqlException: ... ECONNREFUSED postgres:5432` | postgres pod 未就緒 | `kubectl wait --for=condition=Ready pod/postgres-0 -n erp-token-local --timeout=60s` |\n| EF Core Migration 失敗 `relation already exists` | `make db-migrate` 報錯 | Migration 被部分執行過 | `make db-reset`（銷毀 PVC 重建）；或進 psql 手動執行 `DROP TABLE __EFMigrationsHistory CASCADE;` 後重跑 |\n| EF Core Migration 失敗 `permission denied for schema public` | 部署後第一次 migration 失敗 | `erpapp` user 無 `CREATE` privilege | `kubectl exec -n erp-token-local statefulset/postgres -- psql -U postgres -d erpapikey -c \"GRANT ALL ON SCHEMA public TO erpapp;\"` |\n| Secret 找不到 | Pod 事件：`secret \"erp-token-api-secret\" not found` | `make k8s-init` 未執行 | 重新執行 `make k8s-init`；確認 `kubectl get secret -n erp-token-local` 列出 3 個 secret |\n| `Anti-forgery token validation failed`（HTTP 400 on form submit） | UI 操作報錯 | DataProtection Key 在多副本間不一致；Cookie 過期 | 確認 `DataProtection__Key` 在 `erp-token-api-secret` 一致；`make k8s-restart-api`；瀏覽器 logout 重新 login |\n| port-forward 斷線 | `port-forward` 背景 process 結束 | 網路超時或 Pod 重啟 | `make pf-api`（Make 重新啟動）；或在另一個 terminal 重新執行 |\n| Rancher Desktop K8s 未啟動 | `kubectl: connection refused` | k3s 服務未運行 | 開啟 Rancher Desktop GUI，進入 **Kubernetes** 頁面確認已啟用並等待 Ready |\n| nerdctl build 失敗（network） | Build 時 NuGet restore 連線失敗 | containerd 沙箱 DNS 問題 | 嘗試 `nerdctl build --network=host ...`；或重啟 Rancher Desktop |\n| 舊 ConfigMap 未更新 | 修改 configmap 後行為未變 | Pod 未重啟，仍用舊版設定 | `make k8s-restart-all` |\n| `/api/poc/*` 回 404 | POC API 全部 404 | `FeatureFlags__enable_poc_demo_apis=false` 或 ConfigMap 未生效 | 確認 ConfigMap key；`make k8s-restart-api` 重啟；確認 POC controller 已註冊 |\n| `/ApiTokens/AuditLog` 回 403 | UI 點 Audit Log 報錯 | 登入帳號 `erp_role != ITAdmin` | 用 `admin@erp-token.local` 登入（§8）|\n| Tailwind CSS 樣式遺失（裸 HTML 顯示） | 頁面看起來像 1996 風格 | CDN 載入失敗或 production CSS bundle 未掛入 | 確認瀏覽器 DevTools Console 無 CDN 錯誤；若使用 CLI mode，確認 Dockerfile stage 2 已 build `wwwroot/css/site.css` |\n\n---\n\n## 11. Logs & Debugging\n\n### 查看 Pod 日誌\n\n```bash\n# 即時 tail 指定服務\nkubectl logs -f deploy/erp-api-token-manager -n erp-token-local\nkubectl logs -f statefulset/postgres -n erp-token-local\n\n# Make shortcuts\nmake logs-api\nmake logs-db\n\n# 查看上次 crash 日誌\nkubectl logs deploy/erp-api-token-manager -n erp-token-local --previous\n\n# 多副本同時 tail（兩個 pod logs 合併）\nkubectl logs -f -l app=erp-api-token-manager -n erp-token-local --max-log-requests=2\n```\n\n> **本專案無獨立 worker / web-app Deployment**（無 `make logs-worker` / `make logs-web`），因 EDD §3.3 MQ=None 且 FRONTEND.md §2 為 Razor Pages 嵌入式（與 api-server 同 process）。\n\n### 進入 Pod Shell\n\n```bash\n# api-server shell（dotnet 8 image，bash 可用）\nkubectl exec -it deploy/erp-api-token-manager -n erp-token-local -- bash\n# Make shortcut：\nmake shell-api\n\n# 查看環境變數（含 ConfigMap + Secret 注入的值）\nkubectl exec deploy/erp-api-token-manager -n erp-token-local -- env | sort\n\n# 在 pod 內執行一次性指令（Runtime 工具：dotnet 8）\nkubectl exec deploy/erp-api-token-manager -n erp-token-local -- dotnet --version\n# Expected: 8.0.x\n```\n\n### 查看 Pod 事件（啟動失敗時最有用）\n\n```bash\nkubectl describe pod <pod-name> -n erp-token-local\n# 重點看 Events 區塊的 Warning 訊息\n```\n\n### 常見 Log 模式\n\n| Pattern | 含義 | 處理 |\n|---------|------|------|\n| `Npgsql.NpgsqlException: ... database connection failed` | postgres pod 未就緒或 DNS 解析失敗 | 確認 `kubectl get pod postgres-0 -n erp-token-local` Running；確認 service 名稱為 `postgres` |\n| `[INFO] Now listening on: http://[::]:8080` | API 啟動成功 | 正常，無需處理 |\n| `[ERROR] Migration 20260315_AddTokenExpiresAt failed` | EF Core Migration SQL 異常 | 查看完整錯誤；`make db-rollback` 或進 psql 手動修正 `__EFMigrationsHistory` |\n| `[WARN] Anti-forgery token validation failed` | Form 提交 / API 缺 X-XSRF-TOKEN | 確認 Razor `_Layout.cshtml` 注入 token；多副本確認 `DataProtection__Key` 共享 |\n| `[ERROR] Bearer Token validation failed: token revoked` | API_Auth middleware 拒絕已撤銷 Token | 預期行為（PRD §5.4 AC-3）|\n| `[INFO] HTTP GET /health/ready responded 200 in 5ms` | Readiness probe 正常 | 正常 |\n| `[ERROR] Background hosted service ... failed after 3 retries` | （MVP 無 Worker；若未來引入 IHostedService 才會出現）| 查看 hosted service 設定；確認外部依賴 |\n\n### 使用 k9s（推薦）\n\n```bash\n# 開啟 k9s，自動切換至 local namespace\nk9s -n erp-token-local\n# 在 k9s 內：\n#   :pod   — 查看所有 pod\n#   :log   — 查看 log\n#   s      — 進入 pod shell\n#   d      — describe resource\n#   Ctrl+K — 刪除 resource\n```\n\n### 分散式追蹤（選配 Jaeger）\n\n```bash\n# 啟用 Jaeger（k8s/overlays/local/jaeger.yaml 預設停用，取消註解 kustomization patch 啟用）\nmake k8s-apply\n\n# Port forward Jaeger UI\nmake pf-jaeger\nopen http://localhost:16686\n\n# api-server ConfigMap 已配置 OTLP endpoint：\n# OpenTelemetry__Endpoint = http://jaeger-aio:4317\n# 重新 deploy 即會把 trace 送至 jaeger\n```\n\n---\n\n## 12. Port Reference\n\n### Ingress（需 /etc/hosts 設定 `127.0.0.1 erp-token.local`）\n\n| Path | 服務 | 說明 |\n|------|------|------|\n| `http://erp-token.local/` | api-server (Razor Pages) | 主 UI（首頁 / Login / `/ApiTokens` 列表）|\n| `http://erp-token.local/Account/*` | api-server (Razor Pages) | 登入 / 登出 |\n| `http://erp-token.local/ApiTokens/*` | api-server (Razor Pages) | Token 管理頁面 |\n| `http://erp-token.local/api/tokens` | api-server (REST) | Token CRUD REST API |\n| `http://erp-token.local/api/poc/{sales,inventory,orders/{id}/status,health}` | api-server (REST) | POC demo endpoints |\n| `http://erp-token.local/health/live` | api-server (Health) | Liveness probe |\n| `http://erp-token.local/health/ready` | api-server (Health) | Readiness probe（含 DB 連線檢查）|\n| `http://erp-token.local/pgadmin` | pgadmin | DB 瀏覽器（subpath；Ingress rewrite）|\n| `http://erp-token.local/jaeger`（選配）| jaeger-aio UI | 分散式追蹤 |\n\n> 本專案無獨立前端 SPA，故無 `web-app` 行；Razor Pages 與 REST API 共用同一 Pod 與 port 8080。Admin SPA：has_admin_backend=false，無 `/admin` 前綴路由。\n\n### port-forward（直連 ClusterIP Service）\n\n| 服務 | 指令 | 本地 Port |\n|------|------|---------|\n| api-server | `make pf-api` 等同 `kubectl port-forward -n erp-token-local deploy/erp-api-token-manager 8080:8080` | `8080` |\n| postgres | `make pf-db` 等同 `kubectl port-forward -n erp-token-local statefulset/postgres 5432:5432` | `5432` |\n| pgadmin | `make pf-pgadmin` 等同 `kubectl port-forward -n erp-token-local deploy/pgadmin 5050:80` | `5050` |\n| jaeger UI（選配）| `make pf-jaeger` 等同 `kubectl port-forward -n erp-token-local deploy/jaeger-aio 16686:16686` | `16686` |\n| jaeger OTLP（選配）| `kubectl port-forward -n erp-token-local deploy/jaeger-aio 4317:4317` | `4317` |\n\n> 全部 port-forward：`make pf-all`。停止：`make pf-stop`。\n>\n> **Port 一致性驗證**：上表 `8080` / `5432` 與 §2 Architecture Mermaid 圖節點標注、RUNBOOK §2.3 Network and Port Reference、EDD §16.1 Deployment containerPort 完全一致（皆 8080 / 5432）。\n\n---\n\n## 13. Local HTTPS 設定\n\n部分功能需要 HTTPS（OAuth 2.0 回調、`SameSite=Secure` Cookie、Service Worker）。本專案 MVP 階段預設使用 HTTP；若需驗證 SAML SSO 或 OAuth callback，啟用以下流程。\n\n> 若無 OAuth 回調 / SameSite Secure Cookie 需求，可跳過此節（POC MVP 階段以 HTTP 為主）。\n\n```bash\n# 1. 建立本地 CA（只需執行一次）\nmkcert -install\n\n# 2. 為本地 domain 生成憑證\nmkcert \"erp-token.local\" \"*.erp-token.local\"\n# 生成：erp-token.local+1.pem  和  erp-token.local+1-key.pem\n\n# 3. 建立 K8s TLS Secret\nkubectl create secret tls erp-token-local-tls \\\n  --cert=erp-token.local+1.pem \\\n  --key=erp-token.local+1-key.pem \\\n  -n erp-token-local \\\n  --dry-run=client -o yaml | kubectl apply -f -\n\n# 4. 啟用 HTTPS Ingress（已在 k8s/overlays/local/ 提供，預設 disabled）\n# 編輯 k8s/overlays/local/kustomization.yaml，啟用 tls-ingress patch：\n# patches:\n#   - path: patches/ingress-tls.yaml   # 取消此行註解\n\nkubectl apply -k k8s/overlays/local/\n```\n\nOAuth 本地回調設定（若整合 ERP SSO）：\n```\nhttps://erp-token.local/Account/SsoCallback\n```\n\n`.gitignore` 必須加入憑證私鑰（避免誤提交）：\n```gitignore\nerp-token.local-key.pem\nerp-token.local+*-key.pem\n```\n\n---\n\n## 14. Mock Services & External Integration Stubs\n\n依 ARCH §3.1 系統元件圖與 EDD §1.5 Scope，本模組 MVP 不依賴任何外部第三方服務（無 email、storage、payment、SMS、OAuth provider）。本地僅需以下 mock：\n\n| 服務 | 本地替代方案 | K8s Service DNS / 端點 | 說明 |\n|------|-----------|----------------------|------|\n| ERP Identity（外部主應用 `/Account/*`）| 內建 Razor Pages `/Account/Login`（種 4 個 seed user）| `http://erp-token.local/Account/Login` | 模擬 ERP 主應用登入；正式環境此路由由 ERP Core Team 處理 |\n| N8N Workflow（外部呼叫者）| `curl -H \"Authorization: Bearer ...\"` 直接呼叫 `/api/poc/*` | — | 不需起 N8N 容器；US-N8N-005 整合測試以 curl 模擬 |\n| OpenTelemetry Collector → Jaeger（觀測）| jaeger-aio Deployment（all-in-one image）| `jaeger-aio:4317` (OTLP) / `jaeger-aio:16686` (UI) | 本地選配；prod 走 OTEL collector + 集中 Jaeger |\n| Email / SMS / Payment | — | — | **N/A**：本模組 MVP 不發送 email、不送 SMS、不接金流（EDD §1.5 / BRD scope）|\n| Redis / Cache | — | — | **N/A**：EDD §3.3 Cache=None（即時撤銷設計，不做 Token 狀態快取）|\n| Object Storage（MinIO / S3）| — | — | **N/A**：本模組無檔案上傳 / 物件儲存需求 |\n| Mailpit（SMTP trap）| — | — | **N/A**：本模組不發送 email |\n\n> 因 EDD §2.1 系統上下文圖中外部依賴僅有 `ERP Identity` 與 `N8N`（皆可由內部模擬），本地無需起額外 mock service。BC 隔離測試以 ArchUnit / Test 完成（test-plan.md §3），無 WireMock 需求。\n\n---\n\n## 15. Inner Loop — 快速迭代開發\n\n每次修改程式碼後不需要重建整個環境。對 ASP.NET Core 8 Razor Pages + Alpine.js + Tailwind 的迭代策略：\n\n### 後端（C# / ASP.NET Core / Razor Pages）\n\n```bash\n# 方法 1：重 build + rolling restart（約 30-60 秒）\nmake image-build-api && make k8s-restart-api\n\n# 方法 2：使用 skaffold（自動偵測檔案變更，約 10-20 秒）\nskaffold dev --profile local\n# skaffold 會 watch src/，變更後自動 build + load + restart pod\n```\n\n### 前端（Web — Razor Pages + Alpine.js + Tailwind）\n\n依 FRONTEND.md §2.3，前端為 Razor Pages server-rendered + CDN Alpine + CDN Tailwind（dev mode），無獨立 SPA，**不適用 Vite/webpack HMR**。迭代策略：\n\n```bash\n# 方法 1：修改 .cshtml 後重 build image + rollout\n# Razor 視圖 .cshtml 為 build-time 編譯，需重 build image\nmake image-build-api && make k8s-restart-api\n\n# 方法 2：dotnet watch（適合純後端 + Razor 改動）\n# 在本機（非 K8s）跑 dotnet watch，連線 port-forward 的 postgres：\nmake pf-db\ncd src/ErpApplication\nASPNETCORE_ENVIRONMENT=Development \\\n  ConnectionStrings__ApiTokenDb=\"Host=localhost;Port=5432;Database=erpapikey;Username=erpapp;Password=$(kubectl get secret erp-token-db-secret -n erp-token-local -o jsonpath='{.data.POSTGRES_PASSWORD}' | base64 -d)\" \\\n  dotnet watch run\n# .cshtml / .cs 修改後自動 rebuild + reload，瀏覽器自動刷新\n# WARNING: 此密碼來自本機隨機 Secret；切勿用於 staging / production。\n\n# 方法 3：Tailwind CLI watch（若使用 production CSS bundle 模式）\ncd src/ErpApplication\nnpx tailwindcss -i ./wwwroot/css/input.css -o ./wwwroot/css/site.css --watch\n```\n\n> **不適用本專案的引擎特定段落（依 LOCAL_DEPLOY 模板）：**\n> - **Cocos Creator inner loop** — N/A（FRONTEND.md §2.3.1 標記 Cocos N/A，本產品非遊戲）\n> - **Unity WebGL inner loop** — N/A（同上，無 Unity Editor build）\n> - **Phaser inner loop** — N/A（同上）\n\n### 使用 skaffold（完整 inner loop）\n\n`skaffold.yaml` 已設定在 repo root，`local` profile 對應 `k8s/overlays/local/`：\n\n```bash\n# 啟動 skaffold watch 模式（Ctrl+C 停止）\nskaffold dev --profile local\n\n# 一次性 build + deploy（不 watch）\nskaffold run --profile local\n\n# 清除 skaffold 部署的資源\nskaffold delete --profile local\n```\n\n---\n\n## 16. Cleanup\n\n### 停止 port-forward\n\n```bash\nmake pf-stop\n# 等同：\n# pkill -f 'kubectl port-forward.*erp-token-local'\n```\n\n### 刪除所有 K8s 資源（保留 PVC 資料）\n\n```bash\nmake k8s-delete\n# 等同：\nkubectl delete -k k8s/overlays/local/\n```\n\n### 完全重置（刪除所有資源 + 資料）\n\n```bash\nmake k8s-clean\n# 等同：\nkubectl delete namespace erp-token-local\n# Warning: 所有 PVC（postgres、pgadmin 資料）永久刪除\n```\n\n### 重建本地環境（一條龍）\n\n```bash\nmake k8s-clean && make k8s-init && make image-build-all && make k8s-apply && \\\n  kubectl wait --for=condition=Ready pods --all -n erp-token-local --timeout=180s && \\\n  make db-migrate && make db-seed && make health-check\n```\n\n### 確認清除乾淨\n\n```bash\nkubectl get namespace erp-token-local\n# Expected: Error from server (NotFound): namespaces \"erp-token-local\" not found\n\nnerdctl --namespace=k8s.io images | grep erp-token\n# 若需清除 local image：\nnerdctl --namespace=k8s.io rmi erp-token/api:local\n```\n\n---\n\n## 17. Tunnel Access（單一對外 Port，AI 可測試模式）\n\n所有服務均透過同一個 Ingress（port 80）路由，tunnel 只需暴露一個 port。適合 AI agent 自動化測試、QA 驗收、跨團隊協作。\n\n### ngrok（推薦）\n\n```bash\n# 安裝（一次性）\nbrew install ngrok\n\n# 啟動 tunnel → Ingress port 80\nngrok http 80 --host-header=erp-token.local\n\n# 輸出範例：\n# Forwarding  https://a1b2c3d4.ngrok-free.app -> http://localhost:80\n# 記錄此 URL，後續測試步驟使用 _TUNNEL_URL\n_TUNNEL_URL=\"https://a1b2c3d4.ngrok-free.app\"\n```\n\n### Cloudflare Tunnel（無公開 URL 限制）\n\n```bash\n# 安裝 cloudflared（一次性）\nbrew install cloudflared\n\n# 快速 tunnel（dev mode，無需帳號）\ncloudflared tunnel --url http://localhost:80\n\n# 輸出範例：\n# https://random-name.trycloudflare.com\n_TUNNEL_URL=\"https://random-name.trycloudflare.com\"\n```\n\n### 設定 Ingress Host header（ngrok / tunnel 需）\n\nIngress 預設只接受 `erp-token.local`，tunnel 需額外設定：\n\n```bash\n# 方法 1：啟動 ngrok 時加 --host-header（推薦）\nngrok http 80 --host-header=erp-token.local\n\n# 方法 2：Ingress 加 wildcard host（更彈性）\n# 編輯 k8s/overlays/local/patches/ingress-tunnel.yaml：\n# spec.rules[0].host: \"\"  # 空字串 = 接受所有 host\nkubectl apply -k k8s/overlays/local/\n```\n\n### 驗證 Tunnel 可存取\n\n```bash\n# Razor Pages UI 可存取（HTTP 200，登入頁）\ncurl -s -o /dev/null -w \"%{http_code}\" \"${_TUNNEL_URL}/\"\n# Expected: 200\n\n# Health Ready 可存取\ncurl -s \"${_TUNNEL_URL}/health/ready\"\n# Expected: Healthy\n\n# POC API health（不需 Token）\ncurl -s \"${_TUNNEL_URL}/api/poc/health\" | jq .\n# Expected: {\"status\":\"ok\",\"version\":\"1.0.0\",...}\n\n# 開啟瀏覽器測試前端\nopen \"${_TUNNEL_URL}\"\n```\n\n---\n\n## 18. AI Agent Quick Start\n\nAI 代理可執行以下完整腳本，從零啟動完整本地環境並驗證前端可存取，**無需任何人工介入**。\n\n```bash\n#!/usr/bin/env bash\n# ai-quickstart.sh — AI 一鍵啟動 ERP API Token Manager 本地 K8s 環境\nset -e\n\necho \"=== [1/6] 確認 kubectl context ===\"\nkubectl config use-context rancher-desktop\nkubectl cluster-info\n\necho \"=== [2/6] 設定 /etc/hosts（AI 環境若已預設則跳過）===\"\ngrep -q \"erp-token.local\" /etc/hosts || \\\n  echo \"127.0.0.1   erp-token.local api.erp-token.local\" | sudo tee -a /etc/hosts\n\necho \"=== [3/6] 初始化 namespace + Secret（隨機生成所有密碼）===\"\nmake k8s-init   # 等同 bash scripts/bootstrap-secrets.sh\n\necho \"=== [4/6] 建置 Image + 部署 K8s 資源 ===\"\nmake image-build-all\nmake k8s-apply\nkubectl wait --for=condition=Ready pods --all \\\n  -n erp-token-local --timeout=300s\n\necho \"=== [5/6] 初始化資料庫（EF Core migration + seed）===\"\nmake db-migrate\nmake db-seed\n\necho \"=== [6/6] 驗證環境健康 ===\"\nmake health-check\n\necho \"\"\necho \"OK: ERP API Token Manager 本地環境已就緒\"\necho \"   主 UI         → http://erp-token.local/\"\necho \"   API health    → http://erp-token.local/health/ready\"\necho \"   POC API       → http://erp-token.local/api/poc/health\"\necho \"   Test login    → admin@erp-token.local / Password1!\"\necho \"\"\necho \"如需 Tunnel 存取（AI 測試 / ngrok）：\"\necho \"  ngrok http 80 --host-header=erp-token.local\"\n```\n\n> **AI 測試前端的完整流程**：執行 `ai-quickstart.sh` → 啟動 ngrok tunnel → 使用 Playwright 開啟 `_TUNNEL_URL` 驗證前端頁面可見、主要功能可操作（見 `features/client/*.feature`）。\n>\n> **質量要求**：此腳本可被 AI 直接 copy-paste 執行，不含任何需要人工決策的步驟。Local 環境密碼全為隨機生成，不需從 1Password / Vault 取得；`.gitignore` 已涵蓋 `*.env`、`secrets.env`、`.env.*`，無 secret 洩漏風險。\n\n---\n\n## 19. Docker Compose（輔助方案）\n\n> **定位**：Docker Compose 是 K8s 的**輔助工具**，非主要部署方式。本專案 repo 已提供 `docker-compose.yml` + `docker-compose.dev.yml`（gendoc-gen-contracts 生成），用於以下場景：\n> - 不熟悉 K8s 的快速驗證（如單純 API smoke test）\n> - CI 環境中的輕量 integration test（GitHub Actions runner）\n> - VS Code Dev Containers 入門\n>\n> **所有功能與行為必須與 K8s 環境一致**；如有差異，以 K8s 為準。\n\n### 服務對照\n\n| K8s Deployment / StatefulSet | Docker Compose Service | Image |\n|------------------------------|----------------------|-------|\n| erp-api-token-manager | `erp-app` | `erp-api-token-manager:latest`（local build）|\n| postgres | `postgres` | `postgres:16-alpine` |\n| pgadmin（local-only） | `pgadmin` | `dpage/pgadmin4:8.x` |\n| jaeger-aio（dev override）| `jaeger` | `jaegertracing/all-in-one:1.57` |\n\n> **Iron Constraint — Replicas ≥ 2（Docker Compose）**：當以 docker-compose 模式驗證 HA 邏輯時，`services.erp-app.deploy.replicas` 必須 ≥ 2。docker-compose 預設不支援負載分流，需配合 §19.1 nginx proxy 才能正確路由至兩個副本。\n\n合規 docker-compose 範例：\n\n```yaml\nservices:\n  erp-app:\n    image: erp-api-token-manager:latest\n    deploy:\n      replicas: 2  # 本地 HA 最小副本數，禁止填 1\n    expose:\n      - \"8080\"\n    environment:\n      - ASPNETCORE_ENVIRONMENT=Development\n      - ConnectionStrings__ApiTokenDb=Host=postgres;Port=5432;Database=erpapikey;Username=${POSTGRES_USER:-erpapp};Password=${POSTGRES_PASSWORD}\n```\n\n副本數驗證：\n```bash\ndocker compose ps\n# Expected: erp-app 服務顯示 2 個 Running 實例（含 _1 / _2 後綴）\n\ndocker compose ps --filter \"status=running\" --services | xargs -I{} sh -c \\\n  'echo \"{}: $(docker compose ps -q {} | wc -l)\"'\n# Expected: erp-app: 2\n```\n\n### 啟動步驟\n\n```bash\n# 1. 確認 Docker / containerd 執行中（Rancher Desktop container engine 即可）\ndocker info\n\n# 2. 設定 .env（從範例複製，本機假值）\n[[ ! -f .env ]] && cp .env.example .env\n\n# 3. 建置所有 image\ndocker compose build\n\n# 4. 啟動所有服務（背景執行）\ndocker compose --profile dev up -d\n# 含 dev override：jaeger + dotnet watch + pgadmin\n\n# 5. 確認所有容器啟動\ndocker compose ps\n# Expected: 所有服務 Status = \"Up\" / \"running\"\n\n# 6. 初始化資料庫\ndocker compose exec erp-app dotnet ef database update --no-build\ndocker compose exec erp-app dotnet run --project src/Infrastructure -- seed --env Local\n\n# 7. 驗證 API 健康\ncurl -s http://localhost:8080/health/ready\n# Expected: Healthy (HTTP 200)\n\n# 8. 驗證 POC（無需 Token）\ncurl -s http://localhost:8080/api/poc/health | jq .\n# Expected: {\"status\":\"ok\",\"version\":\"1.0.0\",...}\n```\n\n### 對外 Port（docker-compose 模式）\n\n| 服務 | 本地 Port | 說明 |\n|------|---------|------|\n| erp-app（api-server）| `8080` | REST API + Razor Pages（單一 port，無分離前端）|\n| postgres | `5432` | DB（直連）|\n| pgadmin | `5050` | DB 瀏覽器（dev override）|\n| jaeger UI | `16686` | 分散式追蹤 UI（dev override）|\n| jaeger OTLP gRPC | `4317` | OTLP collector（dev override）|\n\n> **注意**：docker-compose 模式預設各服務各自對外暴露 port，與 K8s 單一 port 80 模式不同。如需與 K8s 一致的單一 port 80 模式，需加入 §19.1 nginx proxy。E2E 測試 `--base-url` 也需對應調整：K8s 模式 `http://erp-token.local`，docker-compose 模式 `http://localhost:8080`。\n\n### 19.1 Docker Compose 單一 Port Nginx Proxy（與 K8s Ingress 行為一致）\n\n> 本專案 has_admin_backend=false，無 Admin Portal SPA；單一 port 模式僅需路由 `/api/*` 與 `/`（Razor Pages），不需處理 admin 路徑。若未來引入 Admin SPA，需依 LOCAL_DEPLOY 模板 §19.1 補充 admin upstream + nginx SPA fallback。\n\n**`docker/nginx-proxy/nginx.conf`：**\n\n```nginx\nevents { worker_connections 1024; }\n\nhttp {\n    upstream api_backend {\n        server erp-app:8080;\n    }\n\n    server {\n        listen 80;\n        server_name localhost erp-token.local;\n\n        # Razor Pages + REST API + health 全部走同一 upstream\n        location / {\n            proxy_pass http://api_backend;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n        }\n    }\n}\n```\n\n**`docker-compose.yml`（新增 nginx proxy service）：**\n\n```yaml\nservices:\n  # ... 現有 services（erp-app, postgres）...\n\n  proxy:\n    image: nginx:alpine\n    ports:\n      - \"80:80\"        # 對外只暴露 port 80（與 K8s Ingress 一致）\n    volumes:\n      - ./docker/nginx-proxy/nginx.conf:/etc/nginx/nginx.conf:ro\n    depends_on:\n      - erp-app\n    healthcheck:\n      test: [\"CMD\", \"wget\", \"-q\", \"--spider\", \"http://localhost/health/live\"]\n      interval: 10s\n      timeout: 5s\n      retries: 3\n```\n\n啟動並驗證單一 port 模式：\n\n```bash\ndocker compose up -d\n\n# 驗證單一 port 路由\ncurl -s -o /dev/null -w \"%{http_code}\" http://localhost/\n# Expected: 200（Razor Pages Login 頁）\n\ncurl -s -o /dev/null -w \"%{http_code}\" http://localhost/health/ready\n# Expected: 200\n\ncurl -s http://localhost/api/poc/health | jq .\n# Expected: {\"status\":\"ok\",...}\n```\n\n### 測試（docker-compose 模式）\n\n```bash\n# Unit test（不需 Docker，直接在本機執行）\ndotnet test src/Application.Tests/Application.Tests.csproj --logger \"console;verbosity=normal\"\n\n# Integration test（在 erp-app container 內執行；Testcontainers 會自啟 postgres，但這裡直接用 compose 的 postgres）\ndocker compose exec erp-app dotnet test src/Infrastructure.IntegrationTests/\n\n# E2E test（Playwright，base URL 對應 docker-compose port）\nE2E_BASE_URL=http://localhost:8080 \\\n  dotnet test src/E2ETests/\n# 或單一 port 模式：\nE2E_BASE_URL=http://localhost \\\n  dotnet test src/E2ETests/\n```\n\n### 停止與清除\n\n```bash\n# 停止（保留資料）\ndocker compose stop\n\n# 停止並移除 container（保留 volume 資料）\ndocker compose down\n\n# 完全重置（移除 container + volume）\ndocker compose down -v\n# Warning: 所有 DB 資料永久刪除\n```\n\n---\n\n## 20. k9s Quick Reference（互動式 K8s 操作）\n\n> **定位**：k9s 是 kubectl 的互動式補充層，適合 on-call 快速診斷與資源瀏覽。所有操作等同 kubectl 指令，但以鍵盤驅動，不適合腳本化。`kubectl` 仍是 canonical 工具（腳本、CI、Makefile 均用 kubectl）；k9s 是 power user 效率層。\n\n### 啟動\n\n```bash\n# 指定 namespace 啟動（推薦）\nk9s -n erp-token-local\n\n# 全 cluster 啟動\nk9s --all-namespaces\n```\n\n### 資源切換（命令模式，按 `:` 進入後輸入）\n\n| 命令 | 說明 |\n|------|------|\n| `:pod` | 查看所有 Pod |\n| `:deploy` | 查看 Deployment |\n| `:sts` | 查看 StatefulSet（postgres）|\n| `:svc` | 查看 Service |\n| `:cm` | 查看 ConfigMap |\n| `:secret` | 查看 Secret |\n| `:hpa` | 查看 HorizontalPodAutoscaler |\n| `:job` | 查看 Job（一次性 migration job 適用）|\n| `:cronjob` | 查看 CronJob（本專案無，預留）|\n| `:node` | 查看 Node |\n| `:ns` | 切換 Namespace |\n| `:pvc` | 查看 PersistentVolumeClaim（postgres / pgadmin）|\n| `:event` | 查看 Events（等同 `kubectl get events`） |\n| `:ing` | 查看 Ingress |\n\n### 常用按鍵（Resource 選中後）\n\n| 按鍵 | 說明 | kubectl 等效 |\n|------|------|------------|\n| `l` | 查看 logs（即時串流）| `kubectl logs -f` |\n| `p` | 查看前次容器 logs（CrashLoopBackOff 用）| `kubectl logs --previous` |\n| `s` | exec 進入 container shell | `kubectl exec -it -- bash` |\n| `d` | describe | `kubectl describe` |\n| `e` | edit（直接編輯 YAML）| `kubectl edit` |\n| `ctrl-d` | delete resource（會要求確認）| `kubectl delete` |\n| `shift-f` | port-forward | `kubectl port-forward` |\n| `f` | fullscreen logs 切換 | — |\n| `/` | 文字篩選（過濾清單，支援正則）| `grep` |\n| `Esc` | 返回上層 / 清除篩選 | — |\n| `?` | 顯示所有按鍵說明 | — |\n| `q` | 退出 k9s | — |\n\n### 篩選技巧\n\n```bash\n# 在 :pod 清單中篩選 api server\n/erp-api-token-manager\n\n# 在 :event 中篩選 Warning 事件\n/Warning\n\n# 在 :pod 中篩選崩潰中的 Pod\n/CrashLoop\n\n# 在 :secret 中篩選 erp-token 相關\n/erp-token\n```\n\n### Scale Deployment（互動式）\n\n```\n1. 進入 k9s -n erp-token-local\n2. 輸入 :deploy\n3. 選中 erp-api-token-manager\n4. 按 s → 輸入新 replica 數（≥ 2，Iron Constraint）→ 確認\n```\n\n### 皮膚與設定\n\n```bash\n# k9s 設定目錄\nls ~/.config/k9s/\n\n# 設定 default namespace（config.yml 範例）\n# currentContext: rancher-desktop\n# namespace: erp-token-local\n\n# 推薦 skin（dracula 或 deep-sea）\n# 下載：https://github.com/derailed/k9s/tree/master/skins\n```\n\n### 與 Makefile 整合\n\n```bash\n# §6 Development Commands 已提供\nmake k9s   # 等同 k9s -n erp-token-local\n```\n\n---\n\n## 21. CI/CD 本地模擬（GitHub Actions act runner）\n\n> 本節讓開發者在本機完整重現 CI/CD Pipeline，確保 `.github/workflows/*.yml` 和 `make ci-*` targets 在 commit 前可驗證，避免 PR 被 pipeline 失敗 reject。\n>\n> **CI 工具選擇**：依 RUNBOOK §1.1 / EDD §3.4，本專案使用 **GitHub Actions**（CI）+ **ArgoCD**（CD）。LOCAL_DEPLOY 模板 §21 預設 Jenkins，本節改用 `act` runner 模擬 GitHub Actions。\n\n### 21.0 Local Developer Platform 整體架構\n\n本節說明本地 CI/CD 平台的整體架構。所有元件均在 Rancher Desktop k3s cluster 內執行（除 `act` 為本機進程），開發者 push 到本地 Gitea 即可觸發完整 CI/CD 流程，不依賴任何外部服務。\n\n```mermaid\ngraph LR\n    subgraph DevMachine[\"開發者本機\"]\n        subgraph Local[\"本機進程\"]\n            Act[\"act runner<br/>(本機進程，模擬 GH Actions)\"]\n        end\n        subgraph DevTools[\"dev-tools namespace\"]\n            Gitea[\"Gitea<br/>(ClusterIP:3000)<br/>Local Git Server\"]\n        end\n        subgraph ArgoNS[\"argocd namespace\"]\n            Argo[\"ArgoCD<br/>(ClusterIP:443)<br/>CD GitOps\"]\n        end\n        subgraph App[\"erp-token-local namespace\"]\n            AppPods[\"erp-api-token-manager<br/>(Ingress port 80)<br/>postgres / pgadmin\"]\n        end\n    end\n    Dev[\"Developer<br/>git push\"] --> Gitea\n    Dev -->|\"act push\"| Act\n    Act -->|\"build image\\nrun tests\\nupdate manifest\"| Gitea\n    Argo -->|\"watch repo\"| Gitea\n    Argo -->|\"sync\"| AppPods\n```\n\n**元件說明**：\n\n| 元件 | Namespace | ClusterIP Port | 本地存取（port-forward）|\n|------|-----------|---------------|----------------------|\n| Gitea（Local Git）| `dev-tools` | 3000 | `make dev-tools-forward` → http://localhost:3000 |\n| act runner（CI 本機模擬）| 本機進程（無 namespace）| — | `act` CLI（Docker socket）|\n| ArgoCD（CD）| `argocd` | 443 | `make dev-tools-forward` → https://localhost:8443 |\n| App Ingress | `erp-token-local` | 80 | http://erp-token.local |\n\n> **Port 分離原則**：App domain（port 80 via Ingress）與 dev-tools domain（3000 / 8443 via port-forward）完全隔離，不互相干擾。Gitea 安裝與 GitHub Actions Webhook 設定見 CICD.md §8；Makefile dev-tools targets 見 CICD.md §9。\n\n### 21.1 act runner 安裝（GitHub Actions 本地模擬）\n\n**前提**：Rancher Desktop 已執行（見 §1），Docker / containerd socket 可達。\n\n```bash\n# 安裝 act（Homebrew）\nbrew install act\n\n# 驗證\nact --version\n# Expected: act version 0.2.x\n\n# 列出 .github/workflows/ 中的所有 jobs\nact -l\n```\n\n> 本專案無需 Jenkins helm install；改採 act 在開發者本機跑 GitHub Actions YAML。\n\n### 21.2 Pipeline 設定（`.github/workflows/ci.yml`）\n\n```bash\n# act 自動讀取 .github/workflows/ 下所有 yml\nls .github/workflows/\n# Expected: ci.yml, cd.yml, security-scan.yml\n\n# 列出 jobs\nact -l\n# Expected (範例):\n#  Stage  Job ID            Job name\n#  0      build              Build & Test\n#  1      integration-test   Integration Test (Testcontainers)\n#  2      docker-build       Docker Build & Push\n```\n\nMulti-branch 對應方式（act 預設用 `push` event；可指定 PR）：\n\n```bash\n# 模擬 push 到 main\nact push -W .github/workflows/ci.yml\n\n# 模擬 PR\nact pull_request -W .github/workflows/ci.yml\n```\n\n**對應 GitHub Repo URL**：`https://github.com/erp-org/erp-api-token-manager`\n\n### 21.3 act 快速本地 Dry-Run（無需 GitHub Actions Server）\n\n> 適合在 push 前快速驗證 `.github/workflows/ci.yml` 語法和邏輯，不需推到 remote。\n\n```bash\n# 設定 mock secrets（避免 CI 憑證外洩）\ncat > .secrets <<EOF\nGHCR_TOKEN=mock-token\nDB_PASSWORD=mock-db-password\nHMAC_SECRET=mock-hmac-secret-64chars-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\nDATA_PROTECTION_KEY=mock-dp-key\nEOF\nchmod 600 .secrets\n\n# 執行 dry-run（默認用 -P ubuntu-latest=catthehacker/ubuntu:act-latest，內建 dotnet 8 SDK）\nact push --secret-file .secrets\n\n# 或使用 make target\nmake ci-dry-run\n```\n\n**預期輸出**：\n```\n[CI/build] 🚀  Start image=catthehacker/ubuntu:act-latest\n[CI/build]   ✅  Success - Setup .NET 8 SDK\n[CI/build]   ✅  Success - dotnet restore\n[CI/build]   ✅  Success - dotnet build\n[CI/build]   ✅  Success - dotnet test src/Application.Tests\n[CI/build]   ✅  Success - dotnet test src/Infrastructure.IntegrationTests\n[CI/build] 🏁  Job succeeded\n```\n\n### 21.4 CI 所需 Secrets 設定\n\nCI secrets 分類（依 §3.5 三層策略）：\n\n| Secret | 來源層 | 設定方式 |\n|--------|-------|---------|\n| `GHCR_TOKEN` | 層 2（OS Keychain）| `security find-generic-password -w -s \"erp-token-ghcr\" -a \"dev\"` 或從 1Password 讀取 |\n| `DB_PASSWORD` | 層 1（Ephemeral，CI 用 mock）| `.secrets` 檔（gitignored，每次 CI run 隨機生成）|\n| `HMAC_SECRET` | 層 1 | 同上 |\n| `DATA_PROTECTION_KEY` | 層 1 | 同上 |\n\n```bash\n# K8s 內也建立 ci namespace 的 secret（若 act 走 k8s mode）\nkubectl create namespace ci\n\n# Registry Token\nGHCR_TOKEN=$(security find-generic-password -w -s \"erp-token-ghcr\" -a \"dev\")\nkubectl create secret generic registry-credentials \\\n  --from-literal=username=erp-org-bot \\\n  --from-literal=password=\"${GHCR_TOKEN}\" \\\n  -n ci\n\n# 應用層 Ephemeral secrets（隨機生成 mock）\nkubectl create secret generic app-secrets \\\n  --from-literal=DB_PASSWORD=\"$(openssl rand -hex 32)\" \\\n  --from-literal=HMAC_SECRET=\"$(openssl rand -hex 64)\" \\\n  --from-literal=DATA_PROTECTION_KEY=\"$(openssl rand -hex 32)\" \\\n  -n ci\n\n# 確認所有 secrets 存在\nkubectl get secret -n ci\n```\n\n### 21.5 ArgoCD CD 層設定（GitOps）\n\n```bash\n# 安裝 ArgoCD\nkubectl create namespace argocd\nkubectl apply -n argocd \\\n  -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml\n\n# 等待就緒\nkubectl rollout status deployment/argocd-server -n argocd --timeout=300s\n\n# Port Forward\nkubectl port-forward svc/argocd-server -n argocd 8443:443 &\n\n# 取得初始密碼\nkubectl -n argocd get secret argocd-initial-admin-secret \\\n  -o jsonpath=\"{.data.password}\" | base64 -d; echo\n\n# 登入 CLI\nargocd login localhost:8443 --username admin --insecure\n\n# 建立 Application（對應 k8s/overlays/local）\nargocd app create erp-token-local \\\n  --repo https://github.com/erp-org/erp-api-token-manager \\\n  --path k8s/overlays/local \\\n  --dest-server https://kubernetes.default.svc \\\n  --dest-namespace erp-token-local \\\n  --sync-policy automated\n```\n\n### 21.6 Shared Make Targets（CI ↔ 本地對齊）\n\n| Target | 作用 | CI 呼叫 | 本地可重現 |\n|--------|------|---------|-----------|\n| `make ci-build` | `dotnet build -c Release` | Stage: Build | OK |\n| `make ci-test-unit` | `dotnet test src/Application.Tests/` | Stage: Unit Test | OK |\n| `make ci-test-integration` | `dotnet test src/Infrastructure.IntegrationTests/`（Testcontainers）| Stage: Integration Test | OK（需 nerdctl/docker socket）|\n| `make ci-build-image` | `nerdctl build -t erp-token/api:local -f docker/api/Dockerfile .` | Stage: Build & Push | OK |\n| `make ci-deploy` | `kubectl apply -k k8s/overlays/local/` | Stage: Deploy（local profile）| OK |\n| `make ci-smoke` | `curl -sf http://erp-token.local/health/ready` + 主流程 e2e | Stage: Smoke Test | OK |\n| `make ci-rollback` | `kubectl rollout undo deploy/erp-api-token-manager -n erp-token-local` | post { failure } | OK |\n| `make ci-dry-run` | `act push --secret-file .secrets` | 手動觸發 | OK |\n\n> 確保 Makefile 中以上 8 個 targets 全部存在，CI 和本地使用完全相同的 target 名稱。\n\n### 21.7 PR Gate 驗證（Pre-Push Checklist）\n\n在 push 到 feature branch 之前，確認以下項目：\n\n```bash\n# 1. 所有 CI targets 本地通過\nmake ci-build\nmake ci-test-unit\nmake ci-test-integration   # 需 Rancher Desktop 運行（Testcontainers）\n\n# 2. act dry-run 無錯誤\nmake ci-dry-run\n\n# 3. Image 可本地 build\nmake ci-build-image\n\n# 4. Deploy + Smoke 通過\nmake ci-deploy && make ci-smoke\n\n# 5. 安全掃描\nmake secscan\n\n# 全部通過後才 push\ngit push origin feature/your-branch\n```\n\n### 21.8 常見問題排查\n\n| 問題 | 原因 | 解決 |\n|------|------|------|\n| `act: command not found` | act 未安裝 | `brew install act` |\n| `act: workflow doesn't exist` | `.github/workflows/` 路徑錯誤 | `act -W .github/workflows/ci.yml` 明確指定 |\n| `Cannot connect to Docker daemon` | act 找不到 Docker socket | 啟動 Rancher Desktop 並確認 `docker info` 有輸出；或設 `DOCKER_HOST=unix:///var/run/docker.sock` |\n| `ImagePullBackOff` in ci namespace | `registry-credentials` secret 未設定 | 執行 §21.4 Registry Token 步驟 |\n| `ArgoCD: App not synced` | Git commit 未 push 或 path 錯誤 | 確認 `spec.source.path: k8s/overlays/local`，`argocd app sync erp-token-local` |\n| `make ci-test-integration` 失敗 | Rancher Desktop 未運行（Testcontainers 起不了 postgres）| 啟動 Rancher Desktop → 執行 §4 setup |\n| GitHub Actions 在 act 內 EF Core migration 失敗 | act 環境無 dotnet-ef 全域工具 | `.github/workflows/ci.yml` 加入 `dotnet tool install -g dotnet-ef` 步驟 |\n\n---\n\n## 22. Service Startup / Shutdown Sequencing\n\nLOCAL_DEPLOY 必須包含服務啟動和關閉的依賴順序，避免「先啟動者連線失敗」問題。\n\n### 啟動順序（Dependency Graph）\n\n```\n[Infrastructure Layer]\n  PostgreSQL（StatefulSet, replicas=1）\n          ↓\n  Wait: pg_isready -h postgres -p 5432  → ready\n  Wait: SELECT 1                         → 1 row\n          ↓\n[Application Layer]\n  erp-api-token-manager（Deployment, replicas=2）\n  - Pod 1：startup probe → readiness probe → ready\n  - Pod 2：startup probe → readiness probe → ready\n          ↓\n  Wait: 兩個 Pod 都 Ready\n          ↓\n[DB Schema Layer]（一次性，部署後執行一次）\n  EF Core Migration：dotnet ef database update\n  Seed data：dotnet run --project src/Infrastructure -- seed --env Local\n          ↓\n[Gateway Layer]\n  Ingress（traefik，k3s 內建，無需手動啟動）\n  → http://erp-token.local 可達\n          ↓\n[Optional Layer]\n  pgadmin（Deployment, replicas=1）\n  jaeger-aio（Deployment, replicas=1，dev override）\n```\n\n### 每個服務的就緒判斷（Readiness Gate）\n\n| 服務 | Readiness Check | 逾時 | 失敗行為 |\n|-----|----------------|------|---------|\n| PostgreSQL | `pg_isready -h postgres -p 5432` 或 K8s readinessProbe `tcpSocket: 5432` | 30s | 停止啟動，顯示錯誤；查看 `kubectl logs postgres-0` |\n| api-server（Liveness）| `GET /health/live` → `Healthy` (200) | 60s（initialDelay 15s + period 30s × 2 retries）| Liveness 失敗 → kubelet kill pod 重啟 |\n| api-server（Readiness）| `GET /health/ready` → `Healthy` (200，含 DB 連線檢查 `SELECT 1`）| 90s（initialDelay 10s + period 15s × 5 retries）| Readiness 失敗 → 從 Service endpoints 移除，不接流量 |\n| Ingress（traefik）| `kubectl get pod -n kube-system -l app=traefik` Running | 60s | k3s 內建，通常自動 Ready |\n| pgadmin | `GET /misc/ping` → `PING` 字串 | 60s | 停止啟動 |\n\n### Wait 範例（在 §3 Quick Start / §4 部署中已套用）\n\n```bash\n# 1. Wait postgres ready\nkubectl wait --for=condition=Ready pod/postgres-0 -n erp-token-local --timeout=60s\n\n# 2. Wait api-server 兩個副本都 ready\nkubectl wait --for=condition=Ready pods -l app=erp-api-token-manager \\\n  -n erp-token-local --timeout=180s\n\n# 3. Wait health/ready 真的回 200（K8s readinessProbe 已通過，但有時 traefik 路由要 5s 同步）\nfor i in {1..30}; do\n  curl -sf http://erp-token.local/health/ready && break\n  sleep 2\ndone\n```\n\n### 優雅關閉（Graceful Shutdown）\n\n| 關閉順序 | 服務 | drain timeout | 強制 kill timeout |\n|---------|-----|--------------|-----------------|\n| 1 | api-server（Deployment）| 30s（等待進行中請求完成；ASP.NET Core `HostOptions.ShutdownTimeout=25s`）| 60s（K8s `terminationGracePeriodSeconds=60`）|\n| 2 | pgadmin（Deployment）| 10s | 30s |\n| 3 | postgres（StatefulSet）| `pg_ctl stop -m fast` 30s drain | 60s |\n| 4 | jaeger-aio（選配）| 不 drain（記憶體存儲）| 10s |\n\nK8s 設定：\n\n```yaml\n# api-server Deployment.spec.template.spec\nterminationGracePeriodSeconds: 60\ncontainers:\n  - name: app\n    lifecycle:\n      preStop:\n        exec:\n          command: [\"sh\", \"-c\", \"sleep 5\"]   # 等 Service endpoints 移除後再 SIGTERM\n```\n\n關閉驗證：\n\n```bash\n# 觸發優雅關閉\nkubectl delete pod erp-api-token-manager-<hash> -n erp-token-local --grace-period=60\n\n# 觀察過程：應看到 logs 顯示 \"Application stopping\" → \"Application stopped\"\nkubectl logs erp-api-token-manager-<hash> -n erp-token-local --tail=20\n```\n\n> **禁止**：LOCAL_DEPLOY 無啟動順序說明；依賴服務啟動無就緒判斷（導致 api-server 連 DB 前，DB pod 尚未就緒，CrashLoopBackOff）。\n\n---\n\n## 23. Smoke Test & Verification Checklist\n\n部署完成後執行以下完整驗證，每一步均有可驗證的 Expected 輸出。\n\n### 23.1 一鍵 Smoke Test（推薦）\n\n```bash\nmake ci-smoke\n```\n\n等同執行以下完整檢查：\n\n```bash\n#!/usr/bin/env bash\n# scripts/smoke-test.sh — Local Smoke Test\nset -e\n\nNS=\"erp-token-local\"\nBASE=\"http://erp-token.local\"\n\necho \"=== [1/8] K8s Pod Status ===\"\nkubectl get pods -n \"$NS\"\nrunning=$(kubectl get pods -n \"$NS\" --field-selector=status.phase=Running --no-headers | wc -l | tr -d ' ')\n[[ \"$running\" -ge 4 ]] || { echo \"FAIL: only $running pods Running (expected ≥4: 2 api + 1 db + 1 pgadmin)\"; exit 1; }\necho \"OK: $running pods Running\"\n\necho \"=== [2/8] HA Replicas ≥ 2 ===\"\napi_count=$(kubectl get pods -n \"$NS\" -l app=erp-api-token-manager --field-selector=status.phase=Running --no-headers | wc -l | tr -d ' ')\n[[ \"$api_count\" -ge 2 ]] || { echo \"FAIL: api-server only has $api_count replicas (expected ≥2)\"; exit 1; }\necho \"OK: api-server has $api_count replicas\"\n\necho \"=== [3/8] Liveness Probe ===\"\ncurl -sfo /dev/null \"$BASE/health/live\" || { echo \"FAIL: /health/live\"; exit 1; }\necho \"OK: /health/live\"\n\necho \"=== [4/8] Readiness Probe (DB connectivity) ===\"\ncurl -sfo /dev/null \"$BASE/health/ready\" || { echo \"FAIL: /health/ready\"; exit 1; }\necho \"OK: /health/ready\"\n\necho \"=== [5/8] Razor Pages UI ===\"\nstatus=$(curl -s -o /dev/null -w \"%{http_code}\" \"$BASE/\")\n[[ \"$status\" == \"200\" ]] || { echo \"FAIL: Razor / returned $status\"; exit 1; }\necho \"OK: Razor / returned 200\"\n\necho \"=== [6/8] POC API ===\"\npoc=$(curl -s \"$BASE/api/poc/health\" | jq -r .status)\n[[ \"$poc\" == \"ok\" ]] || { echo \"FAIL: /api/poc/health returned $poc\"; exit 1; }\necho \"OK: /api/poc/health returned ok\"\n\necho \"=== [7/8] DB Schema ===\"\ntable_count=$(kubectl exec -n \"$NS\" statefulset/postgres -- \\\n  psql -U erpapp -d erpapikey -tAc \\\n  \"SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('api_tokens','token_audit_logs','__EFMigrationsHistory');\")\n[[ \"$table_count\" == \"3\" ]] || { echo \"FAIL: only $table_count/3 tables exist\"; exit 1; }\necho \"OK: 3/3 expected tables exist\"\n\necho \"=== [8/8] HA Failover (kill one api pod, verify recovery) ===\"\nvictim=$(kubectl get pod -n \"$NS\" -l app=erp-api-token-manager -o jsonpath='{.items[0].metadata.name}')\nkubectl delete pod \"$victim\" -n \"$NS\" --wait=false\nsleep 6\nready=$(curl -s \"$BASE/health/ready\" 2>/dev/null || echo \"\")\n[[ \"$ready\" == \"Healthy\" ]] || { echo \"FAIL: HA failover; /health/ready not Healthy after kill\"; exit 1; }\necho \"OK: HA recovered in ≤5s after pod kill\"\n\necho \"\"\necho \"OK: All smoke tests passed.\"\n```\n\n### 23.2 完整驗證清單（人工逐項勾選）\n\n**部署成功（共 5 項，§4 驗收）：**\n- [ ] §3 Quick Start 全 9 步順利完成，每步無錯誤輸出\n- [ ] `kubectl get pods -n erp-token-local` 顯示 ≥ 4 個 pod（2 api + 1 postgres + 1 pgadmin）皆 `Running`\n- [ ] `kubectl get pods -n erp-token-local -l app=erp-api-token-manager` 顯示 2 個副本（HA Iron Constraint）\n- [ ] `kubectl get secret -n erp-token-local` 顯示 3 個 secret（api / db / pgadmin）\n- [ ] `kubectl get configmap -n erp-token-local` 顯示 2 個 configmap（api-config / db-config）\n\n**功能驗收（共 7 項，§7/§8 驗收）：**\n- [ ] `curl -sf http://erp-token.local/health/live` 回 `Healthy`\n- [ ] `curl -sf http://erp-token.local/health/ready` 回 `Healthy`（DB 連線正常）\n- [ ] 瀏覽器開 `http://erp-token.local/ApiTokens` 重定向至 `/Account/Login`\n- [ ] 用 `admin@erp-token.local` / `Password1!` 登入成功，可進入 `/ApiTokens` 列表\n- [ ] 點「建立 Token」，輸入描述，submit 成功，看到一次性 raw token\n- [ ] `curl -H \"Authorization: Bearer <raw>\" http://erp-token.local/api/poc/sales` 回 200 + simulated data\n- [ ] `BusinessUser` 帳號訪問 `/ApiTokens/AuditLog` 回 403；`ITAdmin` 帳號訪問成功\n\n**HA 驗證（共 3 項，覆寫 EDD §16.2.1 minReplicas=1）：**\n- [ ] `kubectl delete pod $(kubectl get pods -n erp-token-local -l app=erp-api-token-manager -o jsonpath='{.items[0].metadata.name}')` 後，`/health/ready` ≤ 5s 內仍回 200（流量自動切到另一 pod）\n- [ ] DataProtection Key 在兩個 pod 間共享（從 Pod-1 登入產生的 cookie，發往 Pod-2 仍有效，AntiForgery 不報錯）\n- [ ] `kubectl rollout restart deploy/erp-api-token-manager -n erp-token-local` 滾動重啟期間，`curl /health/ready` 連續 60s 全 200（無中斷）\n\n**安全驗證（共 4 項）：**\n- [ ] `git check-ignore -v k8s/overlays/local/secrets.env` 顯示 `.gitignore` 已忽略\n- [ ] `kubectl get secret -n erp-token-local -o yaml | grep \"stringData\"` 應為空（所有 secret 均為 base64 data，無明文）\n- [ ] `bootstrap-secrets.sh` 全用 `openssl rand` 生成隨機值，無硬編碼明文密碼\n- [ ] `.gitignore` 含 `*.env`、`secrets.env`、`.env.*`、`erp-token.local-key.pem` 規則\n\n**清理驗證（共 2 項）：**\n- [ ] `make k8s-clean` 後 `kubectl get namespace erp-token-local` 回 `NotFound`\n- [ ] `nerdctl --namespace=k8s.io rmi erp-token/api:local` 後 `nerdctl images | grep erp-token` 為空\n\n> **驗收門檻**：所有勾選項全部通過，方視為 LOCAL_DEPLOY 環境就緒，可進入 §8 開發或 §21 CI/CD 模擬。任一項失敗，回到 §10 Common Issues 排查或聯絡 Application Team。\n",
  "expected_html": "<div class=\"diagram-container\"><pre class=\"mermaid\">graph TB\n    Dev[&quot;Developer&lt;br/&gt;(localhost)&quot;]\n    Ingress[&quot;Ingress (traefik)&lt;br/&gt;erp-token.local&quot;]\n\n    subgraph K8s[&quot;K8s Namespace: erp-token-local&quot;]\n        API[&quot;erp-api-token-manager&lt;br/&gt;Deployment (replicas=2)&lt;br/&gt;:8080&quot;]\n        DB[(&quot;postgres&lt;br/&gt;StatefulSet&lt;br/&gt;:5432&quot;)]\n        PGA[&quot;pgadmin&lt;br/&gt;Deployment&lt;br/&gt;:80&quot;]\n        Jaeger[&quot;jaeger-aio (optional)&lt;br/&gt;Deployment&lt;br/&gt;UI :16686 / OTLP :4317&quot;]\n    end\n\n    Dev --&gt;|&quot;HTTP via Ingress / port-forward&quot;| Ingress\n    Ingress --&gt;|&quot;/&quot;| API\n    Ingress --&gt;|&quot;/api/&quot;| API\n    Ingress --&gt;|&quot;/health/*&quot;| API\n    Ingress --&gt;|&quot;/pgadmin&quot;| PGA\n    Ingress --&gt;|&quot;/jaeger&quot;| Jaeger\n    Dev --&gt;|kubectl port-forward 5432| DB\n    Dev --&gt;|kubectl port-forward 5050| PGA\n    API --&gt;|ClusterIP TCP 5432| DB\n    API -.-&gt;|&quot;OTLP :4317 (optional)&quot;| Jaeger</pre></div>\n<p><strong>K8s 資源對照（完整清單，與 §3/§4/§5 完全對齊）：</strong></p>\n<table>\n<tr><th>服務</th><th>Kind</th><th>Image</th><th>ConfigMap</th><th>Secret</th><th>Resource Limits</th></tr>\n<tr><td>erp-api-token-manager</td><td>Deployment (replicas: 2)</td><td><code>erp-token/api:local</code></td><td><code>erp-token-api-config</code></td><td><code>erp-token-api-secret</code></td><td>CPU 500m / Mem 512Mi（per pod，依 EDD §16.2.1 Local 欄）</td></tr>\n<tr><td>postgres</td><td>StatefulSet (replicas: 1)</td><td><code>postgres:16-alpine</code></td><td><code>erp-token-db-config</code></td><td><code>erp-token-db-secret</code></td><td>CPU 500m / Mem 1Gi</td></tr>\n<tr><td>pgadmin</td><td>Deployment (replicas: 1)</td><td><code>dpage/pgadmin4:8.x</code></td><td>—</td><td><code>erp-token-pgadmin-secret</code></td><td>CPU 200m / Mem 256Mi</td></tr>\n<tr><td>jaeger-aio（選配）</td><td>Deployment (replicas: 1)</td><td><code>jaegertracing/all-in-one:1.57</code></td><td>—</td><td>—</td><td>CPU 200m / Mem 512Mi</td></tr>\n</table>\n<blockquote><p><strong>K8s manifest 位置：</strong> <code>k8s/overlays/local/</code>（Kustomize）。Base 在 <code>k8s/base/</code>，與 staging / production overlays 共用，僅資源大小、replicas、ConfigMap 不同。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>imagePullPolicy：</strong> 所有 Deployment / StatefulSet 設定 <code>imagePullPolicy: Never</code>，確保使用本機 <code>nerdctl build</code> 產出的 image，不從 registry 拉取（containerd image store 與 k3s 共用）。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>本地不部署：</strong> Redis（EDD §3.3：Cache=None），Worker / CronJob（EDD §3.3：MQ=None），MinIO / S3（無物件儲存需求），mailpit（無 outbound email），Prometheus / Grafana（觀測平台統一在 staging 起，本地以 stdout JSON log + 選配 Jaeger 即可）。</p></blockquote>\n<h3 id=\"21-bounded-context單一-modular-monolith\">2.1 Bounded Context（單一 Modular Monolith）</h3>\n<p>依 ARCH §4.1 Context Map 與 RUNBOOK §1.3，本模組為單一 Modular Monolith deployment（同 process 內三個 sub-context）：</p>\n<table>\n<tr><th>Sub-Context</th><th>Module Path（C# namespace）</th><th>Owning DB Tables</th><th>Public Surface</th></tr>\n<tr><td>api-token-mgmt</td><td><code>ErpApi.Domain.ApiToken</code> + <code>ErpApi.Application.ApiToken</code></td><td><code>api_tokens</code>, <code>token_audit_logs</code></td><td><code>/api/tokens/<em></code>, <code>/ApiTokens/</em></code>（Razor）</td></tr>\n<tr><td>api-token-auth</td><td><code>ErpApi.Infrastructure.Auth</code></td><td><code>api_tokens</code>（read-only hot path）</td><td>全域 middleware（套用 <code>/api/poc/*</code>）</td></tr>\n<tr><td>poc-demo-api</td><td><code>ErpApi.Presentation.PocController</code></td><td>None（mock data）</td><td><code>/api/poc/sales</code>, <code>/api/poc/inventory</code>, <code>/api/poc/orders/{id}/status</code>, <code>/api/poc/health</code></td></tr>\n</table>\n<blockquote><p>本地環境不做跨 BC stub（無微服務拆分需求）；BC 隔離測試由 ArchUnit / Test 覆蓋（見 test-plan.md §3）。</p></blockquote>\n<hr>\n<h2 id=\"3-quick-start5-分鐘上手\">3. Quick Start（5 分鐘上手）</h2>\n<p>適合熟悉 K8s 的工程師。初次設定請使用 §4 Step-by-Step。</p>\n<pre><code class=\"language-bash\"># 1. Clone 並進入專案\ngit clone git@github.com:erp-org/erp-api-token-manager.git\ncd erp-api-token-manager\n\n# 2. 確認 kubectl context 為 Rancher Desktop\nkubectl config use-context rancher-desktop\n\n# 3. 設定 /etc/hosts（一次性）— 讓 Ingress domain 可解析\necho &quot;127.0.0.1   erp-token.local api.erp-token.local&quot; | sudo tee -a /etc/hosts\n\n# 4. 建立 namespace 與 Secret（執行 bootstrap script，見 §3.5）\nmake k8s-init\n\n# 5. Build 並載入 image（containerd image store）\nmake image-build-all\n\n# 6. 部署所有 K8s 資源\nmake k8s-apply\n\n# 7. 等待所有 Pod 就緒\nkubectl wait --for=condition=Ready pods --all -n erp-token-local --timeout=180s\n\n# 8. 初始化資料庫（EF Core migration + seed）\nmake db-migrate\nmake db-seed\n\n# 9. 驗證所有服務健康\nmake health-check</code></pre>\n<p>預期輸出：</p>\n<pre><code class=\"\">[OK] api-server   http://erp-token.local/health/live   (200 Healthy)\n[OK] api-server   http://erp-token.local/health/ready  (200 Healthy)\n[OK] postgres     pod/postgres-0 — Running\n[OK] pgadmin      pod/pgadmin-&lt;hash&gt; — Running\n[OK] api-server   pod/erp-api-token-manager-&lt;h1&gt; — Running (1/2)\n[OK] api-server   pod/erp-api-token-manager-&lt;h2&gt; — Running (2/2)</code></pre>\n<p>如有任何 <code>[FAIL]</code>，請進入 §10 Common Issues。</p>\n<hr>\n<h2 id=\"35-secret-bootstrap密碼安全管理\">3.5 Secret Bootstrap（密碼安全管理）</h2>\n<blockquote><p><strong>原則</strong>：所有 K8s Secret <strong>禁止進 git</strong>。禁止使用靜態明文密碼（如 <code>password: secret</code>）。本節定義三層密碼策略，確保本地開發環境的安全性符合企業標準。</p></blockquote>\n<h3 id=\"禁止規則\">禁止規則</h3>\n<p>以下檔案 <strong>必須</strong> 加入 <code>.gitignore</code>，違規者視為安全事故：</p>\n<pre><code class=\"language-gitignore\"># Secret files — never commit\n*.env\n.env\n.env.*\nsecrets.env\nk8s/overlays/local/secrets.env\nscripts/bootstrap-secrets.sh.local</code></pre>\n<p>驗證：</p>\n<pre><code class=\"language-bash\">grep -E &#x27;\\.env|secrets\\.env&#x27; .gitignore &amp;&amp; echo &quot;OK: .gitignore covers secrets&quot; || echo &quot;ERROR: .gitignore missing secret patterns&quot;\ngit check-ignore -v k8s/overlays/local/secrets.env\n# Expected: 顯示 .gitignore 規則命中</code></pre>\n<h3 id=\"三層-secret-策略\">三層 Secret 策略</h3>\n<table>\n<tr><th>層</th><th>類型</th><th>適用密碼</th><th>管理方式</th></tr>\n<tr><td><strong>層 1</strong></td><td>Ephemeral（每次重生成）</td><td><code>DB_PASSWORD</code>、<code>HMAC_SECRET</code>（v2 預留）、<code>PGADMIN_DEFAULT_PASSWORD</code>、<code>DATA_PROTECTION_KEY</code></td><td>每次 <code>make k8s-init</code> 或 <code>make secrets-rotate</code> 重生成（openssl rand）</td></tr>\n<tr><td><strong>層 2</strong></td><td>OS Keychain（固定憑證）</td><td><code>GHCR_TOKEN</code>（image push）、<code>NUGET_API_KEY</code></td><td>macOS Keychain / Windows Credential Manager</td></tr>\n<tr><td><strong>層 3</strong></td><td>Enterprise Password Manager（可選）</td><td><code>JAEGER_AUTH_TOKEN</code>（若啟用 Jaeger UI auth）</td><td>1Password <code>op inject</code> / Bitwarden Secrets Manager</td></tr>\n</table>\n<hr>\n<h3 id=\"層-1ephemeral-密碼-bootstrap必做\">層 1：Ephemeral 密碼 Bootstrap（必做）</h3>\n<p>每次重啟 cluster 或執行 <code>make k8s-clean</code> 後，執行以下 bootstrap script 重新生成所有密碼。</p>\n<p><strong>macOS / Linux — <code>scripts/bootstrap-secrets.sh</code>：</strong></p>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\n# scripts/bootstrap-secrets.sh\n# Auto-generate ephemeral K8s secrets — never commit this output\nset -euo pipefail\n\nNAMESPACE=&quot;${NAMESPACE:-erp-token-local}&quot;\nPROJECT_SLUG=&quot;erp-token&quot;\n\necho &quot;[bootstrap-secrets] Generating ephemeral secrets for namespace: ${NAMESPACE}&quot;\n\n# Ensure namespace exists\nkubectl create namespace &quot;${NAMESPACE}&quot; --dry-run=client -o yaml | kubectl apply -f -\n\n# Delete existing secrets to force regeneration\nkubectl delete secret &quot;${PROJECT_SLUG}-api-secret&quot; -n &quot;${NAMESPACE}&quot; --ignore-not-found\nkubectl delete secret &quot;${PROJECT_SLUG}-db-secret&quot;  -n &quot;${NAMESPACE}&quot; --ignore-not-found\nkubectl delete secret &quot;${PROJECT_SLUG}-pgadmin-secret&quot; -n &quot;${NAMESPACE}&quot; --ignore-not-found\n\n# Generate ephemeral values\nDB_PASSWORD=&quot;$(openssl rand -hex 32)&quot;\nHMAC_SECRET=&quot;$(openssl rand -hex 64)&quot;\nDATA_PROTECTION_KEY=&quot;$(openssl rand -hex 32)&quot;\nPGADMIN_PASSWORD=&quot;$(openssl rand -base64 16 | tr -dc &#x27;a-zA-Z0-9&#x27; | head -c 16)&quot;\n\n# api-server connection string (Npgsql format)\nCONNECTION_STRING=&quot;Host=postgres;Port=5432;Database=erpapikey;Username=erpapp;Password=${DB_PASSWORD}&quot;\n\n# api-server Secret\nkubectl create secret generic &quot;${PROJECT_SLUG}-api-secret&quot; \\\n  -n &quot;${NAMESPACE}&quot; \\\n  --from-literal=ConnectionStrings__ApiTokenDb=&quot;${CONNECTION_STRING}&quot; \\\n  --from-literal=HmacSecret=&quot;${HMAC_SECRET}&quot; \\\n  --from-literal=DataProtection__Key=&quot;${DATA_PROTECTION_KEY}&quot;\n\n# postgres bootstrap Secret\nkubectl create secret generic &quot;${PROJECT_SLUG}-db-secret&quot; \\\n  -n &quot;${NAMESPACE}&quot; \\\n  --from-literal=POSTGRES_DB=&quot;erpapikey&quot; \\\n  --from-literal=POSTGRES_USER=&quot;erpapp&quot; \\\n  --from-literal=POSTGRES_PASSWORD=&quot;${DB_PASSWORD}&quot;\n\n# pgadmin Secret\nkubectl create secret generic &quot;${PROJECT_SLUG}-pgadmin-secret&quot; \\\n  -n &quot;${NAMESPACE}&quot; \\\n  --from-literal=PGADMIN_DEFAULT_EMAIL=&quot;admin@erp-token.local&quot; \\\n  --from-literal=PGADMIN_DEFAULT_PASSWORD=&quot;${PGADMIN_PASSWORD}&quot;\n\necho &quot;[bootstrap-secrets] OK: Ephemeral secrets created in ${NAMESPACE}&quot;\necho &quot;[bootstrap-secrets] Secrets are ephemeral — regenerated on every cluster restart&quot;\necho &quot;[bootstrap-secrets] pgadmin login: admin@erp-token.local / ${PGADMIN_PASSWORD}&quot;</code></pre>\n<pre><code class=\"language-bash\"># 每次 cluster 啟動時執行\nchmod +x scripts/bootstrap-secrets.sh\n./scripts/bootstrap-secrets.sh\n\n# 或透過 make target（§6 Development Commands 已提供）\nmake secrets-rotate</code></pre>\n<p><strong>Windows — <code>scripts/bootstrap-secrets.ps1</code>：</strong></p>\n<pre><code class=\"language-powershell\"># scripts/bootstrap-secrets.ps1\n# Auto-generate ephemeral K8s secrets — never commit this output\nparam(\n    [string]$Namespace = &quot;erp-token-local&quot;\n)\n\n$ProjectSlug = &quot;erp-token&quot;\n\nWrite-Host &quot;[bootstrap-secrets] Generating ephemeral secrets for namespace: $Namespace&quot;\n\n# Ensure namespace exists\nkubectl create namespace $Namespace --dry-run=client -o yaml | kubectl apply -f -\n\n# Delete existing secrets\nforeach ($s in @(&quot;$ProjectSlug-api-secret&quot;,&quot;$ProjectSlug-db-secret&quot;,&quot;$ProjectSlug-pgadmin-secret&quot;)) {\n    kubectl delete secret $s -n $Namespace --ignore-not-found\n}\n\n# Helper: generate random hex string with CSPRNG\nfunction New-RandomHex([int]$Bytes) {\n    $rng = [System.Security.Cryptography.RandomNumberGenerator]::Create()\n    $arr = New-Object byte[] $Bytes\n    $rng.GetBytes($arr)\n    return ([System.BitConverter]::ToString($arr) -replace &#x27;-&#x27;,&#x27;&#x27;).ToLower()\n}\n\n$DbPassword         = New-RandomHex 32\n$HmacSecret         = New-RandomHex 64\n$DataProtectionKey  = New-RandomHex 32\n$PgadminPassword    = (New-RandomHex 12).Substring(0,16)\n\n$ConnectionString = &quot;Host=postgres;Port=5432;Database=erpapikey;Username=erpapp;Password=$DbPassword&quot;\n\nkubectl create secret generic &quot;$ProjectSlug-api-secret&quot; -n $Namespace `\n    --from-literal=&quot;ConnectionStrings__ApiTokenDb=$ConnectionString&quot; `\n    --from-literal=&quot;HmacSecret=$HmacSecret&quot; `\n    --from-literal=&quot;DataProtection__Key=$DataProtectionKey&quot;\n\nkubectl create secret generic &quot;$ProjectSlug-db-secret&quot; -n $Namespace `\n    --from-literal=&quot;POSTGRES_DB=erpapikey&quot; `\n    --from-literal=&quot;POSTGRES_USER=erpapp&quot; `\n    --from-literal=&quot;POSTGRES_PASSWORD=$DbPassword&quot;\n\nkubectl create secret generic &quot;$ProjectSlug-pgadmin-secret&quot; -n $Namespace `\n    --from-literal=&quot;PGADMIN_DEFAULT_EMAIL=admin@erp-token.local&quot; `\n    --from-literal=&quot;PGADMIN_DEFAULT_PASSWORD=$PgadminPassword&quot;\n\nWrite-Host &quot;[bootstrap-secrets] OK: Ephemeral secrets created in $Namespace&quot;\nWrite-Host &quot;[bootstrap-secrets] pgadmin login: admin@erp-token.local / $PgadminPassword&quot;</code></pre>\n<pre><code class=\"language-powershell\"># 執行\n.\\scripts\\bootstrap-secrets.ps1\n# 或指定 namespace\n.\\scripts\\bootstrap-secrets.ps1 -Namespace &quot;erp-token-local&quot;</code></pre>\n<p><strong>重生成時機：</strong></p>\n<pre><code class=\"language-bash\"># 每次執行以下任何一個動作後，必須重跑 bootstrap script\nmake k8s-clean        # 刪除 namespace（所有 secret 一起被刪）\nmake cluster-reset    # 完全重建 cluster\nmake secrets-rotate   # 主動輪換（安全需求或 secret 洩漏後）</code></pre>\n<hr>\n<h3 id=\"層-2os-keychain-固定憑證視需求\">層 2：OS Keychain 固定憑證（視需求）</h3>\n<p>適用於<strong>不能每次重生成</strong>的憑證。對 ERP API Token Manager 開發者主要為 GHCR Token（image push）。</p>\n<p><strong>macOS Keychain（<code>security</code> CLI）：</strong></p>\n<pre><code class=\"language-bash\"># 儲存憑證到 macOS Keychain（首次設定）\nsecurity add-generic-password \\\n  -s &quot;erp-token-ghcr&quot; \\\n  -a &quot;dev&quot; \\\n  -w &quot;&lt;YOUR_GHCR_PAT_TOKEN&gt;&quot;\n\n# 讀取憑證並登入（CI dry-run 或本機 push）\nGHCR_TOKEN=$(security find-generic-password -w -s &quot;erp-token-ghcr&quot; -a &quot;dev&quot;)\necho &quot;${GHCR_TOKEN}&quot; | nerdctl login ghcr.io --username erp-org-bot --password-stdin</code></pre>\n<p><strong>Windows Credential Manager（PowerShell）：</strong></p>\n<pre><code class=\"language-powershell\">Install-Module -Name CredentialManager -Force\nNew-StoredCredential -Target &quot;erp-token-ghcr&quot; -UserName &quot;erp-org-bot&quot; -Password &quot;&lt;YOUR_GHCR_PAT_TOKEN&gt;&quot;\n\n$cred = Get-StoredCredential -Target &quot;erp-token-ghcr&quot;\n$token = $cred.GetNetworkCredential().Password\necho $token | nerdctl login ghcr.io --username erp-org-bot --password-stdin</code></pre>\n<hr>\n<h3 id=\"層-3mittwaldkubernetes-secret-generator進階選項\">層 3：mittwald/kubernetes-secret-generator（進階選項）</h3>\n<p>如果偏好 <strong>in-cluster 全自動生成</strong>（無需手動執行 bootstrap script）：</p>\n<pre><code class=\"language-bash\">helm repo add mittwald https://helm.mittwald.de\nhelm upgrade --install secret-generator mittwald/kubernetes-secret-generator \\\n  -n kube-system</code></pre>\n<pre><code class=\"language-yaml\"># k8s/overlays/local/secrets.yaml — 範例（可選方案）\napiVersion: v1\nkind: Secret\nmetadata:\n  name: erp-token-api-secret\n  namespace: erp-token-local\n  annotations:\n    secret-generator.v1.mittwald.de/autogenerate: &quot;HmacSecret,DataProtection__Key&quot;\n    secret-generator.v1.mittwald.de/length: &quot;64&quot;\n    secret-generator.v1.mittwald.de/type: &quot;string&quot;\ntype: Opaque</code></pre>\n<blockquote><p><strong>何時用 mittwald</strong>：當不需要在 bootstrap script 中讀取明文密碼（app 直接從 K8s Secret 讀取）時較便利。但 <code>ConnectionStrings__ApiTokenDb</code> 與 <code>POSTGRES_PASSWORD</code> 必須兩處同值，仍建議走層 1 openssl rand 流程。</p></blockquote>\n<hr>\n<h3 id=\"驗證-secret-已正確建立\">驗證 Secret 已正確建立</h3>\n<pre><code class=\"language-bash\"># 確認 secret 存在（不顯示值）\nkubectl get secret -n erp-token-local\n\n# 確認所有 key 都存在\nkubectl get secret erp-token-api-secret -n erp-token-local \\\n  -o jsonpath=&#x27;{.data}&#x27; | python3 -c &quot;\nimport sys, json\nkeys = list(json.load(sys.stdin).keys())\nrequired = [&#x27;ConnectionStrings__ApiTokenDb&#x27;, &#x27;HmacSecret&#x27;, &#x27;DataProtection__Key&#x27;]\nmissing = [k for k in required if k not in keys]\nprint(&#x27;Missing:&#x27;, missing) if missing else print(&#x27;OK: All required api secrets present&#x27;)\n&quot;\n\n# 確認 .gitignore 涵蓋 secret 檔案\ngrep -E &#x27;\\.env|secrets\\.env&#x27; .gitignore &amp;&amp; echo &quot;OK: .gitignore&quot; || echo &quot;ERROR: .gitignore missing secret patterns&quot;</code></pre>\n<hr>\n<h2 id=\"4-step-by-step-setup\">4. Step-by-Step Setup</h2>\n<h3 id=\"41-clone--configure\">4.1 Clone &amp; Configure</h3>\n<pre><code class=\"language-bash\">git clone git@github.com:erp-org/erp-api-token-manager.git\ncd erp-api-token-manager\n\n# 確認 branch\ngit status\n# Expected: On branch main / develop</code></pre>\n<h3 id=\"42-rancher-desktop-設定\">4.2 Rancher Desktop 設定</h3>\n<pre><code class=\"language-bash\"># 1. 確認 k3s context 已注入\nkubectl config get-contexts\n# 應看到 rancher-desktop 並標示 *（current）\n\n# 若未自動設定：\nkubectl config use-context rancher-desktop\n\n# 2. 確認 K8s API server 可連線\nkubectl cluster-info\n# Expected: Kubernetes control plane is running at https://127.0.0.1:6443\n\n# 3. 建立本地 namespace\nkubectl create namespace erp-token-local --dry-run=client -o yaml | kubectl apply -f -\n\n# 4. 設定 /etc/hosts（一次性）— 讓 Ingress domain 可解析\necho &quot;127.0.0.1   erp-token.local api.erp-token.local&quot; | sudo tee -a /etc/hosts\n\n# 驗證 /etc/hosts 設定生效\ngetent hosts erp-token.local || dscacheutil -q host -a name erp-token.local\n# Expected (macOS): 127.0.0.1   erp-token.local</code></pre>\n<h3 id=\"43-建立-k8s-secret\">4.3 建立 K8s Secret</h3>\n<p>Secret 不走 ConfigMap，不提交到 git。Local 環境全為開發隨機值，由 <code>scripts/bootstrap-secrets.sh</code> 自動生成，<strong>無需人工填寫</strong>：</p>\n<pre><code class=\"language-bash\"># 自動建立 namespace + 三個 Secret（呼叫 §3.5 bootstrap script）\nmake k8s-init\n# 等同：\n# bash scripts/bootstrap-secrets.sh\n\n# 若改採 secrets.env + --from-env-file 流程（不推薦於 local，但 staging 沿用此模式）：\n[[ ! -f k8s/overlays/local/secrets.env ]] &amp;&amp; \\\n  cp k8s/overlays/local/secrets.example.env k8s/overlays/local/secrets.env\nkubectl create secret generic erp-token-api-secret \\\n  --from-env-file=k8s/overlays/local/secrets.env \\\n  -n erp-token-local \\\n  --dry-run=client -o yaml | kubectl apply -f -</code></pre>\n<blockquote><p><code>k8s/overlays/local/secrets.env</code> 已加入 <code>.gitignore</code>。<code>secrets.example.env</code> 是變數名稱的唯一來源，新增變數時需同步更新。<code>--from-env-file</code> 比 <code>--from-literal</code> 安全：避免密碼出現在 shell history 與 process list。</p></blockquote>\n<h3 id=\"44-build--載入-image\">4.4 Build &amp; 載入 Image</h3>\n<p>Rancher Desktop 使用 containerd runtime，需用 <code>nerdctl</code> 建置並直接載入到 k3s 內部 image store（不需額外 push step）。</p>\n<pre><code class=\"language-bash\"># 一次 build 全部 image（僅 api，無獨立前端 image — Razor Pages server-rendered）\nmake image-build-all\n\n# 等同（單一 image：包含 ASP.NET Core api + 編譯後的 Razor 視圖 + Tailwind 編譯產出）：\nnerdctl --namespace=k8s.io build \\\n  -t erp-token/api:local \\\n  -f docker/api/Dockerfile .\n\n# 確認 image 存在\nnerdctl --namespace=k8s.io images | grep erp-token\n# Expected: erp-token/api  local  &lt;hash&gt;  ...</code></pre>\n<blockquote><p><strong>imagePullPolicy: Never</strong> — 本地環境所有 Deployment / StatefulSet 在 manifest 設定 <code>imagePullPolicy: Never</code>，確保使用本機 build 的 image，不嘗試從 ghcr.io 拉取。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong><code>--namespace=k8s.io</code></strong> — Rancher Desktop 內 containerd image store 與 k3s 共用 namespace <code>k8s.io</code>；省略此 flag 會把 image 載入 <code>default</code> namespace，導致 Pod 找不到 image。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>客戶端引擎特定 Build 說明</strong>（依 EDD §3.3 <code>client_engine = ASP.NET Core 8 Razor Pages + Alpine.js + Tailwind</code>）：</p></blockquote>\n<p>&gt;</p>\n<blockquote><p>| 引擎類型 | Dockerfile 策略 | 額外前置步驟 |</p></blockquote>\n<blockquote><p>|---------|----------------|-------------|</p></blockquote>\n<blockquote><p>| <strong>ASP.NET Core 8 Razor Pages</strong>（本專案）| multi-stage：<code>mcr.microsoft.com/dotnet/sdk:8.0</code> build → <code>mcr.microsoft.com/dotnet/aspnet:8.0</code> runtime；同 image 內含 Razor views + 編譯後 Tailwind CSS（<code>wwwroot/css/site.css</code>） | （選配）若使用 Tailwind CLI mode：build stage 內 <code>npm ci &amp;&amp; npx tailwindcss -i wwwroot/css/input.css -o wwwroot/css/site.css --minify</code>。預設 dev mode 從 CDN 載入，不需 npm step |</p></blockquote>\n<p>&gt;</p>\n<blockquote><p>&gt; 不適用於本專案：Cocos Creator / Unity WebGL / Phaser 等遊戲引擎（FRONTEND.md §2.3.1 標記 N/A）。</p></blockquote>\n<p><strong>Dockerfile 範例（<code>docker/api/Dockerfile</code>，與 EDD §3.5 Base Image 一致）：</strong></p>\n<pre><code class=\"language-dockerfile\"># Stage 1: build\nFROM mcr.microsoft.com/dotnet/sdk:8.0 AS build\nWORKDIR /src\nCOPY *.sln ./\nCOPY src/ErpApplication/*.csproj         src/ErpApplication/\nCOPY src/Application/*.csproj            src/Application/\nCOPY src/Domain/*.csproj                 src/Domain/\nCOPY src/Infrastructure/*.csproj         src/Infrastructure/\nRUN dotnet restore\nCOPY src/ src/\nRUN dotnet publish src/ErpApplication/ErpApplication.csproj \\\n    -c Release -o /app/publish --no-restore\n\n# Stage 2: optional Tailwind compile (production CSS bundle)\nFROM node:20-alpine AS css\nWORKDIR /css\nCOPY src/ErpApplication/wwwroot/css/input.css ./\nCOPY src/ErpApplication/tailwind.config.js    ./\nRUN npm install -g tailwindcss@3.4.x \\\n &amp;&amp; tailwindcss -i input.css -o site.css --minify\n\n# Stage 3: runtime\nFROM mcr.microsoft.com/dotnet/aspnet:8.0 AS runtime\nWORKDIR /app\nCOPY --from=build /app/publish .\nCOPY --from=css   /css/site.css ./wwwroot/css/site.css\nENV ASPNETCORE_URLS=http://+:8080\nEXPOSE 8080\nENTRYPOINT [&quot;dotnet&quot;, &quot;ErpApplication.dll&quot;]</code></pre>\n<h3 id=\"45-部署所有-k8s-資源\">4.5 部署所有 K8s 資源</h3>\n<pre><code class=\"language-bash\"># 使用 Kustomize 部署 local overlay\nmake k8s-apply\n# 等同：\n# kubectl apply -k k8s/overlays/local/\n\n# 觀察 Pod 啟動狀態（Ctrl+C 結束）\nkubectl get pods -n erp-token-local -w\n\n# 等待全部 Ready\nkubectl wait --for=condition=Ready pods --all -n erp-token-local --timeout=180s</code></pre>\n<blockquote><p><strong>Iron Constraint — Replicas ≥ 2</strong>：覆寫 EDD §16.2.1 中 <code>min_replicas=1</code> 的設定（已記錄於 RUNBOOK §1.2）。本地 K8s Deployment 必須 <code>replicas: 2</code> 以驗證 HA 程式邏輯（DataProtection key sharing、distributed cookie auth、ASP.NET Core distributed cache）。HPA <code>minReplicas</code> 同樣不得低於 2。</p></blockquote>\n<p>合規 Deployment YAML（<code>k8s/overlays/local/deployment-patch.yaml</code>）：</p>\n<pre><code class=\"language-yaml\">apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: erp-api-token-manager\n  namespace: erp-token-local\nspec:\n  replicas: 2  # 本地 HA 最小副本數，禁止填 1（覆寫 EDD §16.2.1）\n  template:\n    spec:\n      containers:\n        - name: app\n          image: erp-token/api:local\n          imagePullPolicy: Never\n          ports:\n            - containerPort: 8080</code></pre>\n<p>預期 Pod 狀態（全部 <code>Running</code>）：</p>\n<pre><code class=\"\">NAME                                       READY   STATUS    RESTARTS   AGE\nerp-api-token-manager-&lt;hash1&gt;              1/1     Running   0          60s\nerp-api-token-manager-&lt;hash2&gt;              1/1     Running   0          60s\npostgres-0                                 1/1     Running   0          60s\npgadmin-&lt;hash&gt;                             1/1     Running   0          60s</code></pre>\n<p>副本數驗證：</p>\n<pre><code class=\"language-bash\">kubectl get pods -n erp-token-local -l app=erp-api-token-manager\n# Expected: 至少 2 個 erp-api-token-manager Pod 狀態為 Running\n\n# 計數驗證\nkubectl get pods -n erp-token-local -l app=erp-api-token-manager \\\n  -o jsonpath=&#x27;{.items[?(@.status.phase==&quot;Running&quot;)].metadata.name}&#x27; | wc -w\n# Expected: 2</code></pre>\n<p>如有 Pod 停在 <code>Pending</code> 或 <code>CrashLoopBackOff</code>，請見 §10。</p>\n<h3 id=\"46-初始化資料庫ef-core-migration--seed\">4.6 初始化資料庫（EF Core Migration + Seed）</h3>\n<p>EDD §3.3 ORM = Entity Framework Core 8 + Npgsql。Migration 命令：<code>dotnet ef database update</code>。</p>\n<pre><code class=\"language-bash\"># 在 api-server pod 內執行 EF Core migration（推薦：與 production 部署同模式）\nmake db-migrate\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef database update --no-build --connection &quot;$ConnectionStrings__ApiTokenDb&quot;\n\n# 載入 seed / fixture 資料\nmake db-seed\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet run --project src/Infrastructure -- seed --env Local\n\n# 查看 migration 狀態\nmake db-status\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef migrations list --no-build</code></pre>\n<p>預期 migration 輸出：</p>\n<pre><code class=\"\">Build started...\nBuild succeeded.\nApplying migration &#x27;20260101_InitialCreate&#x27;.\nApplying migration &#x27;20260315_AddTokenExpiresAt&#x27;.\nDone.</code></pre>\n<p>驗證 schema 已建立：</p>\n<pre><code class=\"language-bash\">kubectl exec -n erp-token-local statefulset/postgres -- \\\n  psql -U erpapp -d erpapikey -c &quot;\\dt&quot;\n# Expected:\n#  Schema |        Name             | Type  | Owner\n# --------+-------------------------+-------+--------\n#  public | __EFMigrationsHistory   | table | erpapp\n#  public | api_tokens              | table | erpapp\n#  public | token_audit_logs        | table | erpapp</code></pre>\n<h3 id=\"47-驗證所有服務健康\">4.7 驗證所有服務健康</h3>\n<pre><code class=\"language-bash\">make health-check</code></pre>\n<p>個別驗證：</p>\n<pre><code class=\"language-bash\"># Liveness（透過 Ingress）\ncurl -sf http://erp-token.local/health/live &amp;&amp; echo &quot; ALIVE&quot;\n# Expected: Healthy ALIVE\n\n# Readiness（透過 Ingress；確認 DB 可連）\ncurl -sf http://erp-token.local/health/ready &amp;&amp; echo &quot; READY&quot;\n# Expected: Healthy READY (HTTP 200)\n\n# API health（POC endpoint，需 Token 才能呼叫業務 API；health 為公開）\ncurl -s http://erp-token.local/api/poc/health | jq .\n# Expected: {&quot;status&quot;:&quot;ok&quot;,&quot;version&quot;:&quot;1.0.0&quot;,&quot;feature_flags&quot;:{&quot;enable_poc_demo_apis&quot;:true}}\n\n# PostgreSQL（port-forward 後）\nkubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &amp;\nPG_PASSWORD=$(kubectl get secret erp-token-db-secret -n erp-token-local \\\n  -o jsonpath=&#x27;{.data.POSTGRES_PASSWORD}&#x27; | base64 -d)\n# WARNING: 此密碼僅為本機開發隨機值，staging / production 環境禁止使用此 port-forward 流程。\nPGPASSWORD=&quot;${PG_PASSWORD}&quot; psql &quot;postgres://erpapp@localhost:5432/erpapikey&quot; -c &quot;SELECT 1;&quot;\n# Expected: 1 row</code></pre>\n<hr>\n<h2 id=\"5-service-reference\">5. Service Reference</h2>\n<h3 id=\"ingress-存取需設定-etchosts\">Ingress 存取（需設定 /etc/hosts）</h3>\n<table>\n<tr><th>服務</th><th>URL</th><th>說明</th></tr>\n<tr><td>api-server (Razor Pages UI)</td><td><code>http://erp-token.local/</code></td><td>主 UI（Login → /ApiTokens 列表）</td></tr>\n<tr><td>api-server (REST API)</td><td><code>http://erp-token.local/api/tokens</code></td><td>Token 管理 REST API</td></tr>\n<tr><td>api-server (POC API)</td><td><code>http://erp-token.local/api/poc/{sales,inventory,orders/{id}/status,health}</code></td><td>POC demo endpoints（Bearer Token）</td></tr>\n<tr><td>api-server (Health)</td><td><code>http://erp-token.local/health/live</code>, <code>/health/ready</code></td><td>Liveness / Readiness（非 Ingress 限制；亦可直連 Pod port-forward）</td></tr>\n<tr><td>pgadmin</td><td><code>http://erp-token.local/pgadmin</code></td><td>資料庫瀏覽器（subpath 配置；登入見 §8）</td></tr>\n<tr><td>jaeger UI（選配）</td><td><code>http://erp-token.local/jaeger</code></td><td>分散式追蹤 UI（若部署）</td></tr>\n</table>\n<blockquote><p><strong>API 路由前綴：</strong> Razor Pages 占用 <code>/</code>、<code>/Account/<em></code>、<code>/ApiTokens/</em></code>；REST API 占用 <code>/api/tokens/<em></code>、<code>/api/poc/</em></code>；Health Probe 占用 <code>/health/*</code>。Ingress 規則順序由長到短，最長前綴優先匹配（traefik 預設行為）。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>Admin SPA：</strong> has_admin_backend=false，本專案不適用獨立 Admin Portal SPA（管理介面為同 process Razor Pages <code>/ApiTokens</code>，由 <code>[Authorize(Roles=&quot;ITAdmin&quot;)]</code> 控制）。</p></blockquote>\n<h3 id=\"kubectl-port-forward-存取直連-pod繞過-ingress\">kubectl port-forward 存取（直連 Pod，繞過 Ingress）</h3>\n<table>\n<tr><th>服務</th><th>指令</th><th>本地 URL</th></tr>\n<tr><td>api-server</td><td><code>kubectl port-forward -n erp-token-local deploy/erp-api-token-manager 8080:8080</code></td><td><code>http://localhost:8080</code></td></tr>\n<tr><td>postgres</td><td><code>kubectl port-forward -n erp-token-local statefulset/postgres 5432:5432</code></td><td><code>localhost:5432</code></td></tr>\n<tr><td>pgadmin</td><td><code>kubectl port-forward -n erp-token-local deploy/pgadmin 5050:80</code></td><td><code>http://localhost:5050</code></td></tr>\n<tr><td>jaeger UI（選配）</td><td><code>kubectl port-forward -n erp-token-local deploy/jaeger-aio 16686:16686</code></td><td><code>http://localhost:16686</code></td></tr>\n</table>\n<blockquote><p><strong>Make shortcuts：</strong> <code>make pf-api</code>、<code>make pf-db</code>、<code>make pf-pgadmin</code>、<code>make pf-jaeger</code> 會在背景啟動 port-forward 並記錄 PID。<code>make pf-stop</code> 停止全部。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>Admin SPA Ingress：</strong> has_admin_backend=false，無需 <code>/admin</code> 路徑路由設定。所有後台功能透過 Razor Pages <code>/ApiTokens/*</code>（Cookie 驗證 + RBAC <code>Roles=&quot;ITAdmin&quot;</code>）實作。如未來引入獨立 Admin SPA，需依 LOCAL_DEPLOY 模板 §5「Admin SPA Ingress 設定」補充 nginx SPA fallback 與 <code>base: &#x27;/admin/&#x27;</code> 打包設定。</p></blockquote>\n<hr>\n<h2 id=\"6-development-commands\">6. Development Commands</h2>\n<p>所有 <code>make</code> 指令從 repo root 執行。Makefile target 命名與 CI（<code>make ci-*</code>）對齊（見 CICD.md §8）。</p>\n<blockquote><p><strong>註：</strong> 本表新增 target 時須同步更新 <a href=\"./DEVELOPER_GUIDE.md\" target=\"_blank\" rel=\"noopener\">DEVELOPER_GUIDE §3.1 Quick Reference</a> 與 <a href=\"./CICD.md\" target=\"_blank\" rel=\"noopener\">CICD §4.1 標準 Make Target 清單</a>（三處 SoT 共識）。</p></blockquote>\n<h3 id=\"image--deployment\">Image &amp; Deployment</h3>\n<table>\n<tr><th>指令</th><th>等同底層命令</th><th>使用時機</th></tr>\n<tr><td><code>make image-build-api</code></td><td><code>nerdctl --namespace=k8s.io build -t erp-token/api:local -f docker/api/Dockerfile .</code></td><td>修改後端程式碼 / Razor 視圖 / Tailwind input.css 後</td></tr>\n<tr><td><code>make image-build-all</code></td><td><code>make image-build-api</code>（本專案僅一個 image，無獨立前端 image）</td><td>初次設定或全面更新</td></tr>\n<tr><td><code>make k8s-apply</code></td><td><code>kubectl apply -k k8s/overlays/local/</code></td><td>變更 K8s manifest 後</td></tr>\n<tr><td><code>make k8s-restart-api</code></td><td><code>kubectl rollout restart deploy/erp-api-token-manager -n erp-token-local</code></td><td>Build 新 image 後套用</td></tr>\n<tr><td><code>make k8s-restart-all</code></td><td><code>kubectl rollout restart deployment --all -n erp-token-local</code></td><td>更新 ConfigMap / Secret 後</td></tr>\n<tr><td><code>make k8s-delete</code></td><td><code>kubectl delete -k k8s/overlays/local/</code></td><td>重新部署時（保留 PVC）</td></tr>\n<tr><td><code>make k8s-clean</code></td><td><code>kubectl delete namespace erp-token-local</code></td><td>完全重置（銷毀所有資料）</td></tr>\n<tr><td><code>make k8s-init</code></td><td><code>bash scripts/bootstrap-secrets.sh</code></td><td>首次 setup 或 secrets-rotate</td></tr>\n<tr><td><code>make secrets-rotate</code></td><td>同 <code>make k8s-init</code> 但會強制刪除舊 Secret</td><td>安全事件後輪換</td></tr>\n</table>\n<h3 id=\"觀察--偵錯\">觀察 &amp; 偵錯</h3>\n<table>\n<tr><th>指令</th><th>等同底層命令</th><th>說明</th></tr>\n<tr><td><code>make pods</code></td><td><code>kubectl get pods -n erp-token-local</code></td><td>Pod 列表</td></tr>\n<tr><td><code>make logs-api</code></td><td><code>kubectl logs -f deploy/erp-api-token-manager -n erp-token-local</code></td><td>Tail api-server logs</td></tr>\n<tr><td><code>make logs-db</code></td><td><code>kubectl logs -f statefulset/postgres -n erp-token-local</code></td><td>Tail postgres logs</td></tr>\n<tr><td><code>make shell-api</code></td><td><code>kubectl exec -it deploy/erp-api-token-manager -n erp-token-local -- bash</code></td><td>Shell 進入 api-server pod</td></tr>\n<tr><td><code>make shell-db</code></td><td><code>kubectl exec -it -n erp-token-local statefulset/postgres -- psql -U erpapp -d erpapikey</code></td><td>進入 psql shell</td></tr>\n<tr><td><code>make health-check</code></td><td>依序執行 §4.7 個別驗證 curl 命令</td><td>全服務健康檢查</td></tr>\n<tr><td><code>make k9s</code></td><td><code>k9s -n erp-token-local</code>（需已安裝 k9s）</td><td>互動式 K8s UI</td></tr>\n<tr><td><code>make describe-api</code></td><td><code>kubectl describe deploy/erp-api-token-manager -n erp-token-local</code></td><td>描述 deployment（事件 / probe 設定）</td></tr>\n</table>\n<h3 id=\"測試\">測試</h3>\n<table>\n<tr><th>指令</th><th>等同底層命令</th><th>說明</th></tr>\n<tr><td><code>make test-unit</code></td><td><code>dotnet test src/Application.Tests/Application.Tests.csproj --logger &quot;console;verbosity=normal&quot;</code></td><td>在本機執行 unit test（不需 K8s）</td></tr>\n<tr><td><code>make test-integration</code></td><td><code>dotnet test src/Infrastructure.IntegrationTests/Infrastructure.IntegrationTests.csproj --logger &quot;console;verbosity=normal&quot;</code></td><td>Testcontainers 起 PostgreSQL container（需 nerdctl 可達）</td></tr>\n<tr><td><code>make test-e2e</code></td><td><code>dotnet test src/E2ETests/E2ETests.csproj</code> 並使 base URL 指向 <code>http://erp-token.local</code>（環境變數 <code>E2E_BASE_URL</code>）</td><td>Playwright/Selenium E2E（需 K8s 環境已部署）</td></tr>\n<tr><td><code>make test-bdd-server</code></td><td><code>dotnet test src/Bdd.Server.Tests/</code> 對應 <code>features/*.feature</code>（SpecFlow / Reqnroll）</td><td>Server BDD scenarios</td></tr>\n<tr><td><code>make test-bdd-client</code></td><td><code>dotnet test src/Bdd.Client.Tests/</code> 對應 <code>features/client/*.feature</code>（Razor + Alpine 互動驗證）</td><td>Client BDD scenarios</td></tr>\n<tr><td><code>make lint</code></td><td><code>dotnet format --verify-no-changes &amp;&amp; dotnet build /p:TreatWarningsAsErrors=true</code></td><td>C# 格式化 + Warning-as-Error</td></tr>\n<tr><td><code>make secscan</code></td><td><code>dotnet list package --vulnerable --include-transitive</code>（NuGet CVE）</td><td>漏洞掃描</td></tr>\n</table>\n<h3 id=\"database\">Database</h3>\n<table>\n<tr><th>指令</th><th>等同底層命令</th></tr>\n<tr><td><code>make db-migrate</code></td><td><code>kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet ef database update --no-build</code></td></tr>\n<tr><td><code>make db-status</code></td><td><code>kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet ef migrations list --no-build</code></td></tr>\n<tr><td><code>make db-rollback</code></td><td><code>kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet ef database update &lt;PreviousMigrationName&gt; --no-build</code></td></tr>\n<tr><td><code>make db-seed</code></td><td><code>kubectl exec -n erp-token-local deploy/erp-api-token-manager -- dotnet run --project src/Infrastructure -- seed --env Local</code></td></tr>\n<tr><td><code>make db-reset</code></td><td><code>make k8s-delete &amp;&amp; kubectl delete pvc -l app=postgres -n erp-token-local &amp;&amp; make k8s-apply &amp;&amp; make db-migrate &amp;&amp; make db-seed</code></td></tr>\n<tr><td><code>make db-backup</code></td><td><code>kubectl exec -n erp-token-local statefulset/postgres -- pg_dump -U erpapp erpapikey &gt; backups/local-$(date +%Y%m%d-%H%M%S).sql</code></td></tr>\n</table>\n<h3 id=\"port-forward\">Port Forward</h3>\n<table>\n<tr><th>指令</th><th>等同底層命令</th></tr>\n<tr><td><code>make pf-api</code></td><td><code>kubectl port-forward -n erp-token-local deploy/erp-api-token-manager 8080:8080 &amp;</code></td></tr>\n<tr><td><code>make pf-db</code></td><td><code>kubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &amp;</code></td></tr>\n<tr><td><code>make pf-pgadmin</code></td><td><code>kubectl port-forward -n erp-token-local deploy/pgadmin 5050:80 &amp;</code></td></tr>\n<tr><td><code>make pf-jaeger</code></td><td><code>kubectl port-forward -n erp-token-local deploy/jaeger-aio 16686:16686 &amp;</code></td></tr>\n<tr><td><code>make pf-all</code></td><td>依序啟動上述全部 port-forward</td></tr>\n<tr><td><code>make pf-stop</code></td><td><code>pkill -f &#x27;kubectl port-forward.*erp-token-local&#x27;</code></td></tr>\n</table>\n<hr>\n<h2 id=\"7-database-operations\">7. Database Operations</h2>\n<h3 id=\"進入-psql-shell\">進入 psql Shell</h3>\n<blockquote><p><strong>WARNING: 下方連線字串中的密碼 <code>secret</code> 僅為本機開發預設值（實際密碼由 <code>bootstrap-secrets.sh</code> 隨機生成），禁止複製至 staging / production 環境。</strong> 正式環境請從 GCP Secret Manager 取得憑證。</p></blockquote>\n<pre><code class=\"language-bash\"># 方法 1：透過 kubectl exec（不需 port-forward）\nkubectl exec -it -n erp-token-local statefulset/postgres -- \\\n  psql -U erpapp -d erpapikey\n\n# 方法 2：port-forward 後用本地 psql client\nkubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &amp;\nPG_PASSWORD=$(kubectl get secret erp-token-db-secret -n erp-token-local \\\n  -o jsonpath=&#x27;{.data.POSTGRES_PASSWORD}&#x27; | base64 -d)\n# WARNING: 密碼來自隨機生成的本機 Secret；禁止在 staging / production 使用此模式。\nPGPASSWORD=&quot;${PG_PASSWORD}&quot; psql &quot;postgres://erpapp@localhost:5432/erpapikey&quot;\n\n# Make shortcut\nmake shell-db</code></pre>\n<h3 id=\"執行-ef-core-migration\">執行 EF Core Migration</h3>\n<pre><code class=\"language-bash\"># 執行所有 pending migration\nmake db-migrate\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef database update --no-build\n\n# 查看 migration 狀態\nmake db-status\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef migrations list --no-build\n\n# Rollback 到前一個 migration\nmake db-rollback\n# 等同（指定上一個 migration name）：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet ef database update 20260315_AddTokenExpiresAt --no-build</code></pre>\n<h3 id=\"新增一個-migration開發者本機\">新增一個 Migration（開發者本機）</h3>\n<pre><code class=\"language-bash\"># 在本機（非 Pod 內）執行 dotnet ef migrations add\ndotnet ef migrations add AddTokenSomeColumn \\\n  --project src/Infrastructure \\\n  --startup-project src/ErpApplication\n\n# Review 生成的 Up/Down 後 apply\nmake image-build-all &amp;&amp; make k8s-restart-api &amp;&amp; make db-migrate</code></pre>\n<h3 id=\"seed-測試資料\">Seed 測試資料</h3>\n<pre><code class=\"language-bash\"># 載入所有 seed（idempotent，可重複執行）\nmake db-seed\n# 等同：\n# kubectl exec -n erp-token-local deploy/erp-api-token-manager -- \\\n#   dotnet run --project src/Infrastructure -- seed --env Local</code></pre>\n<blockquote><p>Seed 內容對齊 §8 Test Data：3 個角色（ITAdmin / BusinessUser / Auditor 唯讀），各角色 1–2 筆 api_tokens，覆蓋 active / revoked / expired 三種狀態及 token_audit_logs。</p></blockquote>\n<h3 id=\"重置為乾淨狀態\">重置為乾淨狀態</h3>\n<pre><code class=\"language-bash\"># Drop + recreate + migrate + seed（銷毀所有本地資料）\nmake db-reset\n# Warning: PVC 內資料全部清除</code></pre>\n<h3 id=\"backup--restore\">Backup / Restore</h3>\n<blockquote><p><strong>WARNING: 下方連線字串中的密碼僅為本機開發隨機值，禁止複製至 staging / production 環境。</strong></p></blockquote>\n<pre><code class=\"language-bash\"># Backup（在 pod 內執行；不需 port-forward）\nmake db-backup\n# 等同：\n# kubectl exec -n erp-token-local statefulset/postgres -- \\\n#   pg_dump -U erpapp erpapikey &gt; backups/local-$(date +%Y%m%d-%H%M%S).sql\n\n# Restore（port-forward 後執行）\nkubectl port-forward -n erp-token-local statefulset/postgres 5432:5432 &amp;\nPG_PASSWORD=$(kubectl get secret erp-token-db-secret -n erp-token-local \\\n  -o jsonpath=&#x27;{.data.POSTGRES_PASSWORD}&#x27; | base64 -d)\n# WARNING: 密碼僅為本機隨機值，禁止 staging / production 使用此 port-forward 流程。\nPGPASSWORD=&quot;${PG_PASSWORD}&quot; psql &quot;postgres://erpapp@localhost:5432/erpapikey&quot; \\\n  &lt; backups/local-20260508-093000.sql</code></pre>\n<hr>\n<h2 id=\"8-test-data--fixtures\">8. Test Data &amp; Fixtures</h2>\n<p><code>make db-seed</code> 後以下帳號與資料固定可用。角色清單對齊 PRD §3 / RBAC Matrix（EDD §6.10），密碼策略遵守 PRD §6.2。</p>\n<h3 id=\"default-user-accounts\">Default User Accounts</h3>\n<table>\n<tr><th>Role</th><th>Email</th><th>Password</th><th>說明</th></tr>\n<tr><td><strong>IT Admin</strong>（全權限）</td><td><code>admin@erp-token.local</code></td><td><code>Password1!</code></td><td><code>erp_role = ITAdmin</code>；可看全量 Token + 撤銷他人 Token + 看 Audit Log（§7 §11.1）</td></tr>\n<tr><td><strong>Business User</strong>（標準）</td><td><code>user@erp-token.local</code></td><td><code>Password1!</code></td><td><code>erp_role = BusinessUser</code>；可建立 / 看自己的 Token；無 Audit Log 權限</td></tr>\n<tr><td><strong>Auditor</strong>（唯讀）</td><td><code>auditor@erp-token.local</code></td><td><code>Password1!</code></td><td><code>erp_role = Auditor</code>；只讀 Audit Log，不可建立 / 撤銷 Token</td></tr>\n<tr><td><strong>N8N Service Account</strong>（自動化）</td><td><code>n8n-svc@erp-token.local</code></td><td><code>Password1!</code></td><td><code>erp_role = BusinessUser</code>；用於模擬 N8N 整合測試（US-N8N-005）</td></tr>\n</table>\n<blockquote><p><strong>密碼複雜度</strong>：<code>Password1!</code> 滿足 8+ 字元 + 大寫 + 小寫 + 數字 + 特殊字元（PRD §6.2 強度要求）；僅本機開發用，staging 起需 SSO。</p></blockquote>\n<h3 id=\"seed-it-adminis_superuser-升級流程prd-59--oq-prd-4-resolved\">Seed IT Admin（<code>is_superuser</code> 升級流程，PRD §5.9 / OQ-PRD-4 RESOLVED）</h3>\n<blockquote><p>本系統無獨立 Admin 後台（PRD §4.3 Out of Scope），IT Admin 權限以 <code>users.is_superuser=true</code> 旗標升級。</p></blockquote>\n<blockquote><p>MVP 階段升級流程為 <strong>DBA 手動執行 SQL</strong>（OQ-PRD-4 已決議），完整 SOP：</p></blockquote>\n<pre><code class=\"language-bash\"># 1. 進入 postgres（kubectl exec 或 pgadmin）\nkubectl exec -it -n erp-token-local deploy/postgres -- \\\n  psql -U erpapp -d erpapikey</code></pre>\n<pre><code class=\"language-sql\">-- 2. 將指定 ERP 使用者升級為 IT Admin\nUPDATE users\n   SET is_superuser = TRUE,\n       updated_at   = NOW()\n WHERE email = &#x27;admin@erp-token.local&#x27;\nRETURNING id, email, is_superuser;\n-- 預期回傳一筆，is_superuser=t\n\n-- 3. 驗證升級結果（在資料庫層）\nSELECT id, email, is_superuser FROM users WHERE is_superuser = TRUE;\n-- MVP 期望結果：恰好 1 列（admin@erp-token.local）\n\n-- 4. 寫入升級事件至 audit log（合規必要，MVP 由 DBA 手動寫入）\nINSERT INTO token_audit_logs (event_id, action, actor_user_id, target_user_id, ip_address, created_at)\nSELECT gen_random_uuid(), &#x27;admin_grant&#x27;, NULL, u.id, &#x27;127.0.0.1&#x27;, NOW()\n  FROM users u\n WHERE u.email = &#x27;admin@erp-token.local&#x27;;</code></pre>\n<p><code>make db-seed</code> 預設會自動執行上述 SQL（已包含於 <code>scripts/seed-data.sql</code> 之 <code>seed-superuser.sql</code> 區塊）。</p>\n<p>若需要為其他既存 ERP 使用者升級，請以 SOP 步驟 2 替換 email 後重複執行。</p>\n<blockquote><p><strong>Pilot/Prod 階段</strong>：請依 PRD §12 OQ-PRD-4 RESOLVED 註解執行 <code>kubectl exec ... psql</code> 並開立變更工單；嚴禁繞過 audit log。</p></blockquote>\n<blockquote><p><strong>降級流程</strong>：將 <code>is_superuser</code> 設回 FALSE 並寫入 <code>admin_revoke</code> audit event；操作同樣由 DBA 執行。</p></blockquote>\n<h3 id=\"sample-data從-schemamd-3-業務資料表提取\">Sample Data（從 SCHEMA.md §3 業務資料表提取）</h3>\n<table>\n<tr><th>Entity</th><th>Count</th><th>說明</th></tr>\n<tr><td>Users</td><td>4</td><td>涵蓋四種角色，密碼同上</td></tr>\n<tr><td>api_tokens</td><td>8</td><td>涵蓋所有狀態：4 active（admin / user / auditor / n8n-svc 各 1）、2 revoked（手動撤銷與自動撤銷）、2 expired（已過期未清理）</td></tr>\n<tr><td>token_audit_logs</td><td>16</td><td>對應 8 個 token 的 create + 部分 revoke / expire 事件，event_type 涵蓋 <code>create</code> / <code>revoke</code> / <code>expire</code> / <code>last_used</code></td></tr>\n<tr><td>api_tokens (edge case)</td><td>1</td><td>description 含最大欄位長度（255 字元）+ Unicode CJK + 特殊字元（如 <code>「」、—、…</code>），用於 §10 邊界情境驗證</td></tr>\n</table>\n<h3 id=\"pgadmin-登入\">pgadmin 登入</h3>\n<ul><li>URL：<code>http://erp-token.local/pgadmin</code>（透過 Ingress）或 port-forward <code>http://localhost:5050</code></li><li>Email：<code>admin@erp-token.local</code></li><li>Password：<code>scripts/bootstrap-secrets.sh</code> 隨機生成；查詢方式：</li></ul>\n<pre><code class=\"language-bash\">  kubectl get secret erp-token-pgadmin-secret -n erp-token-local \\\n    -o jsonpath=&#x27;{.data.PGADMIN_DEFAULT_PASSWORD}&#x27; | base64 -d; echo</code></pre>\n<blockquote><p><strong>WARNING：本機開發用，禁止用於其他環境</strong></p></blockquote>\n<ul><li>新增 Server：Host = <code>postgres</code>（K8s service 名稱），Port = <code>5432</code>，DB = <code>erpapikey</code>，User = <code>erpapp</code>，Password = 從 <code>kubectl get secret erp-token-db-secret -n erp-token-local -o jsonpath=&#x27;{.data.POSTGRES_PASSWORD}&#x27; | base64 -d</code> 取得</li></ul>\n<h3 id=\"api-測試樣本curl依-apimd-5\">API 測試樣本（curl，依 API.md §5）</h3>\n<pre><code class=\"language-bash\"># 1. 登入取得 Cookie + AntiForgery Token（瀏覽器登入後 export cookie）\ncurl -c /tmp/cookies.txt http://erp-token.local/Account/Login \\\n  -d &quot;email=admin@erp-token.local&amp;password=Password1!&amp;__RequestVerificationToken=&lt;token&gt;&quot;\n\n# 2. 列出當前使用者的 Token\ncurl -s -b /tmp/cookies.txt http://erp-token.local/api/tokens | jq .\n# Expected: HTTP 200, {&quot;success&quot;:true,&quot;data&quot;:[...]}\n\n# 3. 建立 Token（attach AntiForgery header）\ncurl -s -X POST -b /tmp/cookies.txt \\\n  -H &quot;X-XSRF-TOKEN: &lt;antiforgery&gt;&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d &#x27;{&quot;description&quot;:&quot;Local dev verification token&quot;}&#x27; \\\n  http://erp-token.local/api/tokens | jq .\n# Expected: HTTP 201, response 含 rawToken（一次性明文）\n\n# 4. 用 Bearer Token 呼叫 POC\ncurl -s -H &quot;Authorization: Bearer &lt;raw_token&gt;&quot; \\\n  http://erp-token.local/api/poc/sales | jq .\n# Expected: HTTP 200, simulated sales data</code></pre>\n<hr>\n<h2 id=\"9-configmap--secret-reference\">9. ConfigMap &amp; Secret Reference</h2>\n<h3 id=\"configmap--erp-token-api-config\">ConfigMap — <code>erp-token-api-config</code></h3>\n<table>\n<tr><th>Key</th><th>Default</th><th>說明</th></tr>\n<tr><td><code>ASPNETCORE_ENVIRONMENT</code></td><td><code>Development</code></td><td>Runtime 環境（影響 appsettings.{env}.json 讀取）</td></tr>\n<tr><td><code>ASPNETCORE_URLS</code></td><td><code>http://+:8080</code></td><td>API 監聽 URL</td></tr>\n<tr><td><code>Logging__LogLevel__Default</code></td><td><code>Debug</code></td><td>日誌等級（debug 顯示 EF Core SQL）</td></tr>\n<tr><td><code>Logging__LogLevel__Microsoft.EntityFrameworkCore</code></td><td><code>Warning</code></td><td>抑制 EF SQL command 噪音</td></tr>\n<tr><td><code>FeatureFlags__enable_api_token_management</code></td><td><code>true</code></td><td>主功能開關（PRD §5.1 / EDD §16.2.2）</td></tr>\n<tr><td><code>FeatureFlags__enable_token_audit_log</code></td><td><code>true</code></td><td>Audit Log 寫入開關</td></tr>\n<tr><td><code>FeatureFlags__enable_poc_demo_apis</code></td><td><code>true</code></td><td><code>/api/poc/*</code> 端點開關（local / dev / staging：true；prod：false，PRD §5.6）</td></tr>\n<tr><td><code>OpenTelemetry__Endpoint</code></td><td><code>http://jaeger-aio:4317</code>（選配；若未部署 jaeger 則留空）</td><td>OTLP gRPC endpoint</td></tr>\n<tr><td><code>OpenTelemetry__ServiceName</code></td><td><code>erp-api-token-manager</code></td><td>Trace service name</td></tr>\n<tr><td><code>Cors__AllowedOrigins</code></td><td><code>http://erp-token.local</code></td><td>允許的 CORS origins</td></tr>\n<tr><td><code>RateLimiting__TokenCreate__WindowSeconds</code></td><td><code>60</code></td><td>Token 建立速率限制窗口（CONSTANTS.md §6）</td></tr>\n<tr><td><code>RateLimiting__TokenCreate__MaxRequests</code></td><td><code>60</code></td><td>Token 建立速率限制上限</td></tr>\n</table>\n<blockquote><p><strong>K8s internal DNS 連線字串：</strong> <code>ConnectionStrings__ApiTokenDb</code> 不放在 ConfigMap，改放在 Secret <code>erp-token-api-secret</code>。值的格式：<code>Host=postgres;Port=5432;Database=erpapikey;Username=erpapp;Password=&lt;random&gt;</code>。<strong>禁止</strong>使用 <code>localhost</code> 或 <code>127.0.0.1</code>，必須使用 K8s service 名稱 <code>postgres</code>，否則 pod 內無法連線。</p></blockquote>\n<h3 id=\"configmap--erp-token-db-configpostgres-init\">ConfigMap — <code>erp-token-db-config</code>（postgres init）</h3>\n<table>\n<tr><th>Key</th><th>Default</th><th>說明</th></tr>\n<tr><td><code>POSTGRES_INITDB_ARGS</code></td><td><code>--encoding=UTF8 --locale=C</code></td><td>initdb 參數，符合 SCHEMA.md §2 預設</td></tr>\n<tr><td><code>init.sql</code></td><td>（見 <code>docker/postgres/init.sql</code>）</td><td>建立 <code>public</code> schema GRANT 與 <code>erp_token</code> schema（per SCHEMA.md §1.1）</td></tr>\n</table>\n<h3 id=\"secret--erp-token-api-secret從-bootstrap-secretssh-建立\">Secret — <code>erp-token-api-secret</code>（從 bootstrap-secrets.sh 建立）</h3>\n<table>\n<tr><th>Key</th><th>說明</th><th>設定方式</th></tr>\n<tr><td><code>ConnectionStrings__ApiTokenDb</code></td><td>PostgreSQL 連線字串（Host=postgres K8s DNS）</td><td><code>bootstrap-secrets.sh</code>（隨機 DB password）</td></tr>\n<tr><td><code>HmacSecret</code></td><td>v2 HMAC-SHA256 Token Hash 用（MVP 預留，EDD §17.2）</td><td><code>openssl rand -hex 64</code></td></tr>\n<tr><td><code>DataProtection__Key</code></td><td>ASP.NET Core DataProtection Key Ring（Cookie / AntiForgery 簽章）</td><td><code>openssl rand -hex 32</code>；多副本必須共享 key</td></tr>\n</table>\n<h3 id=\"secret--erp-token-db-secretpostgres-bootstrap\">Secret — <code>erp-token-db-secret</code>（postgres bootstrap）</h3>\n<table>\n<tr><th>Key</th><th>說明</th></tr>\n<tr><td><code>POSTGRES_DB</code></td><td>DB 名稱（<code>erpapikey</code>）</td></tr>\n<tr><td><code>POSTGRES_USER</code></td><td>DB user（<code>erpapp</code>）</td></tr>\n<tr><td><code>POSTGRES_PASSWORD</code></td><td>DB password（隨機）</td></tr>\n</table>\n<h3 id=\"secret--erp-token-pgadmin-secret\">Secret — <code>erp-token-pgadmin-secret</code></h3>\n<table>\n<tr><th>Key</th><th>說明</th></tr>\n<tr><td><code>PGADMIN_DEFAULT_EMAIL</code></td><td>pgadmin 登入 email（<code>admin@erp-token.local</code>）</td></tr>\n<tr><td><code>PGADMIN_DEFAULT_PASSWORD</code></td><td>pgadmin 登入密碼（隨機；本機使用，勿用於其他環境）</td></tr>\n</table>\n<h3 id=\"查看目前-configmap--secret\">查看目前 ConfigMap / Secret</h3>\n<pre><code class=\"language-bash\"># 查看 ConfigMap 內容\nkubectl get configmap erp-token-api-config -n erp-token-local -o yaml\n\n# 查看 Secret key 清單（值會 base64 遮罩）\nkubectl get secret erp-token-api-secret -n erp-token-local -o yaml\n\n# 解碼特定 secret 值\nkubectl get secret erp-token-api-secret -n erp-token-local \\\n  -o jsonpath=&#x27;{.data.HmacSecret}&#x27; | base64 -d; echo</code></pre>\n<h3 id=\"更新-configmap--secret-並套用\">更新 ConfigMap / Secret 並套用</h3>\n<pre><code class=\"language-bash\"># 修改 ConfigMap\nkubectl edit configmap erp-token-api-config -n erp-token-local\n# 或直接重新 apply：\nkubectl apply -k k8s/overlays/local/\n\n# 更新後需 rolling restart 才生效\nmake k8s-restart-all</code></pre>\n<hr>\n<h2 id=\"10-common-issues--fixes\">10. Common Issues &amp; Fixes</h2>\n<table>\n<tr><th>Issue</th><th>Symptom</th><th>Root Cause</th><th>Fix</th></tr>\n<tr><td>Pod stuck in <code>Pending</code></td><td><code>kubectl get pods</code> 顯示 Pending</td><td>資源不足或 namespace 未建立</td><td><code>kubectl describe pod &lt;name&gt; -n erp-token-local</code> 查看 Events；增加 Rancher Desktop RAM 至 8 GB（Preferences → Virtual Machine）</td></tr>\n<tr><td>Pod in <code>CrashLoopBackOff</code></td><td>Pod 反覆重啟</td><td>應用程式啟動失敗（DB 未就緒 / appsettings 錯誤 / migration 未跑）</td><td><code>kubectl logs &lt;pod&gt; -n erp-token-local --previous</code> 查看上次崩潰日誌；80% 是 DB 連線失敗，先 <code>kubectl get pod postgres-0</code> 確認就緒</td></tr>\n<tr><td><code>ImagePullBackOff</code></td><td>Pod 無法找到 image</td><td>image 未 build 或載入錯 namespace</td><td>`nerdctl --namespace=k8s.io images \\</td><td>grep erp-token<code>；重新 </code>make image-build-all<code>；確認使用 </code>--namespace=k8s.io` flag</td></tr>\n<tr><td>Ingress 無法解析</td><td><code>curl: Could not resolve host</code></td><td>/etc/hosts 未設定</td><td>確認 <code>/etc/hosts</code> 含 <code>127.0.0.1 erp-token.local</code>；macOS 執行 <code>sudo dscacheutil -flushcache</code></td></tr>\n<tr><td>DB 連線拒絕</td><td>api-server log: <code>Npgsql.NpgsqlException: ... ECONNREFUSED postgres:5432</code></td><td>postgres pod 未就緒</td><td><code>kubectl wait --for=condition=Ready pod/postgres-0 -n erp-token-local --timeout=60s</code></td></tr>\n<tr><td>EF Core Migration 失敗 <code>relation already exists</code></td><td><code>make db-migrate</code> 報錯</td><td>Migration 被部分執行過</td><td><code>make db-reset</code>（銷毀 PVC 重建）；或進 psql 手動執行 <code>DROP TABLE __EFMigrationsHistory CASCADE;</code> 後重跑</td></tr>\n<tr><td>EF Core Migration 失敗 <code>permission denied for schema public</code></td><td>部署後第一次 migration 失敗</td><td><code>erpapp</code> user 無 <code>CREATE</code> privilege</td><td><code>kubectl exec -n erp-token-local statefulset/postgres -- psql -U postgres -d erpapikey -c &quot;GRANT ALL ON SCHEMA public TO erpapp;&quot;</code></td></tr>\n<tr><td>Secret 找不到</td><td>Pod 事件：<code>secret &quot;erp-token-api-secret&quot; not found</code></td><td><code>make k8s-init</code> 未執行</td><td>重新執行 <code>make k8s-init</code>；確認 <code>kubectl get secret -n erp-token-local</code> 列出 3 個 secret</td></tr>\n<tr><td><code>Anti-forgery token validation failed</code>（HTTP 400 on form submit）</td><td>UI 操作報錯</td><td>DataProtection Key 在多副本間不一致；Cookie 過期</td><td>確認 <code>DataProtection__Key</code> 在 <code>erp-token-api-secret</code> 一致；<code>make k8s-restart-api</code>；瀏覽器 logout 重新 login</td></tr>\n<tr><td>port-forward 斷線</td><td><code>port-forward</code> 背景 process 結束</td><td>網路超時或 Pod 重啟</td><td><code>make pf-api</code>（Make 重新啟動）；或在另一個 terminal 重新執行</td></tr>\n<tr><td>Rancher Desktop K8s 未啟動</td><td><code>kubectl: connection refused</code></td><td>k3s 服務未運行</td><td>開啟 Rancher Desktop GUI，進入 <strong>Kubernetes</strong> 頁面確認已啟用並等待 Ready</td></tr>\n<tr><td>nerdctl build 失敗（network）</td><td>Build 時 NuGet restore 連線失敗</td><td>containerd 沙箱 DNS 問題</td><td>嘗試 <code>nerdctl build --network=host ...</code>；或重啟 Rancher Desktop</td></tr>\n<tr><td>舊 ConfigMap 未更新</td><td>修改 configmap 後行為未變</td><td>Pod 未重啟，仍用舊版設定</td><td><code>make k8s-restart-all</code></td></tr>\n<tr><td><code>/api/poc/*</code> 回 404</td><td>POC API 全部 404</td><td><code>FeatureFlags__enable_poc_demo_apis=false</code> 或 ConfigMap 未生效</td><td>確認 ConfigMap key；<code>make k8s-restart-api</code> 重啟；確認 POC controller 已註冊</td></tr>\n<tr><td><code>/ApiTokens/AuditLog</code> 回 403</td><td>UI 點 Audit Log 報錯</td><td>登入帳號 <code>erp_role != ITAdmin</code></td><td>用 <code>admin@erp-token.local</code> 登入（§8）</td></tr>\n<tr><td>Tailwind CSS 樣式遺失（裸 HTML 顯示）</td><td>頁面看起來像 1996 風格</td><td>CDN 載入失敗或 production CSS bundle 未掛入</td><td>確認瀏覽器 DevTools Console 無 CDN 錯誤；若使用 CLI mode，確認 Dockerfile stage 2 已 build <code>wwwroot/css/site.css</code></td></tr>\n</table>\n<hr>\n<h2 id=\"11-logs--debugging\">11. Logs &amp; Debugging</h2>\n<h3 id=\"查看-pod-日誌\">查看 Pod 日誌</h3>\n<pre><code class=\"language-bash\"># 即時 tail 指定服務\nkubectl logs -f deploy/erp-api-token-manager -n erp-token-local\nkubectl logs -f statefulset/postgres -n erp-token-local\n\n# Make shortcuts\nmake logs-api\nmake logs-db\n\n# 查看上次 crash 日誌\nkubectl logs deploy/erp-api-token-manager -n erp-token-local --previous\n\n# 多副本同時 tail（兩個 pod logs 合併）\nkubectl logs -f -l app=erp-api-token-manager -n erp-token-local --max-log-requests=2</code></pre>\n<blockquote><p><strong>本專案無獨立 worker / web-app Deployment</strong>（無 <code>make logs-worker</code> / <code>make logs-web</code>），因 EDD §3.3 MQ=None 且 FRONTEND.md §2 為 Razor Pages 嵌入式（與 api-server 同 process）。</p></blockquote>\n<h3 id=\"進入-pod-shell\">進入 Pod Shell</h3>\n<pre><code class=\"language-bash\"># api-server shell（dotnet 8 image，bash 可用）\nkubectl exec -it deploy/erp-api-token-manager -n erp-token-local -- bash\n# Make shortcut：\nmake shell-api\n\n# 查看環境變數（含 ConfigMap + Secret 注入的值）\nkubectl exec deploy/erp-api-token-manager -n erp-token-local -- env | sort\n\n# 在 pod 內執行一次性指令（Runtime 工具：dotnet 8）\nkubectl exec deploy/erp-api-token-manager -n erp-token-local -- dotnet --version\n# Expected: 8.0.x</code></pre>\n<h3 id=\"查看-pod-事件啟動失敗時最有用\">查看 Pod 事件（啟動失敗時最有用）</h3>\n<pre><code class=\"language-bash\">kubectl describe pod &lt;pod-name&gt; -n erp-token-local\n# 重點看 Events 區塊的 Warning 訊息</code></pre>\n<h3 id=\"常見-log-模式\">常見 Log 模式</h3>\n<table>\n<tr><th>Pattern</th><th>含義</th><th>處理</th></tr>\n<tr><td><code>Npgsql.NpgsqlException: ... database connection failed</code></td><td>postgres pod 未就緒或 DNS 解析失敗</td><td>確認 <code>kubectl get pod postgres-0 -n erp-token-local</code> Running；確認 service 名稱為 <code>postgres</code></td></tr>\n<tr><td><code>[INFO] Now listening on: http://[::]:8080</code></td><td>API 啟動成功</td><td>正常，無需處理</td></tr>\n<tr><td><code>[ERROR] Migration 20260315_AddTokenExpiresAt failed</code></td><td>EF Core Migration SQL 異常</td><td>查看完整錯誤；<code>make db-rollback</code> 或進 psql 手動修正 <code>__EFMigrationsHistory</code></td></tr>\n<tr><td><code>[WARN] Anti-forgery token validation failed</code></td><td>Form 提交 / API 缺 X-XSRF-TOKEN</td><td>確認 Razor <code>_Layout.cshtml</code> 注入 token；多副本確認 <code>DataProtection__Key</code> 共享</td></tr>\n<tr><td><code>[ERROR] Bearer Token validation failed: token revoked</code></td><td>API_Auth middleware 拒絕已撤銷 Token</td><td>預期行為（PRD §5.4 AC-3）</td></tr>\n<tr><td><code>[INFO] HTTP GET /health/ready responded 200 in 5ms</code></td><td>Readiness probe 正常</td><td>正常</td></tr>\n<tr><td><code>[ERROR] Background hosted service ... failed after 3 retries</code></td><td>（MVP 無 Worker；若未來引入 IHostedService 才會出現）</td><td>查看 hosted service 設定；確認外部依賴</td></tr>\n</table>\n<h3 id=\"使用-k9s推薦\">使用 k9s（推薦）</h3>\n<pre><code class=\"language-bash\"># 開啟 k9s，自動切換至 local namespace\nk9s -n erp-token-local\n# 在 k9s 內：\n#   :pod   — 查看所有 pod\n#   :log   — 查看 log\n#   s      — 進入 pod shell\n#   d      — describe resource\n#   Ctrl+K — 刪除 resource</code></pre>\n<h3 id=\"分散式追蹤選配-jaeger\">分散式追蹤（選配 Jaeger）</h3>\n<pre><code class=\"language-bash\"># 啟用 Jaeger（k8s/overlays/local/jaeger.yaml 預設停用，取消註解 kustomization patch 啟用）\nmake k8s-apply\n\n# Port forward Jaeger UI\nmake pf-jaeger\nopen http://localhost:16686\n\n# api-server ConfigMap 已配置 OTLP endpoint：\n# OpenTelemetry__Endpoint = http://jaeger-aio:4317\n# 重新 deploy 即會把 trace 送至 jaeger</code></pre>\n<hr>\n<h2 id=\"12-port-reference\">12. Port Reference</h2>\n<h3 id=\"ingress需-etchosts-設定-127001-erp-tokenlocal\">Ingress（需 /etc/hosts 設定 <code>127.0.0.1 erp-token.local</code>）</h3>\n<table>\n<tr><th>Path</th><th>服務</th><th>說明</th></tr>\n<tr><td><code>http://erp-token.local/</code></td><td>api-server (Razor Pages)</td><td>主 UI（首頁 / Login / <code>/ApiTokens</code> 列表）</td></tr>\n<tr><td><code>http://erp-token.local/Account/*</code></td><td>api-server (Razor Pages)</td><td>登入 / 登出</td></tr>\n<tr><td><code>http://erp-token.local/ApiTokens/*</code></td><td>api-server (Razor Pages)</td><td>Token 管理頁面</td></tr>\n<tr><td><code>http://erp-token.local/api/tokens</code></td><td>api-server (REST)</td><td>Token CRUD REST API</td></tr>\n<tr><td><code>http://erp-token.local/api/poc/{sales,inventory,orders/{id}/status,health}</code></td><td>api-server (REST)</td><td>POC demo endpoints</td></tr>\n<tr><td><code>http://erp-token.local/health/live</code></td><td>api-server (Health)</td><td>Liveness probe</td></tr>\n<tr><td><code>http://erp-token.local/health/ready</code></td><td>api-server (Health)</td><td>Readiness probe（含 DB 連線檢查）</td></tr>\n<tr><td><code>http://erp-token.local/pgadmin</code></td><td>pgadmin</td><td>DB 瀏覽器（subpath；Ingress rewrite）</td></tr>\n<tr><td><code>http://erp-token.local/jaeger</code>（選配）</td><td>jaeger-aio UI</td><td>分散式追蹤</td></tr>\n</table>\n<blockquote><p>本專案無獨立前端 SPA，故無 <code>web-app</code> 行；Razor Pages 與 REST API 共用同一 Pod 與 port 8080。Admin SPA：has_admin_backend=false，無 <code>/admin</code> 前綴路由。</p></blockquote>\n<h3 id=\"port-forward直連-clusterip-service\">port-forward（直連 ClusterIP Service）</h3>\n<table>\n<tr><th>服務</th><th>指令</th><th>本地 Port</th></tr>\n<tr><td>api-server</td><td><code>make pf-api</code> 等同 <code>kubectl port-forward -n erp-token-local deploy/erp-api-token-manager 8080:8080</code></td><td><code>8080</code></td></tr>\n<tr><td>postgres</td><td><code>make pf-db</code> 等同 <code>kubectl port-forward -n erp-token-local statefulset/postgres 5432:5432</code></td><td><code>5432</code></td></tr>\n<tr><td>pgadmin</td><td><code>make pf-pgadmin</code> 等同 <code>kubectl port-forward -n erp-token-local deploy/pgadmin 5050:80</code></td><td><code>5050</code></td></tr>\n<tr><td>jaeger UI（選配）</td><td><code>make pf-jaeger</code> 等同 <code>kubectl port-forward -n erp-token-local deploy/jaeger-aio 16686:16686</code></td><td><code>16686</code></td></tr>\n<tr><td>jaeger OTLP（選配）</td><td><code>kubectl port-forward -n erp-token-local deploy/jaeger-aio 4317:4317</code></td><td><code>4317</code></td></tr>\n</table>\n<blockquote><p>全部 port-forward：<code>make pf-all</code>。停止：<code>make pf-stop</code>。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>Port 一致性驗證</strong>：上表 <code>8080</code> / <code>5432</code> 與 §2 Architecture Mermaid 圖節點標注、RUNBOOK §2.3 Network and Port Reference、EDD §16.1 Deployment containerPort 完全一致（皆 8080 / 5432）。</p></blockquote>\n<hr>\n<h2 id=\"13-local-https-設定\">13. Local HTTPS 設定</h2>\n<p>部分功能需要 HTTPS（OAuth 2.0 回調、<code>SameSite=Secure</code> Cookie、Service Worker）。本專案 MVP 階段預設使用 HTTP；若需驗證 SAML SSO 或 OAuth callback，啟用以下流程。</p>\n<blockquote><p>若無 OAuth 回調 / SameSite Secure Cookie 需求，可跳過此節（POC MVP 階段以 HTTP 為主）。</p></blockquote>\n<pre><code class=\"language-bash\"># 1. 建立本地 CA（只需執行一次）\nmkcert -install\n\n# 2. 為本地 domain 生成憑證\nmkcert &quot;erp-token.local&quot; &quot;*.erp-token.local&quot;\n# 生成：erp-token.local+1.pem  和  erp-token.local+1-key.pem\n\n# 3. 建立 K8s TLS Secret\nkubectl create secret tls erp-token-local-tls \\\n  --cert=erp-token.local+1.pem \\\n  --key=erp-token.local+1-key.pem \\\n  -n erp-token-local \\\n  --dry-run=client -o yaml | kubectl apply -f -\n\n# 4. 啟用 HTTPS Ingress（已在 k8s/overlays/local/ 提供，預設 disabled）\n# 編輯 k8s/overlays/local/kustomization.yaml，啟用 tls-ingress patch：\n# patches:\n#   - path: patches/ingress-tls.yaml   # 取消此行註解\n\nkubectl apply -k k8s/overlays/local/</code></pre>\n<p>OAuth 本地回調設定（若整合 ERP SSO）：</p>\n<pre><code class=\"\">https://erp-token.local/Account/SsoCallback</code></pre>\n<p><code>.gitignore</code> 必須加入憑證私鑰（避免誤提交）：</p>\n<pre><code class=\"language-gitignore\">erp-token.local-key.pem\nerp-token.local+*-key.pem</code></pre>\n<hr>\n<h2 id=\"14-mock-services--external-integration-stubs\">14. Mock Services &amp; External Integration Stubs</h2>\n<p>依 ARCH §3.1 系統元件圖與 EDD §1.5 Scope，本模組 MVP 不依賴任何外部第三方服務（無 email、storage、payment、SMS、OAuth provider）。本地僅需以下 mock：</p>\n<table>\n<tr><th>服務</th><th>本地替代方案</th><th>K8s Service DNS / 端點</th><th>說明</th></tr>\n<tr><td>ERP Identity（外部主應用 <code>/Account/*</code>）</td><td>內建 Razor Pages <code>/Account/Login</code>（種 4 個 seed user）</td><td><code>http://erp-token.local/Account/Login</code></td><td>模擬 ERP 主應用登入；正式環境此路由由 ERP Core Team 處理</td></tr>\n<tr><td>N8N Workflow（外部呼叫者）</td><td><code>curl -H &quot;Authorization: Bearer ...&quot;</code> 直接呼叫 <code>/api/poc/*</code></td><td>—</td><td>不需起 N8N 容器；US-N8N-005 整合測試以 curl 模擬</td></tr>\n<tr><td>OpenTelemetry Collector → Jaeger（觀測）</td><td>jaeger-aio Deployment（all-in-one image）</td><td><code>jaeger-aio:4317</code> (OTLP) / <code>jaeger-aio:16686</code> (UI)</td><td>本地選配；prod 走 OTEL collector + 集中 Jaeger</td></tr>\n<tr><td>Email / SMS / Payment</td><td>—</td><td>—</td><td><strong>N/A</strong>：本模組 MVP 不發送 email、不送 SMS、不接金流（EDD §1.5 / BRD scope）</td></tr>\n<tr><td>Redis / Cache</td><td>—</td><td>—</td><td><strong>N/A</strong>：EDD §3.3 Cache=None（即時撤銷設計，不做 Token 狀態快取）</td></tr>\n<tr><td>Object Storage（MinIO / S3）</td><td>—</td><td>—</td><td><strong>N/A</strong>：本模組無檔案上傳 / 物件儲存需求</td></tr>\n<tr><td>Mailpit（SMTP trap）</td><td>—</td><td>—</td><td><strong>N/A</strong>：本模組不發送 email</td></tr>\n</table>\n<blockquote><p>因 EDD §2.1 系統上下文圖中外部依賴僅有 <code>ERP Identity</code> 與 <code>N8N</code>（皆可由內部模擬），本地無需起額外 mock service。BC 隔離測試以 ArchUnit / Test 完成（test-plan.md §3），無 WireMock 需求。</p></blockquote>\n<hr>\n<h2 id=\"15-inner-loop--快速迭代開發\">15. Inner Loop — 快速迭代開發</h2>\n<p>每次修改程式碼後不需要重建整個環境。對 ASP.NET Core 8 Razor Pages + Alpine.js + Tailwind 的迭代策略：</p>\n<h3 id=\"後端c--aspnet-core--razor-pages\">後端（C# / ASP.NET Core / Razor Pages）</h3>\n<pre><code class=\"language-bash\"># 方法 1：重 build + rolling restart（約 30-60 秒）\nmake image-build-api &amp;&amp; make k8s-restart-api\n\n# 方法 2：使用 skaffold（自動偵測檔案變更，約 10-20 秒）\nskaffold dev --profile local\n# skaffold 會 watch src/，變更後自動 build + load + restart pod</code></pre>\n<h3 id=\"前端web--razor-pages--alpinejs--tailwind\">前端（Web — Razor Pages + Alpine.js + Tailwind）</h3>\n<p>依 FRONTEND.md §2.3，前端為 Razor Pages server-rendered + CDN Alpine + CDN Tailwind（dev mode），無獨立 SPA，<strong>不適用 Vite/webpack HMR</strong>。迭代策略：</p>\n<pre><code class=\"language-bash\"># 方法 1：修改 .cshtml 後重 build image + rollout\n# Razor 視圖 .cshtml 為 build-time 編譯，需重 build image\nmake image-build-api &amp;&amp; make k8s-restart-api\n\n# 方法 2：dotnet watch（適合純後端 + Razor 改動）\n# 在本機（非 K8s）跑 dotnet watch，連線 port-forward 的 postgres：\nmake pf-db\ncd src/ErpApplication\nASPNETCORE_ENVIRONMENT=Development \\\n  ConnectionStrings__ApiTokenDb=&quot;Host=localhost;Port=5432;Database=erpapikey;Username=erpapp;Password=$(kubectl get secret erp-token-db-secret -n erp-token-local -o jsonpath=&#x27;{.data.POSTGRES_PASSWORD}&#x27; | base64 -d)&quot; \\\n  dotnet watch run\n# .cshtml / .cs 修改後自動 rebuild + reload，瀏覽器自動刷新\n# WARNING: 此密碼來自本機隨機 Secret；切勿用於 staging / production。\n\n# 方法 3：Tailwind CLI watch（若使用 production CSS bundle 模式）\ncd src/ErpApplication\nnpx tailwindcss -i ./wwwroot/css/input.css -o ./wwwroot/css/site.css --watch</code></pre>\n<blockquote><p><strong>不適用本專案的引擎特定段落（依 LOCAL_DEPLOY 模板）：</strong></p></blockquote>\n<blockquote><p>- <strong>Cocos Creator inner loop</strong> — N/A（FRONTEND.md §2.3.1 標記 Cocos N/A，本產品非遊戲）</p></blockquote>\n<blockquote><p>- <strong>Unity WebGL inner loop</strong> — N/A（同上，無 Unity Editor build）</p></blockquote>\n<blockquote><p>- <strong>Phaser inner loop</strong> — N/A（同上）</p></blockquote>\n<h3 id=\"使用-skaffold完整-inner-loop\">使用 skaffold（完整 inner loop）</h3>\n<p><code>skaffold.yaml</code> 已設定在 repo root，<code>local</code> profile 對應 <code>k8s/overlays/local/</code>：</p>\n<pre><code class=\"language-bash\"># 啟動 skaffold watch 模式（Ctrl+C 停止）\nskaffold dev --profile local\n\n# 一次性 build + deploy（不 watch）\nskaffold run --profile local\n\n# 清除 skaffold 部署的資源\nskaffold delete --profile local</code></pre>\n<hr>\n<h2 id=\"16-cleanup\">16. Cleanup</h2>\n<h3 id=\"停止-port-forward\">停止 port-forward</h3>\n<pre><code class=\"language-bash\">make pf-stop\n# 等同：\n# pkill -f &#x27;kubectl port-forward.*erp-token-local&#x27;</code></pre>\n<h3 id=\"刪除所有-k8s-資源保留-pvc-資料\">刪除所有 K8s 資源（保留 PVC 資料）</h3>\n<pre><code class=\"language-bash\">make k8s-delete\n# 等同：\nkubectl delete -k k8s/overlays/local/</code></pre>\n<h3 id=\"完全重置刪除所有資源--資料\">完全重置（刪除所有資源 + 資料）</h3>\n<pre><code class=\"language-bash\">make k8s-clean\n# 等同：\nkubectl delete namespace erp-token-local\n# Warning: 所有 PVC（postgres、pgadmin 資料）永久刪除</code></pre>\n<h3 id=\"重建本地環境一條龍\">重建本地環境（一條龍）</h3>\n<pre><code class=\"language-bash\">make k8s-clean &amp;&amp; make k8s-init &amp;&amp; make image-build-all &amp;&amp; make k8s-apply &amp;&amp; \\\n  kubectl wait --for=condition=Ready pods --all -n erp-token-local --timeout=180s &amp;&amp; \\\n  make db-migrate &amp;&amp; make db-seed &amp;&amp; make health-check</code></pre>\n<h3 id=\"確認清除乾淨\">確認清除乾淨</h3>\n<pre><code class=\"language-bash\">kubectl get namespace erp-token-local\n# Expected: Error from server (NotFound): namespaces &quot;erp-token-local&quot; not found\n\nnerdctl --namespace=k8s.io images | grep erp-token\n# 若需清除 local image：\nnerdctl --namespace=k8s.io rmi erp-token/api:local</code></pre>\n<hr>\n<h2 id=\"17-tunnel-access單一對外-portai-可測試模式\">17. Tunnel Access（單一對外 Port，AI 可測試模式）</h2>\n<p>所有服務均透過同一個 Ingress（port 80）路由，tunnel 只需暴露一個 port。適合 AI agent 自動化測試、QA 驗收、跨團隊協作。</p>\n<h3 id=\"ngrok推薦\">ngrok（推薦）</h3>\n<pre><code class=\"language-bash\"># 安裝（一次性）\nbrew install ngrok\n\n# 啟動 tunnel → Ingress port 80\nngrok http 80 --host-header=erp-token.local\n\n# 輸出範例：\n# Forwarding  https://a1b2c3d4.ngrok-free.app -&gt; http://localhost:80\n# 記錄此 URL，後續測試步驟使用 _TUNNEL_URL\n_TUNNEL_URL=&quot;https://a1b2c3d4.ngrok-free.app&quot;</code></pre>\n<h3 id=\"cloudflare-tunnel無公開-url-限制\">Cloudflare Tunnel（無公開 URL 限制）</h3>\n<pre><code class=\"language-bash\"># 安裝 cloudflared（一次性）\nbrew install cloudflared\n\n# 快速 tunnel（dev mode，無需帳號）\ncloudflared tunnel --url http://localhost:80\n\n# 輸出範例：\n# https://random-name.trycloudflare.com\n_TUNNEL_URL=&quot;https://random-name.trycloudflare.com&quot;</code></pre>\n<h3 id=\"設定-ingress-host-headerngrok--tunnel-需\">設定 Ingress Host header（ngrok / tunnel 需）</h3>\n<p>Ingress 預設只接受 <code>erp-token.local</code>，tunnel 需額外設定：</p>\n<pre><code class=\"language-bash\"># 方法 1：啟動 ngrok 時加 --host-header（推薦）\nngrok http 80 --host-header=erp-token.local\n\n# 方法 2：Ingress 加 wildcard host（更彈性）\n# 編輯 k8s/overlays/local/patches/ingress-tunnel.yaml：\n# spec.rules[0].host: &quot;&quot;  # 空字串 = 接受所有 host\nkubectl apply -k k8s/overlays/local/</code></pre>\n<h3 id=\"驗證-tunnel-可存取\">驗證 Tunnel 可存取</h3>\n<pre><code class=\"language-bash\"># Razor Pages UI 可存取（HTTP 200，登入頁）\ncurl -s -o /dev/null -w &quot;%{http_code}&quot; &quot;${_TUNNEL_URL}/&quot;\n# Expected: 200\n\n# Health Ready 可存取\ncurl -s &quot;${_TUNNEL_URL}/health/ready&quot;\n# Expected: Healthy\n\n# POC API health（不需 Token）\ncurl -s &quot;${_TUNNEL_URL}/api/poc/health&quot; | jq .\n# Expected: {&quot;status&quot;:&quot;ok&quot;,&quot;version&quot;:&quot;1.0.0&quot;,...}\n\n# 開啟瀏覽器測試前端\nopen &quot;${_TUNNEL_URL}&quot;</code></pre>\n<hr>\n<h2 id=\"18-ai-agent-quick-start\">18. AI Agent Quick Start</h2>\n<p>AI 代理可執行以下完整腳本，從零啟動完整本地環境並驗證前端可存取，<strong>無需任何人工介入</strong>。</p>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\n# ai-quickstart.sh — AI 一鍵啟動 ERP API Token Manager 本地 K8s 環境\nset -e\n\necho &quot;=== [1/6] 確認 kubectl context ===&quot;\nkubectl config use-context rancher-desktop\nkubectl cluster-info\n\necho &quot;=== [2/6] 設定 /etc/hosts（AI 環境若已預設則跳過）===&quot;\ngrep -q &quot;erp-token.local&quot; /etc/hosts || \\\n  echo &quot;127.0.0.1   erp-token.local api.erp-token.local&quot; | sudo tee -a /etc/hosts\n\necho &quot;=== [3/6] 初始化 namespace + Secret（隨機生成所有密碼）===&quot;\nmake k8s-init   # 等同 bash scripts/bootstrap-secrets.sh\n\necho &quot;=== [4/6] 建置 Image + 部署 K8s 資源 ===&quot;\nmake image-build-all\nmake k8s-apply\nkubectl wait --for=condition=Ready pods --all \\\n  -n erp-token-local --timeout=300s\n\necho &quot;=== [5/6] 初始化資料庫（EF Core migration + seed）===&quot;\nmake db-migrate\nmake db-seed\n\necho &quot;=== [6/6] 驗證環境健康 ===&quot;\nmake health-check\n\necho &quot;&quot;\necho &quot;OK: ERP API Token Manager 本地環境已就緒&quot;\necho &quot;   主 UI         → http://erp-token.local/&quot;\necho &quot;   API health    → http://erp-token.local/health/ready&quot;\necho &quot;   POC API       → http://erp-token.local/api/poc/health&quot;\necho &quot;   Test login    → admin@erp-token.local / Password1!&quot;\necho &quot;&quot;\necho &quot;如需 Tunnel 存取（AI 測試 / ngrok）：&quot;\necho &quot;  ngrok http 80 --host-header=erp-token.local&quot;</code></pre>\n<blockquote><p><strong>AI 測試前端的完整流程</strong>：執行 <code>ai-quickstart.sh</code> → 啟動 ngrok tunnel → 使用 Playwright 開啟 <code>_TUNNEL_URL</code> 驗證前端頁面可見、主要功能可操作（見 <code>features/client/*.feature</code>）。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>質量要求</strong>：此腳本可被 AI 直接 copy-paste 執行，不含任何需要人工決策的步驟。Local 環境密碼全為隨機生成，不需從 1Password / Vault 取得；<code>.gitignore</code> 已涵蓋 <code><em>.env</code>、<code>secrets.env</code>、<code>.env.</em></code>，無 secret 洩漏風險。</p></blockquote>\n<hr>\n<h2 id=\"19-docker-compose輔助方案\">19. Docker Compose（輔助方案）</h2>\n<blockquote><p><strong>定位</strong>：Docker Compose 是 K8s 的<strong>輔助工具</strong>，非主要部署方式。本專案 repo 已提供 <code>docker-compose.yml</code> + <code>docker-compose.dev.yml</code>（gendoc-gen-contracts 生成），用於以下場景：</p></blockquote>\n<blockquote><p>- 不熟悉 K8s 的快速驗證（如單純 API smoke test）</p></blockquote>\n<blockquote><p>- CI 環境中的輕量 integration test（GitHub Actions runner）</p></blockquote>\n<blockquote><p>- VS Code Dev Containers 入門</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>所有功能與行為必須與 K8s 環境一致</strong>；如有差異，以 K8s 為準。</p></blockquote>\n<h3 id=\"服務對照\">服務對照</h3>\n<table>\n<tr><th>K8s Deployment / StatefulSet</th><th>Docker Compose Service</th><th>Image</th></tr>\n<tr><td>erp-api-token-manager</td><td><code>erp-app</code></td><td><code>erp-api-token-manager:latest</code>（local build）</td></tr>\n<tr><td>postgres</td><td><code>postgres</code></td><td><code>postgres:16-alpine</code></td></tr>\n<tr><td>pgadmin（local-only）</td><td><code>pgadmin</code></td><td><code>dpage/pgadmin4:8.x</code></td></tr>\n<tr><td>jaeger-aio（dev override）</td><td><code>jaeger</code></td><td><code>jaegertracing/all-in-one:1.57</code></td></tr>\n</table>\n<blockquote><p><strong>Iron Constraint — Replicas ≥ 2（Docker Compose）</strong>：當以 docker-compose 模式驗證 HA 邏輯時，<code>services.erp-app.deploy.replicas</code> 必須 ≥ 2。docker-compose 預設不支援負載分流，需配合 §19.1 nginx proxy 才能正確路由至兩個副本。</p></blockquote>\n<p>合規 docker-compose 範例：</p>\n<pre><code class=\"language-yaml\">services:\n  erp-app:\n    image: erp-api-token-manager:latest\n    deploy:\n      replicas: 2  # 本地 HA 最小副本數，禁止填 1\n    expose:\n      - &quot;8080&quot;\n    environment:\n      - ASPNETCORE_ENVIRONMENT=Development\n      - ConnectionStrings__ApiTokenDb=Host=postgres;Port=5432;Database=erpapikey;Username=${POSTGRES_USER:-erpapp};Password=${POSTGRES_PASSWORD}</code></pre>\n<p>副本數驗證：</p>\n<pre><code class=\"language-bash\">docker compose ps\n# Expected: erp-app 服務顯示 2 個 Running 實例（含 _1 / _2 後綴）\n\ndocker compose ps --filter &quot;status=running&quot; --services | xargs -I{} sh -c \\\n  &#x27;echo &quot;{}: $(docker compose ps -q {} | wc -l)&quot;&#x27;\n# Expected: erp-app: 2</code></pre>\n<h3 id=\"啟動步驟\">啟動步驟</h3>\n<pre><code class=\"language-bash\"># 1. 確認 Docker / containerd 執行中（Rancher Desktop container engine 即可）\ndocker info\n\n# 2. 設定 .env（從範例複製，本機假值）\n[[ ! -f .env ]] &amp;&amp; cp .env.example .env\n\n# 3. 建置所有 image\ndocker compose build\n\n# 4. 啟動所有服務（背景執行）\ndocker compose --profile dev up -d\n# 含 dev override：jaeger + dotnet watch + pgadmin\n\n# 5. 確認所有容器啟動\ndocker compose ps\n# Expected: 所有服務 Status = &quot;Up&quot; / &quot;running&quot;\n\n# 6. 初始化資料庫\ndocker compose exec erp-app dotnet ef database update --no-build\ndocker compose exec erp-app dotnet run --project src/Infrastructure -- seed --env Local\n\n# 7. 驗證 API 健康\ncurl -s http://localhost:8080/health/ready\n# Expected: Healthy (HTTP 200)\n\n# 8. 驗證 POC（無需 Token）\ncurl -s http://localhost:8080/api/poc/health | jq .\n# Expected: {&quot;status&quot;:&quot;ok&quot;,&quot;version&quot;:&quot;1.0.0&quot;,...}</code></pre>\n<h3 id=\"對外-portdocker-compose-模式\">對外 Port（docker-compose 模式）</h3>\n<table>\n<tr><th>服務</th><th>本地 Port</th><th>說明</th></tr>\n<tr><td>erp-app（api-server）</td><td><code>8080</code></td><td>REST API + Razor Pages（單一 port，無分離前端）</td></tr>\n<tr><td>postgres</td><td><code>5432</code></td><td>DB（直連）</td></tr>\n<tr><td>pgadmin</td><td><code>5050</code></td><td>DB 瀏覽器（dev override）</td></tr>\n<tr><td>jaeger UI</td><td><code>16686</code></td><td>分散式追蹤 UI（dev override）</td></tr>\n<tr><td>jaeger OTLP gRPC</td><td><code>4317</code></td><td>OTLP collector（dev override）</td></tr>\n</table>\n<blockquote><p><strong>注意</strong>：docker-compose 模式預設各服務各自對外暴露 port，與 K8s 單一 port 80 模式不同。如需與 K8s 一致的單一 port 80 模式，需加入 §19.1 nginx proxy。E2E 測試 <code>--base-url</code> 也需對應調整：K8s 模式 <code>http://erp-token.local</code>，docker-compose 模式 <code>http://localhost:8080</code>。</p></blockquote>\n<h3 id=\"191-docker-compose-單一-port-nginx-proxy與-k8s-ingress-行為一致\">19.1 Docker Compose 單一 Port Nginx Proxy（與 K8s Ingress 行為一致）</h3>\n<blockquote><p>本專案 has_admin_backend=false，無 Admin Portal SPA；單一 port 模式僅需路由 <code>/api/*</code> 與 <code>/</code>（Razor Pages），不需處理 admin 路徑。若未來引入 Admin SPA，需依 LOCAL_DEPLOY 模板 §19.1 補充 admin upstream + nginx SPA fallback。</p></blockquote>\n<p><strong><code>docker/nginx-proxy/nginx.conf</code>：</strong></p>\n<pre><code class=\"language-nginx\">events { worker_connections 1024; }\n\nhttp {\n    upstream api_backend {\n        server erp-app:8080;\n    }\n\n    server {\n        listen 80;\n        server_name localhost erp-token.local;\n\n        # Razor Pages + REST API + health 全部走同一 upstream\n        location / {\n            proxy_pass http://api_backend;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n        }\n    }\n}</code></pre>\n<p><strong><code>docker-compose.yml</code>（新增 nginx proxy service）：</strong></p>\n<pre><code class=\"language-yaml\">services:\n  # ... 現有 services（erp-app, postgres）...\n\n  proxy:\n    image: nginx:alpine\n    ports:\n      - &quot;80:80&quot;        # 對外只暴露 port 80（與 K8s Ingress 一致）\n    volumes:\n      - ./docker/nginx-proxy/nginx.conf:/etc/nginx/nginx.conf:ro\n    depends_on:\n      - erp-app\n    healthcheck:\n      test: [&quot;CMD&quot;, &quot;wget&quot;, &quot;-q&quot;, &quot;--spider&quot;, &quot;http://localhost/health/live&quot;]\n      interval: 10s\n      timeout: 5s\n      retries: 3</code></pre>\n<p>啟動並驗證單一 port 模式：</p>\n<pre><code class=\"language-bash\">docker compose up -d\n\n# 驗證單一 port 路由\ncurl -s -o /dev/null -w &quot;%{http_code}&quot; http://localhost/\n# Expected: 200（Razor Pages Login 頁）\n\ncurl -s -o /dev/null -w &quot;%{http_code}&quot; http://localhost/health/ready\n# Expected: 200\n\ncurl -s http://localhost/api/poc/health | jq .\n# Expected: {&quot;status&quot;:&quot;ok&quot;,...}</code></pre>\n<h3 id=\"測試docker-compose-模式\">測試（docker-compose 模式）</h3>\n<pre><code class=\"language-bash\"># Unit test（不需 Docker，直接在本機執行）\ndotnet test src/Application.Tests/Application.Tests.csproj --logger &quot;console;verbosity=normal&quot;\n\n# Integration test（在 erp-app container 內執行；Testcontainers 會自啟 postgres，但這裡直接用 compose 的 postgres）\ndocker compose exec erp-app dotnet test src/Infrastructure.IntegrationTests/\n\n# E2E test（Playwright，base URL 對應 docker-compose port）\nE2E_BASE_URL=http://localhost:8080 \\\n  dotnet test src/E2ETests/\n# 或單一 port 模式：\nE2E_BASE_URL=http://localhost \\\n  dotnet test src/E2ETests/</code></pre>\n<h3 id=\"停止與清除\">停止與清除</h3>\n<pre><code class=\"language-bash\"># 停止（保留資料）\ndocker compose stop\n\n# 停止並移除 container（保留 volume 資料）\ndocker compose down\n\n# 完全重置（移除 container + volume）\ndocker compose down -v\n# Warning: 所有 DB 資料永久刪除</code></pre>\n<hr>\n<h2 id=\"20-k9s-quick-reference互動式-k8s-操作\">20. k9s Quick Reference（互動式 K8s 操作）</h2>\n<blockquote><p><strong>定位</strong>：k9s 是 kubectl 的互動式補充層，適合 on-call 快速診斷與資源瀏覽。所有操作等同 kubectl 指令，但以鍵盤驅動，不適合腳本化。<code>kubectl</code> 仍是 canonical 工具（腳本、CI、Makefile 均用 kubectl）；k9s 是 power user 效率層。</p></blockquote>\n<h3 id=\"啟動\">啟動</h3>\n<pre><code class=\"language-bash\"># 指定 namespace 啟動（推薦）\nk9s -n erp-token-local\n\n# 全 cluster 啟動\nk9s --all-namespaces</code></pre>\n<h3 id=\"資源切換命令模式按--進入後輸入\">資源切換（命令模式，按 <code>:</code> 進入後輸入）</h3>\n<table>\n<tr><th>命令</th><th>說明</th></tr>\n<tr><td><code>:pod</code></td><td>查看所有 Pod</td></tr>\n<tr><td><code>:deploy</code></td><td>查看 Deployment</td></tr>\n<tr><td><code>:sts</code></td><td>查看 StatefulSet（postgres）</td></tr>\n<tr><td><code>:svc</code></td><td>查看 Service</td></tr>\n<tr><td><code>:cm</code></td><td>查看 ConfigMap</td></tr>\n<tr><td><code>:secret</code></td><td>查看 Secret</td></tr>\n<tr><td><code>:hpa</code></td><td>查看 HorizontalPodAutoscaler</td></tr>\n<tr><td><code>:job</code></td><td>查看 Job（一次性 migration job 適用）</td></tr>\n<tr><td><code>:cronjob</code></td><td>查看 CronJob（本專案無，預留）</td></tr>\n<tr><td><code>:node</code></td><td>查看 Node</td></tr>\n<tr><td><code>:ns</code></td><td>切換 Namespace</td></tr>\n<tr><td><code>:pvc</code></td><td>查看 PersistentVolumeClaim（postgres / pgadmin）</td></tr>\n<tr><td><code>:event</code></td><td>查看 Events（等同 <code>kubectl get events</code>）</td></tr>\n<tr><td><code>:ing</code></td><td>查看 Ingress</td></tr>\n</table>\n<h3 id=\"常用按鍵resource-選中後\">常用按鍵（Resource 選中後）</h3>\n<table>\n<tr><th>按鍵</th><th>說明</th><th>kubectl 等效</th></tr>\n<tr><td><code>l</code></td><td>查看 logs（即時串流）</td><td><code>kubectl logs -f</code></td></tr>\n<tr><td><code>p</code></td><td>查看前次容器 logs（CrashLoopBackOff 用）</td><td><code>kubectl logs --previous</code></td></tr>\n<tr><td><code>s</code></td><td>exec 進入 container shell</td><td><code>kubectl exec -it -- bash</code></td></tr>\n<tr><td><code>d</code></td><td>describe</td><td><code>kubectl describe</code></td></tr>\n<tr><td><code>e</code></td><td>edit（直接編輯 YAML）</td><td><code>kubectl edit</code></td></tr>\n<tr><td><code>ctrl-d</code></td><td>delete resource（會要求確認）</td><td><code>kubectl delete</code></td></tr>\n<tr><td><code>shift-f</code></td><td>port-forward</td><td><code>kubectl port-forward</code></td></tr>\n<tr><td><code>f</code></td><td>fullscreen logs 切換</td><td>—</td></tr>\n<tr><td><code>/</code></td><td>文字篩選（過濾清單，支援正則）</td><td><code>grep</code></td></tr>\n<tr><td><code>Esc</code></td><td>返回上層 / 清除篩選</td><td>—</td></tr>\n<tr><td><code>?</code></td><td>顯示所有按鍵說明</td><td>—</td></tr>\n<tr><td><code>q</code></td><td>退出 k9s</td><td>—</td></tr>\n</table>\n<h3 id=\"篩選技巧\">篩選技巧</h3>\n<pre><code class=\"language-bash\"># 在 :pod 清單中篩選 api server\n/erp-api-token-manager\n\n# 在 :event 中篩選 Warning 事件\n/Warning\n\n# 在 :pod 中篩選崩潰中的 Pod\n/CrashLoop\n\n# 在 :secret 中篩選 erp-token 相關\n/erp-token</code></pre>\n<h3 id=\"scale-deployment互動式\">Scale Deployment（互動式）</h3>\n<pre><code class=\"\">1. 進入 k9s -n erp-token-local\n2. 輸入 :deploy\n3. 選中 erp-api-token-manager\n4. 按 s → 輸入新 replica 數（≥ 2，Iron Constraint）→ 確認</code></pre>\n<h3 id=\"皮膚與設定\">皮膚與設定</h3>\n<pre><code class=\"language-bash\"># k9s 設定目錄\nls ~/.config/k9s/\n\n# 設定 default namespace（config.yml 範例）\n# currentContext: rancher-desktop\n# namespace: erp-token-local\n\n# 推薦 skin（dracula 或 deep-sea）\n# 下載：https://github.com/derailed/k9s/tree/master/skins</code></pre>\n<h3 id=\"與-makefile-整合\">與 Makefile 整合</h3>\n<pre><code class=\"language-bash\"># §6 Development Commands 已提供\nmake k9s   # 等同 k9s -n erp-token-local</code></pre>\n<hr>\n<h2 id=\"21-cicd-本地模擬github-actions-act-runner\">21. CI/CD 本地模擬（GitHub Actions act runner）</h2>\n<blockquote><p>本節讓開發者在本機完整重現 CI/CD Pipeline，確保 <code>.github/workflows/<em>.yml</code> 和 <code>make ci-</em></code> targets 在 commit 前可驗證，避免 PR 被 pipeline 失敗 reject。</p></blockquote>\n<p>&gt;</p>\n<blockquote><p><strong>CI 工具選擇</strong>：依 RUNBOOK §1.1 / EDD §3.4，本專案使用 <strong>GitHub Actions</strong>（CI）+ <strong>ArgoCD</strong>（CD）。LOCAL_DEPLOY 模板 §21 預設 Jenkins，本節改用 <code>act</code> runner 模擬 GitHub Actions。</p></blockquote>\n<h3 id=\"210-local-developer-platform-整體架構\">21.0 Local Developer Platform 整體架構</h3>\n<p>本節說明本地 CI/CD 平台的整體架構。所有元件均在 Rancher Desktop k3s cluster 內執行（除 <code>act</code> 為本機進程），開發者 push 到本地 Gitea 即可觸發完整 CI/CD 流程，不依賴任何外部服務。</p>\n<div class=\"diagram-container\"><pre class=\"mermaid\">graph TD\n    subgraph DevMachine[&quot;開發者本機&quot;]\n        subgraph Local[&quot;本機進程&quot;]\n            Act[&quot;act runner&lt;br/&gt;(本機進程，模擬 GH Actions)&quot;]\n        end\n        subgraph DevTools[&quot;dev-tools namespace&quot;]\n            Gitea[&quot;Gitea&lt;br/&gt;(ClusterIP:3000)&lt;br/&gt;Local Git Server&quot;]\n        end\n        subgraph ArgoNS[&quot;argocd namespace&quot;]\n            Argo[&quot;ArgoCD&lt;br/&gt;(ClusterIP:443)&lt;br/&gt;CD GitOps&quot;]\n        end\n        subgraph App[&quot;erp-token-local namespace&quot;]\n            AppPods[&quot;erp-api-token-manager&lt;br/&gt;(Ingress port 80)&lt;br/&gt;postgres / pgadmin&quot;]\n        end\n    end\n    Dev[&quot;Developer&lt;br/&gt;git push&quot;] --&gt; Gitea\n    Dev --&gt;|&quot;act push&quot;| Act\n    Act --&gt;|&quot;build image\\nrun tests\\nupdate manifest&quot;| Gitea\n    Argo --&gt;|&quot;watch repo&quot;| Gitea\n    Argo --&gt;|&quot;sync&quot;| AppPods</pre></div>"
}